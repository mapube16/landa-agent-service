---
phase: 04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0002_payment_tables.py
  - alembic/env.py
  - app/memory/case_store.py
  - app/memory/__init__.py
  - app/features/qa/state.py
  - app/features/payment/__init__.py
  - app/features/payment/graph.py
  - app/features/payment/nodes.py
  - app/features/payment/business_hours.py
  - app/features/payment/tests/__init__.py
  - app/features/payment/tests/test_business_hours.py
  - app/security/output_firewall.py
  - app/security/tests/test_output_firewall.py
  - app/config/settings.py
  - .env.example
autonomous: true
requirements:
  - D-01
  - D-02
  - D-06
  - D-10
  - D-13
  - D-14
  - D-22
  - D-23
  - D-28
user_setup:
  - service: railway-volume
    why: "Persistent storage for comprobantes (D-01)"
    dashboard_config:
      - task: "Provision 1 GB volume on landa-agent-service mounted at /data/comprobantes, alert at 70%"
        location: "Railway dashboard -> landa-agent-service -> Volumes"
  - service: env-vars
    why: "Phase 4 settings"
    env_vars:
      - name: CARTERA_PHONE_ALLOWLIST
        source: "CSV of E.164 numbers authorized as cartera (D-06)"
      - name: LAMBDA_PROYECT_INTERNAL_TOKEN
        source: "Shared secret with lambda-proyect repo (D-23)"
      - name: PAYMENT_VOLUME_PATH
        source: "Defaults to /data/comprobantes (D-01)"
      - name: META_TEMPLATE_NO_ANSWER_NAME
        source: "Defaults to voice_no_answer_followup (D-19/D-22)"
      - name: CHATWOOT_WEBHOOK_SECRET
        source: "Chatwoot admin -> Settings -> Integrations -> Webhooks (D-15)"

must_haves:
  truths:
    - "Postgres has tables `cases` and `attachments` after `alembic upgrade head`"
    - "QAState carries payment-flow fields without breaking Q&A graph"
    - "Settings parse CARTERA_PHONE_ALLOWLIST CSV into a frozenset of E.164 strings"
    - "Output firewall blocks 'pago confirmado' text when payment_approved flag is False"
    - "next_business_window_after correctly skips weekends + lunch and is anchored to America/Bogota"
  artifacts:
    - path: "alembic/versions/0002_payment_tables.py"
      provides: "Migration creating cases + attachments tables with indexes"
      contains: "def upgrade"
    - path: "app/memory/case_store.py"
      provides: "SQLAlchemy Case + Attachment models on app.config.db.Base"
      contains: "class Case"
    - path: "app/features/payment/business_hours.py"
      provides: "is_business_time + next_business_window_after pure functions"
      exports: ["is_business_time", "next_business_window_after", "TZ_CO"]
    - path: "app/security/output_firewall.py"
      provides: "check_outbound deterministic regex gate"
      exports: ["check_outbound"]
    - path: "app/features/qa/state.py"
      provides: "QAState extended with payment fields"
      contains: "case_id"
  key_links:
    - from: "alembic/env.py"
      to: "app/memory/case_store"
      via: "import for autogenerate"
      pattern: "from app.memory import case_store"
    - from: "app/config/settings.py"
      to: "PaymentSettings + LambdaProyectSettings"
      via: "composite Settings"
      pattern: "payment: PaymentSettings"
---

<objective>
Lay the foundation for Phase 4 (D-01, D-02, D-06, D-10, D-13, D-14, D-22, D-23, D-28): Postgres schema, settings, QAState extensions, module skeletons, business-hours helper, and the deterministic output firewall. Wave 1 unblocks every other Phase 4 plan but ships zero runtime behavior changes for the existing Q&A flow.

Purpose: Establish the contracts (DB tables, settings fields, state fields, firewall function, business-hours helper) that Waves 2-5 consume. Keeping skeletons + contracts in one plan prevents the scavenger-hunt anti-pattern in downstream plans.

Output:
- Migration `0002_payment_tables.py` (cases + attachments + indexes per D-01).
- `app/memory/case_store.py` SQLAlchemy models registered on Base.
- `PaymentSettings` + `LambdaProyectSettings` + `CHATWOOT_WEBHOOK_SECRET` field in `ChatwootSettings`.
- `QAState` extended with `case_id`, `attachment_count`, `attachment_idx`, `payment_status`, `cartera_message_wamid`, `payment_approved`.
- `app/features/payment/graph.py` + `nodes.py` skeleton stubs (no logic yet, just module + node-name constants Wave 3 will fill).
- `business_hours.py` complete with unit tests.
- `output_firewall.py` complete with unit tests (Wave 5 wires it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona/04-CONTEXT.md
@.planning/phases/04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona/04-RESEARCH.md
@CLAUDE.md
@alembic/versions/0001_initial_schema.py
@alembic/env.py
@app/config/db.py
@app/config/settings.py
@app/features/qa/state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema migration + SQLAlchemy models + alembic registration</name>
  <files>app/memory/case_store.py, app/memory/__init__.py, alembic/versions/0002_payment_tables.py, alembic/env.py</files>
  <read_first>
    - alembic/versions/0001_initial_schema.py (migration style, upgrade/downgrade pattern)
    - alembic/env.py (the "import application models here" comment block)
    - app/config/db.py (Base, metadata, naming convention)
    - .planning/phases/04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona/04-RESEARCH.md §"Postgres Schema"
  </read_first>
  <behavior>
    - Case row inserts with default status='awaiting_receipt' and gen_random_uuid() PK.
    - Attachment row insert with case_id FK fails with IntegrityError if case_id missing.
    - DELETE CASCADE on cases removes attachments.
    - `alembic upgrade head` on a fresh DB creates both tables + 3 partial indexes on cases.
    - `from app.memory import case_store` resolves Case and Attachment classes attached to `app.config.db.Base.metadata`.
  </behavior>
  <action>
    Create `app/memory/case_store.py` with two SQLAlchemy 2.0 declarative models against `app.config.db.Base`:

    - `Case` (table `cases`): columns `case_id UUID PK default gen_random_uuid()`, `phone Text NOT NULL`, `poliza_id Text`, `cliente_doc Text`, `cliente_nombre Text`, `status Text NOT NULL default 'awaiting_receipt'` constrained to the enum values listed in RESEARCH (`awaiting_receipt`, `forwarded`, `awaiting_cartera`, `approved`, `rejected`, `escalated`, `closed`) via a `CheckConstraint` named `ck_cases_status`, `attachment_count Integer NOT NULL default 0`, `reminder_sent_at TIMESTAMPTZ`, `escalated_at TIMESTAMPTZ`, `work_hours_due_at TIMESTAMPTZ`, `cartera_message_wamid Text`, `created_at TIMESTAMPTZ NOT NULL server_default=func.now()`, `updated_at TIMESTAMPTZ NOT NULL server_default=func.now() onupdate=func.now()`.
    - `Attachment` (table `attachments`): columns `id BigInteger PK autoincrement`, `case_id UUID NOT NULL FK cases.case_id ondelete=CASCADE`, `path Text NOT NULL`, `mime_type Text NOT NULL`, `sha256 Text NOT NULL`, `size_bytes Integer NOT NULL`, `meta_media_id Text`, `received_at TIMESTAMPTZ NOT NULL server_default=func.now()`.

    Export `__all__ = ["Case", "Attachment"]` from `case_store.py`. Re-export both from `app/memory/__init__.py` so `from app.memory import case_store` and `from app.memory import Case` both work.

    In `alembic/env.py`, replace the commented `from app.memory import case_store  # noqa: F401` example with the live import (uncomment / add it where the comment block currently lives, do NOT move it elsewhere).

    Create `alembic/versions/0002_payment_tables.py` with:
    - `revision = "0002_payment_tables"`, `down_revision = "0001_initial_schema"` (read actual revision string from 0001).
    - `upgrade()` creates both tables with `op.create_table` (mirror the 0001 style — explicit columns, server_default=sa.text("gen_random_uuid()"), server_default=sa.text("now()")).
    - Then `op.create_index("ix_cases_phone", "cases", ["phone"])`.
    - `op.create_index("ix_cases_status_open", "cases", ["status"], postgresql_where=sa.text("status NOT IN ('approved', 'rejected', 'closed')"))`.
    - `op.create_index("ix_cases_work_hours_due_at", "cases", ["work_hours_due_at"], postgresql_where=sa.text("status = 'awaiting_cartera'"))`.
    - `op.create_index("ix_attachments_case_id", "attachments", ["case_id"])`.
    - `downgrade()` drops indexes then tables (attachments first, then cases — FK order).

    Per CLAUDE.md no-ABCs rule, use concrete SQLAlchemy `DeclarativeBase` subclass already in `app/config/db.py` — no new base.

    Do NOT run `alembic upgrade head` here; that's an operator step. Verify migration script imports cleanly only.
  </action>
  <verify>
    <automated>uv run python -c "from app.memory import case_store; from app.config.db import Base; assert 'cases' in Base.metadata.tables and 'attachments' in Base.metadata.tables; print('OK')" &amp;&amp; uv run alembic upgrade head --sql | grep -E "CREATE TABLE (cases|attachments)" | wc -l | grep -q "^2$" &amp;&amp; echo OK</automated>
  </verify>
  <done>
    Both tables exist on `Base.metadata`, `alembic upgrade head --sql` emits offline DDL for both tables and 4 indexes, `alembic/env.py` imports `case_store` so autogenerate would also see them.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings + QAState extensions + env.example</name>
  <files>app/config/settings.py, app/features/qa/state.py, .env.example</files>
  <read_first>
    - app/config/settings.py (existing BaseSettings classes, env_prefix pattern, NoDecode usage from F3)
    - app/features/qa/state.py (existing QAState TypedDict + node Literal)
    - .planning/phases/04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona/04-CONTEXT.md (D-06, D-22, D-23 env vars)
    - .planning/phases/04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona/04-RESEARCH.md §"New settings fields needed"
  </read_first>
  <behavior>
    - `Settings().payment.cartera_phone_allowlist` returns a frozenset[str] of E.164 numbers when env var `CARTERA_PHONE_ALLOWLIST="+573001,+573002"` is set; empty frozenset when unset.
    - `Settings().payment.volume_path` returns `Path("/data/comprobantes")` by default and respects `PAYMENT_VOLUME_PATH` override.
    - `Settings().payment.template_no_answer_name` defaults to `"voice_no_answer_followup"`, overridable via `META_TEMPLATE_NO_ANSWER_NAME`.
    - `Settings().lambda_proyect.internal_token` is a `SecretStr`; missing env var raises `ValidationError`.
    - `Settings().chatwoot.webhook_secret` is a `SecretStr`; missing env var raises `ValidationError`.
    - Importing `QAState` and instantiating with new fields type-checks under mypy --strict.
  </behavior>
  <action>
    In `app/config/settings.py`:

    1. Add `class PaymentSettings(BaseSettings)` with `model_config = SettingsConfigDict(env_prefix="PAYMENT_", ...)` mirroring the existing settings classes (use the same `env_file=".env"`, `extra="ignore"`, etc. that the others use — read existing file to copy exactly). Fields:
       - `cartera_phone_allowlist_raw: str = Field(default="", alias="CARTERA_PHONE_ALLOWLIST")` — read via top-level env name, not prefixed, because operator stores it as `CARTERA_PHONE_ALLOWLIST` per D-06.
       - `volume_path: Path = Field(default=Path("/data/comprobantes"))` reads from `PAYMENT_VOLUME_PATH`.
       - `template_no_answer_name: str = Field(default="voice_no_answer_followup", alias="META_TEMPLATE_NO_ANSWER_NAME")`.
       - `@property cartera_phone_allowlist(self) -> frozenset[str]` that splits `cartera_phone_allowlist_raw` on `,`, strips whitespace, drops empties, and returns a frozenset. Each entry must start with `+` (E.164); silently drop entries that don't.

    2. Add `class LambdaProyectSettings(BaseSettings)` with `env_prefix="LAMBDA_PROYECT_"`. Fields: `base_url: str = "http://localhost:8001"`, `internal_token: SecretStr` (no default — required).

    3. Add field `webhook_secret: SecretStr` to existing `ChatwootSettings` (read its env_prefix from the file; the alias should resolve to `CHATWOOT_WEBHOOK_SECRET`).

    4. Add `payment: PaymentSettings = Field(default_factory=PaymentSettings)` and `lambda_proyect: LambdaProyectSettings = Field(default_factory=LambdaProyectSettings)` to the composite `Settings` class.

    In `app/features/qa/state.py`:

    Extend the `QAState` TypedDict with:
    - `case_id: NotRequired[str | None]`
    - `attachment_count: NotRequired[int]`
    - `attachment_idx: NotRequired[int]`
    - `payment_status: NotRequired[Literal["none", "awaiting_receipt", "forwarded", "awaiting_cartera", "approved", "rejected", "escalated"] | None]`
    - `cartera_message_wamid: NotRequired[str | None]`
    - `payment_approved: NotRequired[bool]`

    Extend the existing `node` Literal type (if it exists in this file — verify by reading) with the payment node names: `"node_receive_comprobante"`, `"node_forward_to_cartera"`, `"node_awaiting_cartera"`, `"node_confirming"`, `"node_payment_escalate"`.

    In `.env.example`, append (at end, preserving alphabetical-ish grouping if file has it):

    # Phase 4 — Payment / Cartera (D-06, D-22, D-23, D-15)
    CARTERA_PHONE_ALLOWLIST=
    PAYMENT_VOLUME_PATH=/data/comprobantes
    META_TEMPLATE_NO_ANSWER_NAME=voice_no_answer_followup
    LAMBDA_PROYECT_BASE_URL=http://localhost:8001
    LAMBDA_PROYECT_INTERNAL_TOKEN=
    CHATWOOT_WEBHOOK_SECRET=


    Do not change any existing settings field; only additive.
  </action>
  <verify>
    <automated>uv run python -c "import os; os.environ['CARTERA_PHONE_ALLOWLIST']='+573001,+573002, bad,'; os.environ['LAMBDA_PROYECT_INTERNAL_TOKEN']='x'; os.environ['CHATWOOT_WEBHOOK_SECRET']='y'; from app.config.settings import Settings; s=Settings(); assert s.payment.cartera_phone_allowlist == frozenset({'+573001','+573002'}), s.payment.cartera_phone_allowlist; assert str(s.payment.volume_path)=='/data/comprobantes'; assert s.lambda_proyect.internal_token.get_secret_value()=='x'; from app.features.qa.state import QAState; print('OK')"</automated>
  </verify>
  <done>
    All settings parse correctly with sensible defaults; QAState importable with the 6 new fields typed correctly; .env.example documents every new env var.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: business_hours.py + output_firewall.py + payment module skeletons + tests</name>
  <files>app/features/payment/business_hours.py, app/features/payment/graph.py, app/features/payment/nodes.py, app/features/payment/tests/__init__.py, app/features/payment/tests/test_business_hours.py, app/security/output_firewall.py, app/security/tests/test_output_firewall.py</files>
  <read_first>
    - app/features/qa/graph.py (StateGraph + checkpointer pattern Wave 3 will mirror)
    - app/features/qa/nodes.py (node function signature shape)
    - app/security/judge.py (existing security module pattern, exports)
    - .planning/phases/04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona/04-RESEARCH.md §"Working Hours Algorithm" + §"Output Firewall Extension"
  </read_first>
  <behavior>
    test_business_hours.py:
    - is_business_time(Monday 10:00 Bogota) == True
    - is_business_time(Monday 13:00 Bogota) == False  (lunch)
    - is_business_time(Saturday 10:00 Bogota) == False
    - is_business_time(Friday 16:00 Bogota) == False  (end exclusive)
    - next_business_window_after(Friday 15:59 UTC-mapped) returns Monday 08:00 Bogota expressed as UTC.
    - next_business_window_after(Tuesday 13:00 Bogota → UTC) returns Tuesday 14:00 Bogota → UTC.
    - next_business_window_after(during a block) returns the input unchanged.
    test_output_firewall.py:
    - check_outbound("Tu pago fue confirmado para POL-123", payment_approved=False) returns (False, reason_str_containing_match).
    - check_outbound("Tu pago fue confirmado para POL-123", payment_approved=True) returns (True, None).
    - check_outbound("Pago aprobado.", payment_approved=False) returns (False, _).
    - check_outbound("Tu saldo es 100", payment_approved=False) returns (True, None).
    - check_outbound("tu pago fue REGISTRADO POL-9", payment_approved=False) returns (False, _) (case-insensitive).
  </behavior>
  <action>
    Create `app/features/payment/business_hours.py`. Module is pure (no imports from app.*, no I/O). Exports:

    - `TZ_CO = zoneinfo.ZoneInfo("America/Bogota")`
    - `WORKDAY_BLOCKS = ((8, 0, 12, 0), (14, 0, 16, 0))` — D-10 schedule.
    - `is_business_time(dt: datetime) -> bool`: accepts tz-aware datetime, converts to TZ_CO if not already, returns True iff weekday < 5 and dt falls within any block (start inclusive, end exclusive).
    - `next_business_window_after(dt_utc: datetime) -> datetime`: accepts tz-aware datetime (must be UTC; assert or raise ValueError if naive). If `is_business_time(dt_utc)` then returns `dt_utc`. Otherwise walks forward day-by-day (max 14 iterations safety bound) checking each block start. Returns a tz-aware UTC datetime.

    Implementation follows the algorithm in RESEARCH §"Working Hours Algorithm" exactly. No mutable module state.

    Create `app/features/payment/graph.py` as a skeleton:
    - Module docstring noting "Wave 3 (Plan 04-04) populates the payment subgraph; this skeleton only declares the node-name constants so other plans can import without runtime risk."
    - Constants: `NODE_RECEIVE_COMPROBANTE = "node_receive_comprobante"`, `NODE_FORWARD_TO_CARTERA = "node_forward_to_cartera"`, `NODE_AWAITING_CARTERA = "node_awaiting_cartera"`, `NODE_CONFIRMING = "node_confirming"`, `NODE_PAYMENT_ESCALATE = "node_payment_escalate"`.
    - `__all__` listing the five constants.
    - No `build_*` function yet (Plan 04-04 adds it).

    Create `app/features/payment/nodes.py` as a skeleton with the same docstring note plus module-level `from __future__ import annotations` and an empty `__all__: list[str] = []`. Plan 04-04 will fill it.

    Create `app/features/payment/tests/__init__.py` empty.

    Create `app/features/payment/tests/test_business_hours.py` covering the behavior listed above. Use `datetime` + `zoneinfo.ZoneInfo("America/Bogota")` to construct test inputs; convert to `datetime.timezone.utc` before calling `next_business_window_after`. Test cases must include the Friday→Monday transition and the lunch hole.

    Create `app/security/output_firewall.py`:
    - `import re` only.
    - `_PAYMENT_CONFIRMED_RE` compiled exactly as RESEARCH §"Output Firewall Extension" specifies: `r"pago\s+(confirmado|aprobado)|tu\s+pago\s+fue\s+(registrado|aceptado|recibido)"` with `re.IGNORECASE`. (D-28: the "followed by número de póliza" clause is enforced semantically by the firewall blocking ALL appearances without the flag — overly cautious is correct here.)
    - `check_outbound(text: str, payment_approved: bool) -> tuple[bool, str | None]`. If `_PAYMENT_CONFIRMED_RE.search(text)` matches and `payment_approved` is False, return `(False, f"payment_confirmation_without_approval: {match.group()!r}")`. Else return `(True, None)`.
    - `__all__ = ["check_outbound"]`.

    Create `app/security/tests/__init__.py` if it doesn't already exist (check first). Create `app/security/tests/test_output_firewall.py` with the test cases listed in behavior.

    Per CLAUDE.md: async by default for I/O, but these are pure functions — no async needed. No emojis in code.

    NOTE: Plan 04-05 wires `check_outbound` into `_run_and_dispatch`. This plan does NOT wire it (keeps Wave 1 zero-runtime-behavior-change for existing flow).
  </action>
  <verify>
    <automated>uv run pytest app/features/payment/tests/test_business_hours.py app/security/tests/test_output_firewall.py -x -q</automated>
  </verify>
  <done>
    All business_hours tests + output_firewall tests pass; payment graph + nodes skeletons importable; `from app.security.output_firewall import check_outbound` resolves.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → env vars | Operator sets CARTERA_PHONE_ALLOWLIST, LAMBDA_PROYECT_INTERNAL_TOKEN, CHATWOOT_WEBHOOK_SECRET — misconfiguration risk |
| app → Postgres | New DDL via alembic; concurrent reads/writes to cases/attachments later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | alembic migration replayed | mitigate | Migration uses standard alembic revision/down_revision chain; re-running is a no-op (idempotent within a version). |
| T-04-01-02 | Information disclosure | cases.cliente_doc + phone columns | accept | PII stored only when needed for case correlation; Phase 5 audit log will hash. Documented. |
| T-04-01-03 | Spoofing | empty CARTERA_PHONE_ALLOWLIST in prod | mitigate | Wave 3+ code MUST treat empty allowlist as "no cartera authorized" (silent discard for unknown numbers); not a Wave 1 risk because no runtime branch exists yet. Documented in user_setup. |
| T-04-01-04 | Elevation of privilege | "pago confirmado" leaks from LLM | mitigate (D-28) | `check_outbound` regex gate; Wave 5 wires into outbound dispatch. Function unit-tested in Wave 1. |
| T-04-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages added in this plan (alembic, sqlalchemy, pydantic already pinned). N/A for Wave 1. |
</threat_model>

<verification>
- `uv run alembic upgrade head` runs cleanly against a fresh Postgres (operator-driven smoke; not gated here).
- `uv run pytest app/features/payment/tests/ app/security/tests/test_output_firewall.py -q` passes.
- `uv run ruff check app/ && uv run black --check app/` clean.
- `uv run mypy --strict app/features/payment/business_hours.py app/security/output_firewall.py app/memory/case_store.py` clean.
- `grep -v '^#' app/features/qa/state.py | grep -c "case_id\|payment_status\|payment_approved"` &ge; 3.
</verification>

<success_criteria>
- Migration script created and registered.
- 8 settings env vars documented in `.env.example`.
- QAState carries 6 new payment fields without breaking the Q&A `node` Literal extension.
- business_hours covers Friday-end → Monday-start, weekend, and lunch transitions.
- output_firewall blocks "pago confirmado"/"pago aprobado"/"tu pago fue registrado|aceptado|recibido" when flag is False.
</success_criteria>

<output>
Create `.planning/phases/04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona/04-01-SUMMARY.md` when done.
</output>
