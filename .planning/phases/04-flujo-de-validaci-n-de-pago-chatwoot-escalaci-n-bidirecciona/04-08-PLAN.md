---
phase: 04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona
plan: 08
type: execute
wave: 5
depends_on:
  - 04-04
  - 04-05
  - 04-06
  - 04-07
files_modified:
  - app/webhooks/meta.py
  - app/integrations/chatwoot.py
  - app/security/output_firewall.py
  - tests/integration/__init__.py
  - tests/integration/conftest.py
  - tests/integration/test_payment_e2e.py
autonomous: false

requirements:
  - D-05
  - D-06
  - D-15
  - D-18
  - D-23
  - D-28

must_haves:
  truths:
    - "_send_outbound to client AND mirror to Chatwoot both pass through check_outbound; 'pago confirmado' is blocked when payment_approved flag is absent"
    - "Cliente envía imagen → aparece en cartera con caption D-08 y botones — confirmable por inspección de mock posts"
    - "Cartera tap aprobar → cliente recibe AIMessage con payment_approved=True; check_outbound permits it"
    - "Cartera tap rechazar → conversación queda asignada a agente en Chatwoot; cliente recibe la D-12 message"
    - "Inbound desde número fuera de allowlist es descartado silenciosamente (cero outbound, log present)"
    - "Agente humano envía texto en Chatwoot → cliente lo recibe vía Meta send_text"
    - "lambda dispara POST /case/handoff/no_answer → template Meta enviado; segunda llamada idempotente"
  artifacts:
    - path: "tests/integration/test_payment_e2e.py"
      provides: "End-to-end coverage of the 6 phase success criteria"
      contains: "test_happy_path_approve"
  key_links:
    - from: "_send_outbound (and Chatwoot mirror sibling)"
      to: "check_outbound"
      via: "guard before send"
      pattern: "check_outbound"
---

<objective>
Wire the output firewall into the outbound dispatch paths (D-28) and validate the full Phase 4 success criteria with integration tests. This plan is the gate before the phase is considered shippable: it both adds the last cross-cutting security control AND verifies the deliverables end-to-end.

Purpose: Plans 04-01 through 04-07 each landed a slice. This plan proves the slices fit together and that the "pago confirmado" deterministic gate works in the real outbound paths.

Output:
- `_send_outbound` (and the Chatwoot mirror that runs on the same code path) gated by `check_outbound`.
- End-to-end integration tests covering all 6 ROADMAP success criteria.
- Human-verify checkpoint at the end for the live smoke (mirrors F3 03-06 SMOKE pattern).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona/04-CONTEXT.md
@.planning/phases/04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona/04-RESEARCH.md
@app/webhooks/meta.py
@app/integrations/chatwoot.py
@app/security/output_firewall.py
@app/features/payment/nodes.py
@app/features/payment/graph.py
@app/features/payment/cartera.py
@app/features/payment/scheduler.py
@app/webhooks/handoff.py
@app/webhooks/chatwoot.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire check_outbound into _send_outbound + Chatwoot mirror outbound path</name>
  <files>app/webhooks/meta.py, app/integrations/chatwoot.py, app/security/output_firewall.py</files>
  <read_first>
    - app/webhooks/meta.py (`_run_and_dispatch`, `_send_outbound`, `_extract_outbound_message`; the AIMessage `additional_kwargs` access)
    - app/integrations/chatwoot.py (`post_message` — verify whether bot outbound mirror passes through here)
    - app/worker.py (`mirror_outbound` ARQ job — confirms whether the firewall must also gate Chatwoot mirroring or only the Meta send)
    - app/security/output_firewall.py (Plan 04-01 `check_outbound` signature: `(text, payment_approved) -> tuple[bool, str | None]`)
    - app/features/payment/nodes.py (`node_payment_escalate` — needed to call on block)
    - .planning/phases/04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona/04-RESEARCH.md §"Output Firewall Extension"
    - .planning/phases/04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona/04-CONTEXT.md D-28
  </read_first>
  <behavior>
    - When an AIMessage with content matching the firewall regex is emitted WITHOUT `additional_kwargs["payment_approved"]=True`: send is blocked, the firewall logs `output_firewall.payment_blocked` with the matched fragment, and the message is replaced with an escalation path (Chatwoot opened + client receives "La revisión requiere agente humano").
    - When the same content carries `payment_approved=True`: send proceeds normally.
    - The Chatwoot mirror of a blocked message is also suppressed (no leakage of the hallucinated confirmation).
    - The firewall does NOT affect any outbound that does not match the regex.
  </behavior>
  <action>
    Edit `app/webhooks/meta.py` `_send_outbound` (or its caller `_run_and_dispatch`, whichever currently owns the "send to Meta" call site — read to locate; the call typically lives right after `_extract_outbound_message`):

    1. Import: `from app.security.output_firewall import check_outbound`. Import `node_payment_escalate` is NOT needed — we just call the existing escalation helper. If F3 has `escalate_to_chatwoot(state, reason)`, reuse it; else inline the same Chatwoot calls (`get_or_create_conversation` + `post_message` with private note).

    2. Just before the `await meta.send_text(...)` (or `send_buttons`/`send_media` for outbound `AIMessage` content), compute:
       - `text = str(outbound_msg.content) if outbound_msg.content else ""`.
       - `payment_approved = bool(outbound_msg.additional_kwargs.get("payment_approved", False))`.
       - `allowed, reason = check_outbound(text, payment_approved)`.
    3. If `not allowed`:
       - `log.error("output_firewall.payment_blocked", reason=reason, phone_hash=_hash_phone(phone), wamid_in=wamid_in)`.
       - Send Sentry alert via the existing structlog → Sentry hook (if F1/F2 set one up; otherwise structlog.error is sufficient — F5 will harden).
       - Replace outbound: `await meta.send_text(phone, "La revisión requiere validación adicional. Te conecto con un agente humano.")` and call the escalation helper (open Chatwoot, assign to agent, post private note containing `reason`).
       - Do NOT enqueue `mirror_outbound` for the original blocked text (the substituted text + the private Chatwoot note replace it).
       - Return early.

    4. Else (allowed): proceed with the existing send path unchanged.

    Edit `app/integrations/chatwoot.py` if the outbound mirror path (called by ARQ `mirror_outbound` job) is independent of `_send_outbound`: add the same gate. Specifically: if `mirror_outbound` posts the AIMessage content to Chatwoot directly without going through `_send_outbound`, then gate at that point too — pass `payment_approved` as an extra ARQ kwarg from the enqueue site so the worker can re-check. Read `app/worker.py mirror_outbound` to confirm: if it currently takes only `(phone, text, wamid)` as kwargs, extend with `payment_approved: bool = False` and update the enqueue call in `_send_outbound` to forward `outbound_msg.additional_kwargs.get("payment_approved", False)`.

    Add tests inside `app/security/tests/test_output_firewall.py` (extend the file from Plan 04-01) covering the integration:
    - test_send_outbound_blocks_payment_text_without_flag: monkeypatch `meta.send_text` as AsyncMock; build an `AIMessage(content="Tu pago fue confirmado para POL-9", additional_kwargs={})`; assert `meta.send_text` was called with the SUBSTITUTED escalation text, not the original, and the chatwoot escalation helper was invoked.
    - test_send_outbound_allows_payment_text_with_flag: same content but `additional_kwargs={"payment_approved": True}`; assert `meta.send_text` called with the original text.
    - test_unrelated_text_passes_through: content="Tu saldo es 100"; sends as-is.

    No fenced code in source. No new dependency.
  </action>
  <verify>
    <automated>uv run pytest app/security/tests/test_output_firewall.py -x -q</automated>
  </verify>
  <done>
    Outbound payment text without flag is blocked + escalated; with flag passes; non-matching unaffected; mirror path also gated.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests — 6 success criteria covered end-to-end</name>
  <files>tests/integration/__init__.py, tests/integration/conftest.py, tests/integration/test_payment_e2e.py</files>
  <read_first>
    - app/main.py (FastAPI lifespan, app.state attributes)
    - app/webhooks/meta.py (TestClient invocation pattern from F2/F3 tests; HMAC signature helper)
    - app/webhooks/chatwoot.py (Plan 04-03 — HMAC signature helper)
    - app/webhooks/handoff.py (Plan 04-07 — bearer auth)
    - app/features/payment/graph.py + nodes.py + cartera.py
    - existing root conftest.py (test Postgres fixture, Redis fixture, app builder)
    - .planning/ROADMAP.md §Phase 4 Success criteria — the 6 deliverables this plan must verify
  </read_first>
  <behavior>
    test_happy_path_approve:
    - POST inbound media webhook from client `+573500` (in client allowlist) — image payload with media_id "MID-1".
    - Mocked `meta.download_media("MID-1")` returns valid JPEG bytes.
    - Assert: file written to tmp volume path; Case row exists with status `forwarded` or `awaiting_cartera`; cartera received forward call with caption matching D-08 regex and last attachment carrying 3 buttons; 0 calls passed comprobante bytes to any LLM mock.
    - POST inbound webhook from cartera `+573999` (cartera allowlist) with `interactive.button_reply.id="aprobar|<case>"`.
    - Assert: Case.status='approved'; client received text containing "Tu pago fue confirmado" or "Pago aprobado"; firewall permitted because `payment_approved=True` flag on the AIMessage.

    test_reject_path_escalates:
    - Same as above through forward; cartera taps `rechazar|<case>`.
    - Assert: Case.status='escalated'; Chatwoot conversation `assign_to_agent` (or equivalent) called; client received D-12 message.

    test_spoofed_cartera_number_silently_dropped:
    - POST inbound from `+999999` (not in any allowlist) with cartera-style button payload.
    - Assert: zero outbound calls; HTTP 200; log line with `meta.webhook.unknown_sender` (or whichever name F2/F3 used).

    test_chatwoot_agent_reply_relays_to_client:
    - Seed inverse index (`chatwoot:phone_by_conv:42 = +573500`).
    - POST `/webhooks/chatwoot` with valid HMAC, `message_type=outgoing`, `sender.type=user`, content="Hola, te ayudo".
    - Assert: `meta.send_text("+573500","Hola, te ayudo")` called once.

    test_handoff_no_answer_dispatches_template:
    - POST `/case/handoff/no_answer` with valid bearer + body.
    - Assert: `meta.send_template` called with template name from `Settings().payment.template_no_answer_name`, body params and quick-reply payloads matching D-20+D-21.
    - Repeat the POST: assert `meta.send_template.call_count == 1` (idempotent).

    test_output_firewall_blocks_hallucinated_confirmation:
    - Drive the Q&A graph to emit a malicious AIMessage with content "Tu pago fue confirmado" but no `payment_approved` flag (force via monkeypatched node).
    - Assert: client received escalation substitute, not the hallucination; Chatwoot private note contains `output_firewall.payment_blocked`.
  </behavior>
  <action>
    Create `tests/integration/__init__.py` (empty).

    Create `tests/integration/conftest.py`:
    - Provide a session-scoped fixture that boots the FastAPI app with a real Postgres (test DB) + real Redis (test DB index 1) + all integration-level mocks for outbound effects.
    - Mock `app.state.meta` with `AsyncMock(spec=MetaCloudClient)` so test assertions can inspect `send_text`/`send_media`/`send_template`/`upload_media`/`download_media`.
    - `meta.download_media.side_effect = lambda mid: (Path("tests/fixtures/sample.jpg").read_bytes(), "image/jpeg")`.
    - Mock `app.state.chatwoot` with `AsyncMock(spec=ChatwootClient)`.
    - Helper `meta_sign(body: bytes, secret: str) -> str` and `chatwoot_sign(body: bytes, secret: str) -> str` mirroring the production verifiers.
    - Helper `build_inbound_text(from_, text, msg_id)` and `build_inbound_interactive(from_, button_id, msg_id)` and `build_inbound_image(from_, media_id, mime, msg_id)` returning Meta webhook envelope dicts.
    - Set env vars: `CARTERA_PHONE_ALLOWLIST="+573999"`, `LAMBDA_PROYECT_INTERNAL_TOKEN="testtok"`, `CHATWOOT_WEBHOOK_SECRET="cwsecret"`, `WA_WEBHOOK_SECRET="metasecret"`, `PAYMENT_VOLUME_PATH=<tmp>`.
    - Create `tests/fixtures/sample.jpg` with a real 5-byte JPEG header (`\xff\xd8\xff\xe0\x00`) plus filler. Commit it to repo.

    Create `tests/integration/test_payment_e2e.py` implementing the 6 tests listed above.

    For each test, use `httpx.AsyncClient` against the app via `httpx.ASGITransport(app=app)` (FastAPI standard). Sign the Meta payload with the test secret. Use `TestClient` for the synchronous handoff endpoint.

    Mark the entire module with `@pytest.mark.integration` so it can be run/skipped via `-m integration`.

    NOTE on UAT: these tests are necessary but not sufficient. The 6 ROADMAP success criteria also include real-deployment verification (Railway smoke). The human-verify checkpoint below covers that step. The integration tests here exist so regressions in the graph or webhook plumbing fail in CI without waiting for a live smoke.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_payment_e2e.py -x -q -m integration</automated>
  </verify>
  <done>
    6 integration tests green; each test exercises the full webhook → graph → outbound path; mocks confirm comprobante bytes never reach an LLM call; firewall blocks hallucinated confirmations.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live smoke on Railway — 6 ROADMAP success criteria + worker redeploy</name>
  <what-built>
    Phase 4 complete: payment flow, cartera bidi, Chatwoot agent relay, lambda no-answer endpoint, output firewall, scheduler. All 8 plans merged.
  </what-built>
  <how-to-verify>
    Pre-smoke operator setup (do these in order):
    1. Confirm Railway volume mounted at /data/comprobantes on landa-agent-service (Plan 04-01 user_setup).
    2. Confirm env vars set on landa-agent-service: CARTERA_PHONE_ALLOWLIST, LAMBDA_PROYECT_INTERNAL_TOKEN, CHATWOOT_WEBHOOK_SECRET, PAYMENT_VOLUME_PATH=/data/comprobantes, META_TEMPLATE_NO_ANSWER_NAME=voice_no_answer_followup.
    3. Confirm Meta template `voice_no_answer_followup` is APPROVED in Meta Business Suite (D-22 prerequisite). If status is "in review" the no-answer endpoint will get a 4xx; OK to defer this single test.
    4. Configure Chatwoot outbound webhook URL → `https://<agent-domain>/webhooks/chatwoot` with the same HMAC secret as CHATWOOT_WEBHOOK_SECRET.
    5. Deploy: `git push` (web service auto-deploys) then `railway up --service agent-worker --ci --detach` (worker does NOT auto-deploy per F3 SMOKE finding).
    6. `railway run --service landa-agent-service -- alembic upgrade head` to apply migration 0002.

    Live smoke (cross off each criterion from ROADMAP §Phase 4 Success criteria):

    [ ] 1. From the test client WhatsApp number, send a JPEG image of any document to the bot. Observe: cartera number receives the forward with caption matching `📎 Comprobante [1/1] — Caso #...` and 3 buttons. Inspect LangSmith trace — verify the LLM did NOT receive the image bytes (only metadata).

    [ ] 2. From the cartera WhatsApp number tap "✅ Aprobar". Within 10 seconds the client receives a confirmation message. In Chatwoot, the conversation is marked resolved.

    [ ] 3. Start a fresh case (send another comprobante). From cartera tap "❌ Rechazar". Client receives the D-12 escalation message; in Chatwoot the conversation is open and assigned to an agent.

    [ ] 4. From a personal WhatsApp number that is NOT in CARTERA_PHONE_ALLOWLIST nor the client allowlist, send a message claiming to be cartera ("aprobar abc-123" as text). Verify: no outbound from the bot, no Chatwoot conversation created. Inspect Railway logs for `meta.webhook.unknown_sender` (or whatever the F2/F3 silent-discard log line is).

    [ ] 5. Open the Chatwoot agent UI, find the resolved/active case, send a text reply "Hola, te ayudo". Within 5 seconds the client receives the same text via WhatsApp from the bot's number. No infinite loop (verify Chatwoot agent does NOT see their own message echoed back).

    [ ] 6. From lambda-proyect (or a curl with the LAMBDA_PROYECT_INTERNAL_TOKEN), POST to `/case/handoff/no_answer` with a sample payload. Client receives the `voice_no_answer_followup` template with their name + póliza number and 2 quick-reply buttons. Repeat the POST: no second template.

    Edge cases worth probing if time permits:
    - Send a 6 MB image → bot replies "El archivo supera 5 MB..." and no row in `attachments`.
    - Send a `.exe` renamed `.jpg` → magic-byte rejection.
    - Send comprobante on Saturday → bot acks D-13, no forward yet. On Monday 08:00, scheduler fires and forwards.

    Any failure: capture exact request/response + log lines + Sentry breadcrumbs into `04-08-SMOKE.md` (mirror F3 03-06 SMOKE format). Iterate.
  </how-to-verify>
  <resume-signal>
    Reply "approved" once all 6 criteria pass live, or paste the failures into the chat to trigger gap-closure planning.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| All boundaries from Plans 04-01..04-07 | Now exercised together; this plan adds no new boundary |
| LLM output → client | Firewall now hard-gates "pago confirmado" text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Elevation of privilege | LLM hallucinates "pago confirmado" before cartera approval | mitigate (D-28) | `check_outbound` runs on every Meta + Chatwoot mirror outbound; blocks + escalates without the flag. Integration test covers. |
| T-04-08-02 | Tampering | `payment_approved` flag set incorrectly by future node author | mitigate | `node_confirming` is the only call site that sets the flag (Plan 04-04); code review + grep gate `grep -rn 'payment_approved.*True' app/` catches new offenders. |
| T-04-08-03 | Information disclosure | Blocked text leaked via Chatwoot mirror | mitigate | Mirror path also gated; ARQ kwarg carries the flag forward. Integration test asserts Chatwoot does not receive the blocked content (only the private "blocked" note). |
| T-04-08-04 | DoS | Adversarial LLM output repeatedly triggers firewall escalations | accept | Escalation is the correct action; Phase 5 rate limiting will cap. Documented. |
| T-04-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages. |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_payment_e2e.py app/security/tests/test_output_firewall.py -q -m integration` green.
- `uv run pytest -q` (full suite) green.
- `uv run ruff check app/ tests/ && uv run black --check app/ tests/` clean.
- `grep -rn "check_outbound" app/webhooks/ app/integrations/chatwoot.py app/worker.py | grep -v "^app/security/" | wc -l` &ge; 2 (wired in both dispatch and mirror paths).
- `grep -v '^#' app/features/payment/nodes.py | grep -c "payment_approved.*True"` equals 1 (only node_confirming sets it).
- Human checkpoint task above completed; SMOKE notes if any in `04-08-SMOKE.md`.
</verification>

<success_criteria>
- All 6 ROADMAP §Phase 4 success criteria pass live in Railway.
- Output firewall blocks hallucinated payment confirmations in both Meta send and Chatwoot mirror.
- Integration tests cover happy approve, reject escalate, spoofed cartera silent drop, Chatwoot agent reply relay, lambda handoff idempotent, and firewall block-and-escalate.
- agent-worker redeployed via `railway up --service agent-worker --ci --detach`.
</success_criteria>

<output>
Create `.planning/phases/04-flujo-de-validaci-n-de-pago-chatwoot-escalaci-n-bidirecciona/04-08-SUMMARY.md` (+ optional `04-08-SMOKE.md` if any live failures) when done.
</output>
