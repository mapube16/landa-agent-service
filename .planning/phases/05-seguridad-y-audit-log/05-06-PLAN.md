---
phase: 05-seguridad-y-audit-log
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-03"]
files_modified:
  - app/webhooks/meta.py
  - tests/test_webhooks_meta_f5.py
autonomous: true
requirements:
  - SEC-04
  - SEC-06

must_haves:
  truths:
    - "A flooding client number gets blocked by the rate limiter after the dedup check, receives T_RATE_LIMITED once per blocked message, and never reaches the firewall/graph"
    - "Cartera numbers are exempt from rate limiting (the cartera branch runs before the limiter — D-06 escalations can never be throttled)"
    - "Per-poliza limiting works: the poliza_id is peeked from the LangGraph checkpoint state when available and passed to check_rate_limit"
    - "Every successful outbound send emits an outbound_sent audit event; every firewall-blocked outbound emits outbound_blocked"
    - "Rate limiter or audit failures never break the webhook (fail-open both ways, 200 to Meta preserved)"
  artifacts:
    - path: "app/webhooks/meta.py"
      provides: "rate limit enforcement in _dispatch_message + audit capture in _send_outbound"
      contains: "check_rate_limit"
  key_links:
    - from: "app/webhooks/meta.py:_dispatch_message"
      to: "app/security/rate_limiter.py"
      via: "check_rate_limit after dedup + cartera branch, before client allowlist"
      pattern: "await check_rate_limit"
    - from: "app/webhooks/meta.py:_send_outbound"
      to: "app/security/audit_log.py"
      via: "emit_task after successful send"
      pattern: "audit_log.emit_task"
---

<objective>
Wire Phase 5 security into the webhook hot path (single owner of `app/webhooks/meta.py`): rate limit enforcement in `_dispatch_message` (SEC-06) and the `outbound_sent`/`outbound_blocked` audit capture in `_send_outbound` (SEC-04).

Dispatch order becomes (extends D-15 + Plan 04-05): HMAC -> parse -> dedup -> cartera-allowlist -> **rate_limit** -> client-allowlist -> firewall -> graph. Placing the limiter AFTER the cartera branch implements the cartera exemption recommended in RESEARCH Open Question 3 without a separate exemption list.

Purpose: ROADMAP success criterion "100 mensajes en 1 min desde mismo número -> bot bloquea con mensaje claro" enforced end-to-end, and the outbound leg of the SEC-04 capture table.

Output: 2 surgical edits to meta.py + `tests/test_webhooks_meta_f5.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-seguridad-y-audit-log/05-RESEARCH.md
@CLAUDE.md
@app/webhooks/meta.py
@app/security/rate_limiter.py
@app/security/audit_log.py
</context>

<interfaces>
<!-- Provided by Plans 05-01 and 05-03. Use directly. -->

From app/security/rate_limiter.py (05-03):
```python
T_RATE_LIMITED: str
class RateLimitResult(NamedTuple): allowed: bool; scope: str | None
async def check_rate_limit(redis: Any, *, phone: str, poliza_id: str | None = None) -> RateLimitResult
```

From app/security/audit_log.py (05-01):
```python
def emit_task(*, action, actor, conversation_id=None, poliza_id=None, payload, metadata=None) -> None
```

From app/webhooks/meta.py (existing):
```python
_dispatch_message(...)   # dedup at 4b (redis.set nx), cartera branch at 4c, client allowlist at 4d
_send_outbound(app_state, phone, msg, wamid)  # D-28 firewall gate at top, sends at ~209
_reset_if_closed(...)    # checkpointer.aget peek pattern to mirror for poliza_id
_hash_phone(phone)       # log-safe phone digest
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rate limit enforcement in _dispatch_message (with checkpoint poliza peek)</name>
  <files>app/webhooks/meta.py, tests/test_webhooks_meta_f5.py</files>
  <read_first>
    - app/webhooks/meta.py `_dispatch_message` (~541-661: exact insertion point between 4c cartera branch and 4d client allowlist) + `_reset_if_closed` (~351: checkpointer.aget pattern)
    - tests/test_webhooks_meta_gap2.py or nearest existing webhook test module (stub conventions for msg/redis/request)
    - .planning/phases/05-seguridad-y-audit-log/05-RESEARCH.md §Pattern 5 (enforcement point) + Open Question 3 (cartera exemption)
  </read_first>
  <behavior>
    tests/test_webhooks_meta_f5.py (stub redis + request/app_state like existing webhook tests; patch `meta_module.check_rate_limit`):
    - check_rate_limit returning `(False, "phone")` -> `meta.send_text` called once with body == T_RATE_LIMITED, and neither `is_echo_allowed` nor `_handle_text_message` is reached (patch/spy).
    - check_rate_limit returning `(True, None)` -> dispatch continues to client allowlist unchanged.
    - Cartera-allowlisted sender -> `check_rate_limit` NEVER called (exemption by ordering).
    - Duplicate message (dedup hit) -> `check_rate_limit` NEVER called (limiter must not count Meta redeliveries).
    - `check_rate_limit` raising -> message still dispatched (fail-open, log only).
    - Poliza peek helper returns the poliza_id from a stubbed checkpointer state and None when checkpointer missing/errors.
  </behavior>
  <action>
    In `app/webhooks/meta.py`:

    1. Imports: `from app.security.rate_limiter import T_RATE_LIMITED, check_rate_limit` and `from app.security import audit_log` (Task 2 uses it).
    2. New helper `async def _peek_poliza_id(app_state: Any, thread_id: str) -> str | None` — mirror `_reset_if_closed` exactly: checkpointer aget, `channel_values.get("poliza_id")`, try/except -> None. One indexed checkpoint read per message is acceptable (same cost class as the existing `_reset_if_closed` read later in the flow).
    3. In `_dispatch_message`, insert step 4c' between the cartera branch return and the client allowlist check:
       - `poliza_id = await _peek_poliza_id(request.app.state, normalized_from)`
       - `try: rl = await check_rate_limit(redis, phone=normalized_from, poliza_id=poliza_id)` / `except Exception: log.error("rate_limit.check_failed", ...); rl = allowed sentinel` (belt-and-suspenders on top of the limiter's own fail-open).
       - If `not rl.allowed`: `log.warning("webhook.rate_limited", scope=rl.scope, phone_hash=phone_hash, result="rate_limited")`; send `T_RATE_LIMITED` via `meta.send_text` inside try/except (send failure only logs); `return`. Comment the D-15 order update.
    4. Update the `_dispatch_message` docstring and the `receive` docstring order line to: `dedup -> cartera-allowlist -> rate_limit -> client-allowlist -> firewall -> graph dispatch`.

    Create `tests/test_webhooks_meta_f5.py` implementing the behavior bullets, copying the msg/redis/request stub style from the existing webhook tests.
  </action>
  <verify>
    <automated>uv run pytest tests/test_webhooks_meta_f5.py -q -m "not integration"</automated>
  </verify>
  <done>
    Flood blocked with clear message, cartera + duplicates exempt, poliza-level fed from checkpoint state, fail-open proven.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: outbound_sent / outbound_blocked audit capture in _send_outbound</name>
  <files>app/webhooks/meta.py, tests/test_webhooks_meta_f5.py</files>
  <read_first>
    - app/webhooks/meta.py `_send_outbound` (~130-222: firewall-block early return at ~150-177, successful send at ~181-211, mirror enqueue at ~212)
  </read_first>
  <behavior>
    - Successful text send -> recorder-patched `audit_log.emit_task` called once: action="outbound_sent", actor="bot", conversation_id=`_hash_phone(phone)`, payload with `wamid_in` (str), `kind` in {"text","buttons","list"}, `text_sha256` (64 hex chars).
    - Firewall-blocked send (payment text without approval) -> emit_task called with action="outbound_blocked", payload containing the fw_reason string; the substituted escalation text is what goes to Meta (existing behavior untouched).
    - `meta.send_text` raising -> NO outbound_sent event (only sends that succeeded are audited).
    - Existing output-firewall tests (04-08 suite) stay green.
  </behavior>
  <action>
    In `_send_outbound`:

    1. In the firewall-block branch (after the `log.error("output_firewall.payment_blocked", ...)` call): `audit_log.emit_task(action="outbound_blocked", actor="bot", conversation_id=_hash_phone(phone), payload={"wamid_in": wamid, "reason": str(fw_reason)})`.
    2. In the send try-block, after the successful send call path (restructure minimally: set a local `sent = True` after the send calls inside the try, emit after the try/except only when `sent`): `audit_log.emit_task(action="outbound_sent", actor="bot", conversation_id=_hash_phone(phone), payload={"wamid_in": wamid, "kind": kind, "text_sha256": hashlib.sha256(text_for_mirror.encode()).hexdigest()})` where `kind` is derived from the interactive branch taken. Do not reorder the mirror enqueue.

    conversation_id uses `_hash_phone(phone)` — never the raw phone (no-PII invariant; consistent with capture in 05-04 which uses the state's thread identifier — note the difference in a code comment).

    Extend `tests/test_webhooks_meta_f5.py` with the four behavior cases (stub app_state with fake meta client, patch `meta_module.audit_log.emit_task` recorder).
  </action>
  <verify>
    <automated>uv run pytest tests/test_webhooks_meta_f5.py -q -m "not integration" &amp;&amp; uv run pytest tests/ app/ -q -m "not integration"</automated>
  </verify>
  <done>
    Outbound leg of SEC-04 captured on success and on block; full suite green.
  </done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Denial of service | client flood exhausting LLM/SoftSeguros budget | mitigate | limiter before firewall/graph; blocked messages cost one Redis eval + one send |
| T-05-06-02 | Denial of service | attacker floods to throttle cartera decisions | mitigate | cartera branch precedes the limiter — cartera numbers structurally exempt |
| T-05-06-03 | Spoofing | rate-limit reply loop (bot replying to itself) | accept | T_RATE_LIMITED goes to the client number only; Meta statuses path unaffected |
| T-05-06-04 | Repudiation | outbound messages unaudited | mitigate | outbound_sent/outbound_blocked events chained in audit_log |
| T-05-06-05 | Information disclosure | raw phone in audit rows | mitigate | conversation_id = _hash_phone(phone); payload carries hashes only |
</threat_model>

<verification>
- `uv run pytest -q -m "not integration"` full suite green.
- `grep -n "check_rate_limit" app/webhooks/meta.py` shows the call between the cartera branch and `is_echo_allowed`.
- `grep -c "emit_task" app/webhooks/meta.py` >= 2.
- `uv run ruff check app/ && uv run black --check app/` clean.
</verification>

<success_criteria>
- ROADMAP criterion "100 mensajes en 1 min desde mismo número -> bloquea con mensaje claro" holds at the dispatch layer (limit 20/60s blocks well before 100).
- SEC-04 capture table complete once 05-04 + 05-05 + this plan land.
- Webhook returns 200 to Meta under every failure mode introduced here.
</success_criteria>

<output>
Create `.planning/phases/05-seguridad-y-audit-log/05-06-SUMMARY.md` when done.
</output>
