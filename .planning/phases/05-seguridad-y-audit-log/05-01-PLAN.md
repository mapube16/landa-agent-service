---
phase: 05-seguridad-y-audit-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0003_audit_log.py
  - alembic/env.py
  - app/security/audit_log.py
  - app/models/audit.py
  - app/security/tests/test_audit_log.py
  - app/config/settings.py
  - .env.example
autonomous: true
requirements:
  - SEC-01
  - SEC-02
user_setup:
  - service: railway-postgres
    why: "Migration 0003 must run against live Postgres before Wave 2 capture points emit rows"
    dashboard_config:
      - task: "Run `uv run alembic upgrade head` against Railway Postgres (same operator step as 0002)"
        location: "Railway -> landa-agent-service -> shell / deploy hook"

must_haves:
  truths:
    - "`alembic upgrade head --sql` emits CREATE TABLE audit_log plus the BEFORE DELETE OR UPDATE trigger DDL"
    - "DELETE or UPDATE on a live audit_log row raises a Postgres exception (append-only at engine level)"
    - "compute_entry_hash is deterministic: same prev_hash + entry dict always yields the same sha256 hex"
    - "verify_chain_rows detects a tampered payload_hash or broken prev_hash linkage and reports the first bad id"
    - "emit never raises — any DB failure is logged and swallowed (fail-open, Pitfall 3)"
  artifacts:
    - path: "alembic/versions/0003_audit_log.py"
      provides: "audit_log table + audit_log_immutable trigger + indexes"
      contains: "trg_audit_log_immutable"
    - path: "app/security/audit_log.py"
      provides: "AuditLog model + canonical + compute_entry_hash + emit + emit_task + verify_chain_rows + verify_chain"
      exports: ["AuditLog", "canonical", "compute_entry_hash", "emit", "emit_task", "verify_chain_rows", "verify_chain"]
    - path: "app/models/audit.py"
      provides: "AuditPayload flat-primitives validation model (no floats — Pitfall 4)"
      contains: "AuditPayload"
    - path: "app/config/settings.py"
      provides: "AuditSettings + RateLimitSettings registered on composite Settings"
      contains: "class RateLimitSettings"
  key_links:
    - from: "alembic/env.py"
      to: "app/security/audit_log"
      via: "import for autogenerate (uncomment the Phase 5 placeholder at line 30)"
      pattern: "from app.security import audit_log"
    - from: "app/security/audit_log.py"
      to: "app.config.db.Base"
      via: "declarative model registration"
      pattern: "Base"
---

<objective>
Foundations of Phase 5 (SEC-01 audit log inmutable, SEC-02 hash chain): migration `0003_audit_log.py` with the append-only trigger guard, the `app/security/audit_log.py` module (hash chain + fail-open `emit` + fire-and-forget `emit_task` + chain verifier functions), the `AuditPayload` model, and ALL Phase 5 settings (`AuditSettings`, `RateLimitSettings`) so no other Phase 5 plan touches `app/config/settings.py` (single-owner rule, mirrors 04-01).

Requirement ID map (no formal IDs in ROADMAP — Phase 5 deliverables numbered): SEC-01 audit log append-only, SEC-02 hash chain + tamper detection, SEC-03 object-storage sink, SEC-04 capture points, SEC-05 egress controls, SEC-06 rate limiting, SEC-07 adversarial suite, SEC-08 attachment validation, SEC-09 retrospectiva 13 capas.

Purpose: Waves 2-3 (capture points, worker crons, webhook wiring) consume `emit_task`, `verify_chain` and the new settings. Zero runtime behavior change for existing flows — nothing calls `emit` yet.

Output:
- `alembic/versions/0003_audit_log.py` — table + `audit_log_immutable()` trigger + 2 indexes (RESEARCH Pattern 1).
- `app/security/audit_log.py` — model + hash-chain functions + emit/emit_task/verify (RESEARCH Patterns 2-3).
- `app/models/audit.py` — `AuditPayload` (flat `str | int | bool | None` values only).
- `AuditSettings` (sink path/enabled) + `RateLimitSettings` (3 levels) + `.env.example` entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seguridad-y-audit-log/05-RESEARCH.md
@CLAUDE.md
@alembic/versions/0002_payment_tables.py
@alembic/env.py
@app/config/db.py
@app/config/settings.py
</context>

<interfaces>
<!-- Contracts downstream Phase 5 plans build against. Executor: create these EXACT signatures. -->

From app/security/audit_log.py (created here, consumed by 05-04/05-05/05-06):
```python
def canonical(entry: dict[str, Any]) -> bytes: ...
def compute_payload_hash(payload: dict[str, Any]) -> str: ...
def compute_entry_hash(prev_hash: str, entry: dict[str, Any]) -> str: ...

async def emit(
    session_factory: async_sessionmaker[AsyncSession],
    *,
    action: str,
    actor: str,
    conversation_id: str | None = None,
    poliza_id: str | None = None,
    payload: dict[str, Any],
    metadata: dict[str, Any] | None = None,
) -> None: ...  # fail-open: NEVER raises

def emit_task(
    *,
    action: str,
    actor: str,
    conversation_id: str | None = None,
    poliza_id: str | None = None,
    payload: dict[str, Any],
    metadata: dict[str, Any] | None = None,
) -> None: ...  # sync, fire-and-forget asyncio.create_task; NEVER raises

def verify_chain_rows(rows: Sequence[Any]) -> tuple[bool, int | None]: ...
async def verify_chain(session_factory: async_sessionmaker[AsyncSession]) -> tuple[bool, int | None]: ...
```

From app/config/settings.py (extended here):
```python
settings.audit.sink_path          # Path, default Path("/data/comprobantes/audit")
settings.audit.sink_enabled       # bool, default True
settings.rate_limit.enabled       # bool, default True
settings.rate_limit.phone_limit   # int, default 20   (per 60s window)
settings.rate_limit.poliza_limit  # int, default 10
settings.rate_limit.global_limit  # int, default 500
settings.rate_limit.window_s      # int, default 60
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0003 + AuditLog SQLAlchemy model + alembic registration</name>
  <files>alembic/versions/0003_audit_log.py, alembic/env.py, app/security/audit_log.py</files>
  <read_first>
    - alembic/versions/0002_payment_tables.py (revision chain style — revision "0002_payment_tables", down_revision "0001")
    - alembic/env.py line 30 (the `# Phase 5 (future): from app.security import audit_log  # noqa: F401` placeholder)
    - app/config/db.py (Base, naming convention)
    - .planning/phases/05-seguridad-y-audit-log/05-RESEARCH.md §Pattern 1 (trigger DDL) + §Code Examples
  </read_first>
  <behavior>
    - `from app.security import audit_log` registers table `audit_log` on `app.config.db.Base.metadata`.
    - `alembic upgrade head --sql` (offline) emits `CREATE TABLE audit_log`, `CREATE OR REPLACE FUNCTION audit_log_immutable`, `CREATE TRIGGER trg_audit_log_immutable`, and 2 CREATE INDEX statements.
    - Integration (marked `integration`, skipif no `POSTGRES_URL` env): after inserting one row, `DELETE FROM audit_log` raises `sqlalchemy.exc.DBAPIError` (Postgres RAISE EXCEPTION from the trigger); same for UPDATE.
  </behavior>
  <action>
    In `app/security/audit_log.py` start with the SQLAlchemy 2.0 declarative model (hash-chain functions come in Task 2 in the same file):

    - `class AuditLog(Base)` — table `audit_log`. Columns per RESEARCH schema: `id BigInteger PK autoincrement`, `created_at TIMESTAMPTZ NOT NULL server_default=func.now()`, `conversation_id Text NULL`, `poliza_id Text NULL`, `action Text NOT NULL`, `actor Text NOT NULL`, `payload_hash Text NOT NULL`, `prev_hash Text NOT NULL server_default=""`, `entry_hash Text NOT NULL`, and the metadata column. CRITICAL: `metadata` is a reserved attribute on declarative models — map it as `metadata_json: Mapped[str | None] = mapped_column("metadata", Text, nullable=True)` so the SQL column is named `metadata` but the Python attribute is `metadata_json`.
    - Module docstring: append-only invariant, hash chain design, fail-open rule (Pitfall 3), and the documented v1 concurrency stance (advisory lock serializes inserts — RESEARCH Pitfall 1).

    In `alembic/env.py`: replace the Phase 5 placeholder comment at line 30 with the live import `from app.security import audit_log  # noqa: F401`. Do not move the import block.

    Create `alembic/versions/0003_audit_log.py`:
    - `revision = "0003_audit_log"`, `down_revision = "0002_payment_tables"`.
    - `upgrade()`: `op.create_table("audit_log", ...)` mirroring the model exactly (server_default=sa.text("now()") for created_at, server_default="''" via `sa.text("''")` for prev_hash). Then `op.execute(sa.text(...))` twice for the function + trigger EXACTLY as RESEARCH §Code Examples (RAISE EXCEPTION on TG_OP with OLD.id). Then `op.create_index("ix_audit_log_created_at", ...)` and `op.create_index("ix_audit_log_conversation_id", ...)`.
    - `downgrade()`: drop trigger, drop function, drop indexes, drop table (that order).

    Add an integration test in `app/security/tests/test_audit_log.py` (file continues in Task 2): `@pytest.mark.integration` + `@pytest.mark.skipif(not os.getenv("POSTGRES_URL"), ...)` — connects with the app engine, inserts a minimal row via the model, then asserts `pytest.raises(DBAPIError)` on `session.execute(text("DELETE FROM audit_log WHERE id = :i"))` and on an UPDATE. This is the ROADMAP success criterion "Intento de DELETE en audit_log falla a nivel DB".

    Do NOT run `alembic upgrade head` against live DB here — operator step (user_setup).
  </action>
  <verify>
    <automated>uv run python -c "from app.security import audit_log; from app.config.db import Base; assert 'audit_log' in Base.metadata.tables; print('OK')" &amp;&amp; uv run alembic upgrade head --sql | grep -c "audit_log_immutable" | grep -qv "^0$" &amp;&amp; echo OK</automated>
  </verify>
  <done>
    Table registered on Base.metadata, offline DDL includes trigger + function, env.py imports audit_log, integration DELETE/UPDATE test exists (skips locally without POSTGRES_URL).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hash chain + emit (fail-open, advisory lock) + emit_task + verify_chain + AuditPayload</name>
  <files>app/security/audit_log.py, app/models/audit.py, app/security/tests/test_audit_log.py</files>
  <read_first>
    - .planning/phases/05-seguridad-y-audit-log/05-RESEARCH.md §Pattern 2 (canonical/hash) + §Pitfalls 1, 3, 4, 5
    - app/config/db.py (session_scope pattern)
    - app/webhooks/meta.py `_log_task_error` (done-callback pattern for create_task)
  </read_first>
  <behavior>
    - `canonical({"b": 1, "a": "x"}) == canonical({"a": "x", "b": 1})` (orjson OPT_SORT_KEYS — orjson==3.11.9 already pinned, no new dep).
    - `compute_entry_hash("", entry)` equals `hashlib.sha256(b"" + canonical(entry)).hexdigest()` — assert against an independently computed vector.
    - `AuditPayload` accepts flat `{"a": "s", "b": 1, "c": True, "d": None}`; rejects floats and nested dicts with ValidationError (Pitfall 4).
    - `verify_chain_rows` on 3 well-chained synthetic rows returns `(True, None)`; tampering row 2's `payload_hash` (without recomputing entry_hash) returns `(False, 2)`; breaking linkage (row 3 prev_hash != row 2 entry_hash) returns `(False, 3)`. This is the ROADMAP success criterion "tampering manual detectada por el verificador".
    - `emit` with a session_factory whose session raises on execute: returns None, no exception propagates, `structlog` error logged.
    - `emit_task` when `app.main.app.state` has no `session_factory`: returns silently (no crash, no task).
  </behavior>
  <action>
    In `app/security/audit_log.py` add (below the model):

    1. `canonical(entry: dict) -> bytes` — `orjson.dumps(entry, option=orjson.OPT_SORT_KEYS | orjson.OPT_NON_STR_KEYS)`.
    2. `compute_payload_hash(payload) -> str` — `hashlib.sha256(canonical(payload)).hexdigest()`.
    3. `compute_entry_hash(prev_hash: str, entry: dict) -> str` — `sha256(prev_hash.encode() + canonical(entry)).hexdigest()`.
    4. `async def emit(session_factory, *, action, actor, conversation_id=None, poliza_id=None, payload, metadata=None) -> None`. ENTIRE body inside `try/except Exception` that logs `log.error("audit_log.emit_failed", action=action, error_type=...)` — fail-open, never raises (Pitfall 3). Inside:
       - Validate payload with `AuditPayload.model_validate(payload)` (flat primitives only).
       - `payload_hash = compute_payload_hash(payload)`.
       - `async with session_scope(session_factory) as session:`
         - `await session.execute(text("SELECT pg_advisory_xact_lock(hashtext('audit_log_chain'))"))` — serializes chain inserts (Pitfall 1; documented v1 stance).
         - `next_id = (await session.execute(text("SELECT nextval(pg_get_serial_sequence('audit_log', 'id'))"))).scalar_one()`.
         - `prev_hash = (await session.execute(text("SELECT entry_hash FROM audit_log ORDER BY id DESC LIMIT 1"))).scalar() or ""`.
         - `created_at = datetime.now(timezone.utc)` — Python-side so it can participate in the hash.
         - `entry = {"id": next_id, "created_at": created_at.isoformat(), "conversation_id": conversation_id, "poliza_id": poliza_id, "action": action, "actor": actor, "payload_hash": payload_hash}` — metadata deliberately EXCLUDED from the hash (RESEARCH Pattern 2).
         - `entry_hash = compute_entry_hash(prev_hash, entry)`.
         - Insert an `AuditLog` row with explicit `id=next_id`, explicit `created_at`, `metadata_json=orjson.dumps(metadata).decode() if metadata else None`.
    5. `def emit_task(*, action, actor, conversation_id=None, poliza_id=None, payload, metadata=None) -> None` — sync fire-and-forget for hot paths (RESEARCH Pattern 3 async concern). Body in try/except: local import `from app.main import app as _app`; `sf = getattr(_app.state, "session_factory", None)`; if None -> `log.debug("audit_log.no_session_factory", action=action)` and return; else `task = asyncio.create_task(emit(sf, ...))` + done-callback that logs `task.exception()` type only (mirror `_log_task_error` in meta.py). Any exception -> `log.error("audit_log.emit_task_failed", ...)`, never raise.
    6. `def verify_chain_rows(rows) -> tuple[bool, int | None]` — pure. Iterate rows ordered by id; for each: (a) `row.prev_hash` must equal previous row's `entry_hash` (first row: `""`), (b) recompute entry dict from row columns (`created_at.isoformat()` — normalize tz-aware) and assert `compute_entry_hash(row.prev_hash, entry) == row.entry_hash`. First failure returns `(False, row.id)`; else `(True, None)`.
    7. `async def verify_chain(session_factory) -> tuple[bool, int | None]` — fetch all rows `ORDER BY id ASC` via the model, delegate to `verify_chain_rows`. On DB error: log + return `(True, None)` (fail-open — verification job alerting is 05-05's concern).
    8. `__all__` with the 8 public names (model + 7 functions).

    Create `app/models/audit.py`: `AuditPayload(RootModel[dict[str, str | int | bool | None]])` (Pydantic v2 RootModel) — flat dict, values restricted so floats and nested structures fail validation. Docstring: cast money to int cents or str before auditing (Pitfall 4). `__all__ = ["AuditPayload"]`.

    In `app/security/tests/test_audit_log.py` add the unit tests listed in behavior. For `verify_chain_rows` build synthetic rows with `types.SimpleNamespace(id=..., created_at=..., conversation_id=..., poliza_id=..., action=..., actor=..., payload_hash=..., prev_hash=..., entry_hash=...)` chained via the real `compute_entry_hash`. For `emit` fail-open use a session_factory stub whose `__aenter__` raises. For `emit_task` monkeypatch `app.main.app.state` (delattr session_factory if set).

    Per CLAUDE.md: no planning docstrings, structlog, mypy --strict clean.
  </action>
  <verify>
    <automated>uv run pytest app/security/tests/test_audit_log.py -q -m "not integration"</automated>
  </verify>
  <done>
    All unit tests pass; emit/emit_task provably fail-open; verify_chain_rows detects both tamper modes; AuditPayload rejects floats.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AuditSettings + RateLimitSettings + .env.example</name>
  <files>app/config/settings.py, .env.example</files>
  <read_first>
    - app/config/settings.py (PaymentSettings as the style reference: env_prefix, SettingsConfigDict, composite registration)
    - .planning/phases/05-seguridad-y-audit-log/05-RESEARCH.md §Pattern 5 (the 3-level limits table)
  </read_first>
  <behavior>
    - `Settings().audit.sink_path == Path("/data/comprobantes/audit")` by default; `AUDIT_SINK_PATH` env overrides; `Settings().audit.sink_enabled is True` by default.
    - `Settings().rate_limit.phone_limit == 20`, `.poliza_limit == 10`, `.global_limit == 500`, `.window_s == 60`, `.enabled is True` by default; each overridable via `RATE_LIMIT_*` env vars.
  </behavior>
  <action>
    In `app/config/settings.py` (additive only):

    1. `class AuditSettings(BaseSettings)` with `env_prefix="AUDIT_"` (copy the exact `SettingsConfigDict` kwargs the sibling classes use). Fields: `sink_path: Path = Path("/data/comprobantes/audit")` (inside the already-mounted Railway volume from 04-01 — no new mount needed), `sink_enabled: bool = True`.
    2. `class RateLimitSettings(BaseSettings)` with `env_prefix="RATE_LIMIT_"`. Fields: `enabled: bool = True`, `phone_limit: int = 20`, `poliza_limit: int = 10`, `global_limit: int = 500`, `window_s: int = 60` (limits per RESEARCH Pattern 5 table).
    3. Register on composite `Settings`: `audit: AuditSettings = Field(default_factory=AuditSettings)`, `rate_limit: RateLimitSettings = Field(default_factory=RateLimitSettings)`.

    Append to `.env.example`:

    # Phase 5 — Audit log + rate limiting (SEC-01..SEC-06)
    AUDIT_SINK_PATH=/data/comprobantes/audit
    AUDIT_SINK_ENABLED=true
    RATE_LIMIT_ENABLED=true
    RATE_LIMIT_PHONE_LIMIT=20
    RATE_LIMIT_POLIZA_LIMIT=10
    RATE_LIMIT_GLOBAL_LIMIT=500
    RATE_LIMIT_WINDOW_S=60
  </action>
  <verify>
    <automated>uv run python -c "from app.config.settings import Settings; s=Settings(); assert str(s.audit.sink_path)=='/data/comprobantes/audit'; assert s.rate_limit.phone_limit==20 and s.rate_limit.poliza_limit==10 and s.rate_limit.global_limit==500 and s.rate_limit.enabled; print('OK')"</automated>
  </verify>
  <done>
    Both settings classes parse with defaults, registered on composite Settings, all 7 env vars documented in .env.example.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> Postgres | New DDL + trigger; audit inserts under advisory lock |
| payload -> audit_log | Payloads may contain PII — only sha256 hash persisted, never the raw payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | audit_log rows | mitigate | BEFORE DELETE OR UPDATE trigger raises at engine level regardless of connecting role (RESEARCH Anti-pattern: REVOKE useless with single owner role) |
| T-05-01-02 | Repudiation | chain fork on concurrent emit | mitigate | pg_advisory_xact_lock('audit_log_chain') serializes inserts inside the emit transaction |
| T-05-01-03 | Information disclosure | PII in audit payloads | mitigate | Only payload_hash stored; AuditPayload restricts to flat primitives; CLAUDE.md no-PII invariant |
| T-05-01-04 | Denial of service | audit failure crashes webhook | mitigate | emit + emit_task fully fail-open; failures log-only (Pitfall 3) |
| T-05-01-SC | Tampering | supply chain | accept | No new dependency — orjson already pinned in pyproject.toml |
</threat_model>

<verification>
- `uv run pytest app/security/tests/test_audit_log.py -q -m "not integration"` passes.
- `uv run alembic upgrade head --sql` emits table + trigger + function + 2 indexes.
- `uv run ruff check app/ && uv run black --check app/` clean.
- `uv run mypy --strict app/security/audit_log.py app/models/audit.py` clean.
</verification>

<success_criteria>
- Migration 0003 registered in the alembic chain after 0002_payment_tables.
- DELETE/UPDATE integration test encodes the ROADMAP criterion "DELETE con role de aplicación falla a nivel DB".
- verify_chain_rows unit tests encode the ROADMAP criterion "tampering manual detectada".
- emit_task ready for Wave 2 consumers; settings ready for 05-03/05-05.
</success_criteria>

<output>
Create `.planning/phases/05-seguridad-y-audit-log/05-01-SUMMARY.md` when done.
</output>
