---
phase: 05-seguridad-y-audit-log
plan: 07
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06"]
files_modified:
  - app/security/input_sanitizer.py
  - app/features/payment/attachment.py
  - app/features/payment/tests/test_attachment_hardening.py
  - tests/security/test_egress_allowlist.py
  - .planning/adr/005-malware-scan.md
  - .planning/adr/006-egress-controls.md
  - .planning/phases/05-seguridad-y-audit-log/05-13-LAYERS-AUDIT.md
autonomous: true
requirements:
  - SEC-05
  - SEC-08
  - SEC-09

must_haves:
  truths:
    - "A comprobante with .exe magic bytes (MZ) or any non-allowlisted type is rejected before forward to cartera; oversize payloads (> cap) are rejected — both proven by tests"
    - "Egress is constrained by a CI-enforced hostname allowlist test: no hardcoded external URL outside SoftSeguros/Meta/Chatwoot/OpenRouter/LangSmith/Sentry appears in app/"
    - "ADR-005 documents the v1 malware-scan deferral; ADR-006 documents Railway egress limitations + the app-level compensating control"
    - "05-13-LAYERS-AUDIT.md marks each of the 13 PROJECT.md layers as implemented-and-tested with file + test evidence, zero remaining gaps"
    - "app/security/input_sanitizer.py exists, closing the CLAUDE.md structure-diagram gap"
  artifacts:
    - path: "app/security/input_sanitizer.py"
      provides: "thin re-export of prompt_firewall.sanitize (structure contract, RESEARCH OQ-4)"
      contains: "sanitize_input"
    - path: "app/features/payment/attachment.py"
      provides: "BLOCKED_EXTENSIONS belt-and-suspenders list alongside magic-byte validation"
      contains: "BLOCKED_EXTENSIONS"
    - path: ".planning/adr/005-malware-scan.md"
      provides: "malware scan deferral decision"
      contains: "ClamAV"
    - path: ".planning/adr/006-egress-controls.md"
      provides: "egress controls decision (Railway limitation + compensating control)"
      contains: "Railway"
    - path: ".planning/phases/05-seguridad-y-audit-log/05-13-LAYERS-AUDIT.md"
      provides: "retrospective 13-layer checklist with evidence per layer"
      contains: "13"
  key_links:
    - from: "tests/security/test_egress_allowlist.py"
      to: "app/"
      via: "static scan for hardcoded http(s) hosts outside the allowlist"
      pattern: "ALLOWED_EGRESS_HOSTS"
---

<objective>
Close Phase 5 (SEC-05 egress, SEC-08 attachment validation, SEC-09 retrospectiva): the retrospective audit of F1-F4 against the 13 declared layers with small gap fixes, the two ADRs for infra/deferred deliverables (Railway hobby has no VPC egress rules; ClamAV deferred per RESEARCH), and attachment hardening.

Per phase constraints: infra-only deliverables (Railway egress rules) and deferred-by-decision items (malware scan) are documented as ADRs with app-level compensating controls — NOT fake code tasks. The 13-layer audit is the deliverable "Auditoría retrospectiva del código de F1/F2/F3: marcar cada capa como implementada-y-testeada o gap — cerrar gaps".

Output:
- Gap fixes: `input_sanitizer.py` (RESEARCH OQ-4), `BLOCKED_EXTENSIONS` in attachment.py, tool-output-sanitization evidence check (layer 4 was PARTIAL in RESEARCH).
- `tests/security/test_egress_allowlist.py` — CI-enforced compensating control for layer 10.
- ADR-005 (malware scan), ADR-006 (egress controls).
- `05-13-LAYERS-AUDIT.md` — the final checklist mapping every layer to code + test evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-seguridad-y-audit-log/05-RESEARCH.md
@CLAUDE.md
@app/features/payment/attachment.py
@app/security/prompt_firewall.py
@app/integrations/softseguros.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gap fixes — input_sanitizer wrapper + attachment BLOCKED_EXTENSIONS + egress allowlist test</name>
  <files>app/security/input_sanitizer.py, app/features/payment/attachment.py, app/features/payment/tests/test_attachment_hardening.py, tests/security/test_egress_allowlist.py</files>
  <read_first>
    - app/features/payment/attachment.py (validate_magic_bytes, ATTACHMENT_MAX_BYTES=5MB per D-25, ALLOWED_MIME_TYPES)
    - .planning/phases/05-seguridad-y-audit-log/05-RESEARCH.md §Pattern 7 + §Pitfall 6 + Open Question 4
    - The call site of validate_magic_bytes (grep app/ for it) to wire the extension check at the same choke point
  </read_first>
  <behavior>
    test_attachment_hardening.py:
    - Bytes starting with `MZ` (PE/.exe magic) declared as image/jpeg -> rejected by magic-byte validation.
    - Filename "comprobante.exe" (any mime) -> rejected by the new extension check; same for .js/.sh/.bat/.dll/.msi/.scr.
    - Payload of ATTACHMENT_MAX_BYTES + 1 -> rejected (encodes ROADMAP criterion ">10MB rechazado" — the 5 MB cap per D-25 is strictly tighter; keep 5 MB and document, per RESEARCH recommendation).
    - Valid small JPEG magic bytes + benign filename -> accepted (regression guard).
    test_egress_allowlist.py:
    - Static scan of `app/**/*.py` for `https?://` literals: every hostname found must be in ALLOWED_EGRESS_HOSTS = {softseguros, Meta graph.facebook.com, chatwoot host pattern/env-driven, openrouter.ai, langsmith/smith.langchain.com, sentry.io} or be localhost/example placeholders. Unknown host -> test fails with the offending file:line.
  </behavior>
  <action>
    1. Create `app/security/input_sanitizer.py` per RESEARCH OQ-4: docstring explaining it fulfills the CLAUDE.md structure contract; `from app.security.prompt_firewall import sanitize as sanitize_input` and `__all__ = ["sanitize_input"]`. No duplicated logic.

    2. In `app/features/payment/attachment.py` add `BLOCKED_EXTENSIONS: Final[frozenset[str]] = frozenset({".exe", ".js", ".sh", ".bat", ".cmd", ".dll", ".msi", ".scr", ".ps1", ".jar"})` and a function `def has_blocked_extension(filename: str | None) -> bool` (case-insensitive suffix check, None -> False). Wire it into the existing validation choke point (wherever validate_magic_bytes is called — read first; if the caller doesn't currently receive a filename, add the check inside attachment.py's validation function with an optional `filename` parameter defaulted to None so existing callers keep working, and pass the filename at the one call site that has it). Belt-and-suspenders per RESEARCH Pattern 7 — magic bytes remain the primary control. Keep ATTACHMENT_MAX_BYTES at 5 MB; update its comment to note it intentionally under-cuts the ROADMAP 10 MB ceiling (conservative, ADR-005).

    3. Create `app/features/payment/tests/test_attachment_hardening.py` implementing the behavior bullets.

    4. Create `tests/security/test_egress_allowlist.py`: module-level `ALLOWED_EGRESS_HOSTS` frozenset; walk `app/` with pathlib, regex `https?://([A-Za-z0-9.-]+)` over each file, assert every captured host is allowlisted or localhost/127.0.0.1/example.com/app.softseguros.com etc. — build the initial allowlist by RUNNING the scan and classifying every real hit against CLAUDE.md layer 10 (SoftSeguros + Meta + Chatwoot + OpenRouter + LangSmith; Sentry DSN is env-driven). This is the app-level compensating control for the Railway egress gap (Pitfall 6) and MUST fail on any newly hardcoded host.
  </action>
  <verify>
    <automated>uv run pytest app/features/payment/tests/test_attachment_hardening.py tests/security/test_egress_allowlist.py -q -m "not integration"</automated>
  </verify>
  <done>
    .exe/oversize comprobantes provably rejected pre-forward; egress allowlist enforced in CI; input_sanitizer.py structure gap closed.
  </done>
</task>

<task type="auto">
  <name>Task 2: ADR-005 (malware scan deferral) + ADR-006 (egress controls)</name>
  <files>.planning/adr/005-malware-scan.md, .planning/adr/006-egress-controls.md</files>
  <read_first>
    - .planning/phases/05-seguridad-y-audit-log/05-RESEARCH.md §Standard Stack (malware options) + §Pattern 7 + §Pitfall 6
    - CLAUDE.md READ-ONLY invariant section (existing decision-documentation tone; note: .planning/adr/ does not exist yet — create the directory)
  </read_first>
  <action>
    Create `.planning/adr/005-malware-scan.md` (standard ADR format: Status/Context/Decision/Consequences, ADR numbering continues any existing convention — dir is new, so establish it):
    - Status: Accepted. Decision: v1 attachment defense = magic-byte validation + MIME allowlist (jpeg/png/webp/pdf) + 5 MB cap + BLOCKED_EXTENSIONS; ClamAV deferred. Context: ClamAV needs a sidecar/second Railway service (RESEARCH); comprobantes are never opened by LANDA nor passed to an LLM (layer 13) — they are relayed to cartera's WhatsApp where Meta re-scans media. Revisit triggers: >100 attachments/day or any threat incident. Consequences: residual risk documented — a valid-JPEG-with-payload reaches cartera's phone; accepted for v1.

    Create `.planning/adr/006-egress-controls.md`:
    - Status: Accepted. Context: PROJECT.md layer 10 asks for infra-level egress restriction; Railway hobby plan exposes NO VPC/firewall egress rules (RESEARCH Pitfall 6 — operator should re-verify against current Railway pricing when upgrading plans). Decision: compensating app-level controls: (a) all outbound HTTP goes through the four integration clients whose base URLs come from settings, (b) CI static guard `tests/security/test_egress_allowlist.py` fails on any hardcoded host outside SoftSeguros/Meta/Chatwoot/OpenRouter/LangSmith/Sentry, (c) READ-ONLY CI guard already constrains SoftSeguros verbs. Consequences: network-level exfiltration by a compromised dependency is NOT blocked — accepted for v1, revisit on plan upgrade or Phase 7 hardening.

    No emojis. Spanish or English matching .planning/ house style (read a neighboring planning doc and match).
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; a=Path('.planning/adr/005-malware-scan.md').read_text(encoding='utf-8'); b=Path('.planning/adr/006-egress-controls.md').read_text(encoding='utf-8'); assert 'ClamAV' in a and 'Railway' in b and 'Decision' in a and 'Decision' in b; print('OK')"</automated>
  </verify>
  <done>
    Both ADRs exist with Status/Context/Decision/Consequences and explicit revisit triggers.
  </done>
</task>

<task type="auto">
  <name>Task 3: Retrospective 13-layer audit checklist (F1-F4 + F5 closure)</name>
  <files>.planning/phases/05-seguridad-y-audit-log/05-13-LAYERS-AUDIT.md</files>
  <read_first>
    - .planning/PROJECT.md §"Seguridad y mitigación de prompt injection" (the authoritative 13-layer list)
    - .planning/phases/05-seguridad-y-audit-log/05-RESEARCH.md §Retrospective table (starting point — re-verify each row against CURRENT code, it predates Waves 1-3)
  </read_first>
  <action>
    Create `05-13-LAYERS-AUDIT.md`: one row per layer with columns Layer / Status / Code evidence (file:symbol) / Test evidence (test file::test or catalog id) / Notes. Procedure:

    1. For each of the 13 layers, VERIFY the claim against the current tree (grep the cited file + run the cited test selector) — do not copy the RESEARCH table blindly; layers 10-12 changed in this phase (egress test, audit_log, rate_limiter) and layer 4 was marked PARTIAL.
    2. Layer 4 (tool output sanitization): confirm SoftSeguros responses are field-allowlisted/escaped before reaching the LLM. If a concrete small gap exists (e.g. no test asserting `"system:"` patterns in tool output are neutralized), CLOSE it inline: add the missing test to `tests/security/test_adversarial.py` (extend the catalog with a tool-output-injection case using the 05-02 mock harness) and cite it. If the gap is structural (needs a new sanitize layer in softseguros.py), do NOT bolt it on here — record it as an explicit deferred item with file/line and raise it in the SUMMARY for the operator.
    3. Every layer must reference at least one automated test — this encodes the ROADMAP criterion "cada item del bloque Seguridad en PROJECT.md tiene un test que lo verifica". Any layer without one is a FAIL of this task until a test is written or a deferral is justified (only acceptable for layer 10 infra half — cite ADR-006 + the egress allowlist test as the compensating evidence).
    4. Footer: verdict line ("13/13 implementadas; N con controles compensatorios documentados en ADR"), date, and the commands run as evidence.
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; t=Path('.planning/phases/05-seguridad-y-audit-log/05-13-LAYERS-AUDIT.md').read_text(encoding='utf-8'); import re; rows=[l for l in t.splitlines() if l.strip().startswith('|') and not set(l) &lt;= set('|- ')]; assert len(rows) &gt;= 14, len(rows); assert 'ADR-006' in t; print('OK')" &amp;&amp; uv run pytest -q -m "not integration"</automated>
  </verify>
  <done>
    All 13 layers audited against current code with test evidence each; small gaps closed inline; structural gaps (if any) explicitly deferred with justification; full suite green.
  </done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | malicious attachment relayed to cartera | mitigate | magic bytes + MIME + 5MB + BLOCKED_EXTENSIONS; residual risk in ADR-005 |
| T-05-07-02 | Information disclosure | exfiltration to unauthorized host | mitigate (partial) | app-level egress allowlist CI guard; network-level gap accepted in ADR-006 |
| T-05-07-03 | Elevation of privilege | injection via tool output (layer 4 PARTIAL) | mitigate | retrospective step 2 adds the tool-output-injection catalog test or documents the structural deferral |
| T-05-07-04 | Repudiation | untestable security claims in PROJECT.md | mitigate | audit doc requires test evidence per layer |
</threat_model>

<verification>
- `uv run pytest -q -m "not integration"` full suite green.
- `.planning/adr/` contains ADR-005 + ADR-006.
- 05-13-LAYERS-AUDIT.md has 13 evidence rows and a verdict footer.
- `uv run ruff check app/ tests/ && uv run black --check app/ tests/` clean.
</verification>

<success_criteria>
- ROADMAP criteria "comprobante .exe o >10MB rechazado antes de reenviar" and "cada item del bloque Seguridad tiene un test" both encoded in automated tests + the audit doc.
- Infra-limited and deferred deliverables (egress rules, malware scan) resolved as ADRs with compensating controls, not fake code.
- Phase 5 goal fully closed: 13/13 layers implemented-and-tested or ADR-compensated.
</success_criteria>

<output>
Create `.planning/phases/05-seguridad-y-audit-log/05-07-SUMMARY.md` when done.
</output>
