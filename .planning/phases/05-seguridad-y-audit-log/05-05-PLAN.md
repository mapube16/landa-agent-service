---
phase: 05-seguridad-y-audit-log
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/worker.py
  - app/security/audit_sink.py
  - tests/test_worker_audit.py
autonomous: true
requirements:
  - SEC-02
  - SEC-03
  - SEC-04
user_setup:
  - service: railway-worker
    why: "agent-worker does not auto-deploy and needs volume access for the NDJSON sink"
    dashboard_config:
      - task: "After merge run `railway up --service agent-worker --ci --detach` (worker.py OPERATOR NOTE)"
        location: "Railway CLI"
      - task: "Verify agent-worker can reach the /data/comprobantes volume (same mount as landa-agent-service); if the worker service has no volume, attach one and set AUDIT_SINK_PATH accordingly — sink is fail-open meanwhile"
        location: "Railway dashboard -> agent-worker -> Volumes"

must_haves:
  truths:
    - "process_attachment emits an attachment_received audit event before invoking the graph"
    - "A daily ARQ cron verifies the full hash chain and logs audit_log.chain_tampered (error level -> Sentry) on mismatch"
    - "A daily ARQ cron appends new audit rows as NDJSON lines to AUDIT_SINK_PATH/YYYY-MM-DD.ndjson (append-only secondary sink)"
    - "Both crons are fail-open: missing table, missing volume or DB errors log and return — the worker never crashes"
  artifacts:
    - path: "app/security/audit_sink.py"
      provides: "export_audit_ndjson(session_factory, sink_dir) incremental NDJSON exporter"
      exports: ["export_audit_ndjson"]
    - path: "app/worker.py"
      provides: "verify_audit_chain + sink_audit_log ARQ jobs registered as cron_jobs"
      contains: "verify_audit_chain"
  key_links:
    - from: "app/worker.py"
      to: "app/security/audit_log.py"
      via: "verify_chain(session_factory) inside the cron job"
      pattern: "verify_chain"
    - from: "app/worker.py"
      to: "app/security/audit_sink.py"
      via: "export_audit_ndjson inside the sink cron"
      pattern: "export_audit_ndjson"
---

<objective>
Worker-side audit machinery (SEC-02 verifier, SEC-03 secondary sink, SEC-04 attachment capture): the `attachment_received` capture point in `process_attachment`, the daily hash-chain verification cron, and the NDJSON object-storage sink cron writing to the Railway volume (RESEARCH Pattern 4, volume approach — volume exists since 04-01, so no S3 needed; sink path defaults inside the existing mount).

Purpose: ROADMAP deliverables "Sink secundario: replicación asíncrona a object storage con append-only" and "detección de tampering vía verificación de la cadena", plus the worker slot reserved at worker.py line 18 ("F5: audit log fan-out").

Output: 2 new ARQ jobs + 2 cron registrations + `app/security/audit_sink.py` + capture hook in `process_attachment` + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-seguridad-y-audit-log/05-RESEARCH.md
@CLAUDE.md
@app/worker.py
@app/security/audit_log.py
@app/config/settings.py
@app/features/payment/scheduler.py
</context>

<interfaces>
<!-- Provided by Plan 05-01. -->

From app/security/audit_log.py:
```python
async def verify_chain(session_factory) -> tuple[bool, int | None]   # (ok, first_bad_id)
def emit_task(*, action, actor, conversation_id=None, poliza_id=None, payload, metadata=None) -> None
class AuditLog(Base)  # columns: id, created_at, conversation_id, poliza_id, action, actor, payload_hash, prev_hash, entry_hash, metadata_json ("metadata")
```

From app/config/settings.py:
```python
settings.audit.sink_path      # Path, default /data/comprobantes/audit
settings.audit.sink_enabled   # bool
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: attachment_received capture + verify_audit_chain cron</name>
  <files>app/worker.py, tests/test_worker_audit.py</files>
  <read_first>
    - app/worker.py (process_attachment at ~103, WorkerSettings.functions/cron_jobs, on_startup session_factory wiring — Pitfall 5)
    - app/features/payment/scheduler.py (existing cron job function style: ctx first arg, local imports, structlog)
    - .planning/phases/05-seguridad-y-audit-log/05-RESEARCH.md §Pattern 2 (chain verification job)
  </read_first>
  <behavior>
    tests/test_worker_audit.py:
    - `process_attachment` (fast path with a stub graph in ctx) calls the recorder-patched `audit_log.emit_task` once with action="attachment_received", actor="worker", conversation_id=phone, payload containing `media_id`, `mime_type`, `wamid` (all str).
    - `verify_audit_chain(ctx)` with `verify_chain` patched to return `(True, None)` logs info and returns without error.
    - `verify_audit_chain(ctx)` with `verify_chain` patched to return `(False, 42)` logs `log.error("audit_log.chain_tampered", first_bad_id=42)` (error level reaches Sentry via the structlog/sentry pipeline).
    - `verify_audit_chain` with a session_factory resolution failure (no app.state.session_factory) logs and returns — no exception.
  </behavior>
  <action>
    In `app/worker.py`:

    1. In `process_attachment`, immediately before the graph work (both slow and fast paths share the top of the function — place it once at the start after imports): `from app.security import audit_log` (local import per worker convention) then `audit_log.emit_task(action="attachment_received", actor="worker", conversation_id=phone, payload={"media_id": media_id, "mime_type": mime_type, "wamid": wamid})`.
    2. New job `async def verify_audit_chain(ctx: dict[str, Any]) -> None`: local imports; resolve session_factory the same way payment nodes do (`from app.main import app as _app; sf = getattr(_app.state, "session_factory", None)` — Pitfall 5 pattern already established in on_startup); if None -> log warning + return. `ok, bad_id = await verify_chain(sf)`. If ok: `log.info("audit_log.chain_verified")`. Else: `log.error("audit_log.chain_tampered", first_bad_id=bad_id)` — ROADMAP Phase 7 alert "audit log roto" keys off this event. Wrap the whole body in try/except -> log.error, never raise.
    3. Register `verify_audit_chain` in `WorkerSettings.functions` and add `cron(verify_audit_chain, hour={3}, minute={0})` to `cron_jobs` (daily 03:00 UTC, off-peak, after the 02:00 cleanup). Update the F5 milestone comments in the module docstring and class docstring from "future" phrasing to done.

    Create `tests/test_worker_audit.py` implementing the behavior bullets (monkeypatch recorder for emit_task; stub graph object with `aupdate_state`/`ainvoke` async no-ops for the fast path).
  </action>
  <verify>
    <automated>uv run pytest tests/test_worker_audit.py -q -m "not integration"</automated>
  </verify>
  <done>
    Attachment capture emits before graph invoke; tamper detection cron registered daily and fail-open; chain_tampered error event proven by test.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NDJSON secondary sink (Railway volume) + cron</name>
  <files>app/security/audit_sink.py, app/worker.py, tests/test_worker_audit.py</files>
  <read_first>
    - .planning/phases/05-seguridad-y-audit-log/05-RESEARCH.md §Pattern 4 (volume approach, append-only NDJSON)
    - app/security/audit_log.py (AuditLog columns for serialization)
  </read_first>
  <behavior>
    - `export_audit_ndjson(session_factory, sink_dir)` writes one JSON line per audit row to `{sink_dir}/{row.created_at:%Y-%m-%d}.ndjson`, appending (mode "a"), creating sink_dir if missing; returns the number of rows exported.
    - Incremental: only rows with `id >` the max id already exported (tracked in `{sink_dir}/.cursor` containing the last exported id as text; missing cursor -> export all). Second run with no new rows returns 0 and appends nothing (append-only, idempotent).
    - Each NDJSON line contains id, created_at ISO, conversation_id, poliza_id, action, actor, payload_hash, prev_hash, entry_hash (orjson, sorted keys) — enough to re-verify the chain offline.
    - Unwritable sink_dir (e.g. volume not mounted) -> logs `audit_sink.write_failed` and returns 0, no exception.
    - Worker cron `sink_audit_log` respects `settings.audit.sink_enabled=False` (immediate return).
  </behavior>
  <action>
    Create `app/security/audit_sink.py`: `async def export_audit_ndjson(session_factory, sink_dir: Path) -> int` implementing the behavior above. Read the cursor (int) with a tolerant parse; query `AuditLog` rows `WHERE id > cursor ORDER BY id ASC` via the session; group rows by created_at date; for each date open the file with `open(path, "ab")` and write `orjson.dumps(line_dict, option=orjson.OPT_SORT_KEYS) + b"\n"` (sync file I/O acceptable here: cron context, small batches — note the why in a brief comment per CLAUDE.md). Rewrite the cursor with the max exported id at the end. Entire body try/except -> `log.error("audit_sink.write_failed", error_type=...)` return 0.

    In `app/worker.py`: new job `async def sink_audit_log(ctx: dict[str, Any]) -> None` — if `not settings.audit.sink_enabled`: return. Resolve session_factory (same Pitfall 5 pattern), call `export_audit_ndjson(sf, settings.audit.sink_path)`, log count. Register in `functions` + `cron(sink_audit_log, hour={3}, minute={30})` (daily, after the verifier).

    Extend `tests/test_worker_audit.py`: use `tmp_path` as sink_dir with a stub session_factory returning 3 synthetic AuditLog-shaped rows across 2 dates -> assert 2 files, 3 lines total, cursor file contains max id; rerun with same rows -> returns 0, files unchanged; unwritable dir (a path under a FILE, not a dir) -> returns 0 without raising; sink_enabled False -> export function never called (patch recorder).
  </action>
  <verify>
    <automated>uv run pytest tests/test_worker_audit.py -q -m "not integration" &amp;&amp; uv run python -c "from app.worker import WorkerSettings; names=[f.__name__ for f in WorkerSettings.functions]; assert 'verify_audit_chain' in names and 'sink_audit_log' in names; crons=[getattr(c,'coroutine',None) or getattr(c,'func',None) for c in WorkerSettings.cron_jobs]; cron_names=[getattr(f,'__name__','') for f in crons]; assert 'verify_audit_chain' in cron_names and 'sink_audit_log' in cron_names; print('OK')"</automated>
  </verify>
  <done>
    Secondary sink appends NDJSON to the volume path incrementally and idempotently; both new crons registered (4 total); everything fail-open.
  </done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | DB-level chain manipulation | mitigate | daily verify_chain recompute + chain_tampered error alert; NDJSON replica allows offline re-verification |
| T-05-05-02 | Repudiation | audit rows lost on DB loss | mitigate | daily append-only NDJSON replica on persistent volume (SEC-03) |
| T-05-05-03 | Denial of service | sink/verifier failures kill worker | mitigate | both jobs fully fail-open; cron retries next day |
| T-05-05-04 | Information disclosure | NDJSON on shared volume | accept | replica carries hashes + metadata only (no raw payloads/PII by construction of audit_log) |
</threat_model>

<verification>
- `uv run pytest tests/test_worker_audit.py -q -m "not integration"` passes.
- `WorkerSettings.functions` includes 7 jobs; `cron_jobs` includes 4 entries.
- `uv run ruff check app/ && uv run black --check app/` clean.
- `uv run mypy --strict app/security/audit_sink.py` clean.
</verification>

<success_criteria>
- attachment_received completes the worker-side SEC-04 capture point.
- Chain verification runs daily and surfaces tampering as a Sentry-visible error event.
- Secondary append-only sink operational on the existing Railway volume (SEC-03), fail-open when the volume is absent.
</success_criteria>

<output>
Create `.planning/phases/05-seguridad-y-audit-log/05-05-SUMMARY.md` when done.
</output>
