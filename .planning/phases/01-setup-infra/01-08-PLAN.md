---
phase: 01-setup-infra
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - GOAL-1.16  # Update CLAUDE.md quick-start to uv instead of pip
estimated_complexity: S

must_haves:
  truths:
    - "El bloque '## Quick start' de CLAUDE.md ya NO contiene 'pip install -r requirements.txt'"
    - "El bloque '## Quick start' usa uv para setup (uv sync) y para correr comandos (uv run uvicorn, uv run pytest, uv run ruff)"
    - "El bloque documenta el comando alternativo `uv pip compile pyproject.toml -o requirements.txt` para flujos que aún requieran requirements.txt (CI hosts limitados, contenedores externos)"
    - "El bloque referencia que el lockfile de verdad es `uv.lock`"
    - "El resto de CLAUDE.md queda intacto — no se modifican secciones de arquitectura, stack, reglas críticas, etc."
  artifacts:
    - path: CLAUDE.md
      provides: "Briefing rápida del repo con quick-start corregido"
  key_links: []
---

<objective>
Reemplazar el bloque "## Quick start" de CLAUDE.md para usar `uv` (CONTEXT.md D-02) en lugar de `pip install -r requirements.txt`. Documentar el comando alternativo para generar un `requirements.txt` derivado si algún flujo externo lo requiere.

Purpose: CLAUDE.md es la briefing principal del repo — devs (humanos o Claude futuro) lo leen primero. Si dice `pip install -r requirements.txt` y el repo no tiene ese archivo, hay confusión inmediata.

Output: CLAUDE.md actualizada con quick-start correcto. Una sola edición quirúrgica.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-setup-infra/CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editar CLAUDE.md — reemplazar el bloque Quick start</name>
  <files>CLAUDE.md</files>
  <action>
    Editar `CLAUDE.md` (en repo root) reemplazando ÚNICAMENTE el bloque "## Quick start" (líneas 9-24 del estado actual). NO tocar:
    - El header (líneas 1-7)
    - "## Arquitectura: Vertical Slice" (líneas 39+)
    - "## Stack" / "## Reglas críticas" / "## Seguridad" / "## Memoria multi-capa" / "## Integración con lambda-proyect" / "## Cliente: DPG Seguros" / "## Out of scope" / "## Convenciones de código" / "## Cómo navegar este repo" / footer

    El bloque actual:

    ```
    ## Quick start

    ```bash
    # Setup
    cp .env.example .env  # Llenar credenciales antes de correr
    pip install -r requirements.txt

    # Dev local
    uvicorn app.main:app --reload

    # Tests
    pytest

    # Lint
    ruff check . && black --check .
    ```

    Variables de entorno críticas (ver `.env.example` para la lista completa):
    ...
    ```

    Reemplazar el bloque completo (entre `## Quick start` y la siguiente sección `---` o `## Arquitectura`) por:

    ```markdown
    ## Quick start

    Prerequisitos: Python 3.12, [uv](https://docs.astral.sh/uv/) (`pip install uv` o vía Homebrew).

    ```bash
    # Setup
    cp .env.example .env                  # Llenar credenciales antes de correr (ver .env.example)
    uv sync --frozen                      # Instala todas las deps según uv.lock (fuente de verdad)

    # Dev local
    uv run uvicorn app.main:app --reload  # FastAPI en http://localhost:8000

    # Tests
    uv run pytest -q

    # Lint + format + type-check
    uv run ruff check .
    uv run ruff format --check .
    uv run black --check .
    uv run mypy app

    # Hooks pre-commit (una sola vez tras clonar)
    uv run pre-commit install

    # Migraciones DB (requiere POSTGRES_URL en .env)
    uv run alembic upgrade head
    ```

    El lockfile autoritativo es `uv.lock` (commiteado). `pyproject.toml` define las dependencias. **NO existe `requirements.txt`** en este repo.

    Si algún flujo externo (CI legado, contenedor antiguo) requiere un `requirements.txt`, generarlo on-the-fly sin commitearlo:

    ```bash
    uv pip compile pyproject.toml -o requirements.txt
    ```

    Variables de entorno críticas (ver `.env.example` para la lista completa):
    ```

    Mantener la lista de env vars (`OPENROUTER_API_KEY`, `LLM_MODEL_*`, etc.) tal como está — esa parte no cambia.

    Justificación inline al final del bloque (NO requerido en el output, pero documentar en SUMMARY): CONTEXT.md D-02 lockeó uv + pyproject.toml + uv.lock como standard. RESEARCH.md State of the Art confirma uv como herramienta de-facto 2026. Mantener referencias a pip rompe la consistencia.

    NO añadir `pip install -r requirements.txt` como fallback en el quick-start — eso induce a devs nuevos a usar el flujo equivocado. El comando `uv pip compile` queda como nota de pie por si alguien lo necesita realmente.

    Verificar tras la edición que el archivo siga válido:
    - El `## Quick start` block reemplazado
    - Resto intacto (mismo número de secciones, mismo footer)
    - Markdown válido (no fenced blocks rotos)
  </action>
  <verify>
    <automated>grep -c "pip install -r requirements.txt" CLAUDE.md | grep -q "^0$" && grep -q "uv sync --frozen" CLAUDE.md && grep -q "uv run uvicorn app.main:app" CLAUDE.md && grep -q "uv pip compile pyproject.toml" CLAUDE.md && grep -q "Arquitectura: Vertical Slice" CLAUDE.md</automated>
  </verify>
  <done>
    CLAUDE.md ya no contiene `pip install -r requirements.txt`; contiene `uv sync --frozen` y `uv run`; menciona `uv pip compile` como fallback; el resto del documento permanece intacto.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| dev → CLAUDE.md | Doc-only edit; no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-34 | Information disclosure | Doc accidentally exposes secret in example | accept | Edit only modifies the install/run section; no env values added |

</threat_model>

<verification>
- `grep "pip install -r requirements.txt" CLAUDE.md` returns 0 matches
- `grep "uv sync" CLAUDE.md` returns ≥1 match
- Rest of CLAUDE.md unchanged (verify by diff that no other sections modified)
</verification>

<success_criteria>
- Quick start block uses uv exclusively
- Fallback note for `uv pip compile` present
- All other CLAUDE.md sections untouched
</success_criteria>

<output>
Create `.planning/phases/01-setup-infra/01-08-SUMMARY.md` when done with:
- Diff summary (lines changed)
- Confirmation that no other section was modified
</output>
