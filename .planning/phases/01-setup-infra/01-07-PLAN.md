---
phase: 01-setup-infra
plan: 07
type: execute
wave: 5
depends_on:
  - 01-05
  - 01-06
files_modified:
  - .planning/phases/01-setup-infra/SMOKE_E2E.md
autonomous: false
requirements:
  - GOAL-1.SC1   # GET /health 200 con Postgres + Redis + LangSmith conectados
  - GOAL-1.SC2   # /test/llm invoca get_llm("conversation") y retorna respuesta OpenRouter
  - GOAL-1.SC3   # Chatwoot panel accesible chat.landatech.org admin login + crear inbox
  - GOAL-1.SC4   # Trace dummy en LangSmith
  - GOAL-1.SC5   # Error sintético en Sentry
estimated_complexity: S

must_haves:
  truths:
    - "curl https://<agent-domain>/health retorna 200 + status=healthy + 4 components ok (postgres, redis, openrouter, langsmith_env)"
    - "curl -X POST https://<agent-domain>/test/llm con payload {text: 'ping'} retorna 200 + JSON {reply, model, role, latency_ms} con reply no vacío y model=google/gemini-2.0-pro"
    - "https://chat.landatech.org carga panel Chatwoot, admin login funciona, posible crear inbox manualmente"
    - "LangSmith dashboard del proyecto landa-agent-dev muestra el trace de la última llamada /test/llm con prompt 'ping', latencia, tokens"
    - "Sentry dashboard del proyecto correspondiente muestra el evento sintético disparado por /test/sentry con stack trace + correlation_id"
    - "Documento SMOKE_E2E.md registra los 5 verifications con timestamps + screenshots/output redacted"
  artifacts:
    - path: .planning/phases/01-setup-infra/SMOKE_E2E.md
      provides: "Reporte verificación E2E de los 5 success criteria del ROADMAP"
  key_links: []
---

<objective>
Cerrar Phase 1 ejecutando un smoke test E2E que verifica los 5 success criteria del ROADMAP en orden. Cada uno se valida individualmente y el resultado consolidado se documenta en `SMOKE_E2E.md`. Si algún criterio falla, Phase 1 NO está done — el plan registra el gap y se cierra solo cuando todos pasen.

Purpose: Single source of truth de que F1 está done según los criteria que el ROADMAP definió, antes de habilitar F2.

Output: SMOKE_E2E.md verde + confirmación al operador de que F1 cerró.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-setup-infra/CONTEXT.md
@.planning/phases/01-setup-infra/01-05-SUMMARY.md
@.planning/phases/01-setup-infra/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ejecutar smoke commands de /health, /test/llm, /test/sentry contra Railway agent-app</name>
  <files>.planning/phases/01-setup-infra/SMOKE_E2E.md</files>
  <action>
    1. Recuperar el dominio público de agent-app del summary del plan 05:
       ```
       AGENT_DOMAIN=$(grep -oE "[a-z0-9-]+\.up\.railway\.app|agent\.landatech\.org" .planning/phases/01-setup-infra/01-05-SUMMARY.md | head -1)
       ```

    2. **SC1 — /health**:
       ```
       HEALTH=$(curl -sS https://${AGENT_DOMAIN}/health)
       echo "$HEALTH" | jq .
       ```
       Verificar:
       - HTTP status = 200
       - `.status == "healthy"`
       - `.components.postgres.ok == true`
       - `.components.redis.ok == true`
       - `.components.openrouter.ok == true`
       - `.components.langsmith_env.ok == true`

       Si alguno falla, NO continuar. Marcar SC1 = FAIL en SMOKE_E2E.md y reportar.

    3. **SC2 — /test/llm**:
       ```
       LLM_RESPONSE=$(curl -sS -X POST https://${AGENT_DOMAIN}/test/llm \
         -H "Content-Type: application/json" \
         -d '{"text":"Responde con exactamente la palabra: pong"}')
       echo "$LLM_RESPONSE" | jq .
       ```
       Verificar:
       - HTTP status = 200
       - `.reply` no vacío, idealmente contiene "pong" (la respuesta exacta depende del modelo y temperature 0.7, así que también acepta cualquier no-empty)
       - `.model == "google/gemini-2.0-pro"`
       - `.role == "conversation"`
       - `.latency_ms` < 10000

    4. **SC5 — /test/sentry** (lo ejecutamos antes de chequear Sentry en step 6):
       ```
       curl -sS -X POST https://${AGENT_DOMAIN}/test/sentry
       ```
       Esperar HTTP 500 (Internal Server Error). Esto es deliberado — el endpoint raisea para que Sentry capture.

    5. Capturar correlation_id de los responses (X-Request-ID header) para correlacionar luego con LangSmith y Sentry:
       ```
       LLM_REQ_ID=$(curl -sSI -X POST https://${AGENT_DOMAIN}/test/llm \
         -H "Content-Type: application/json" \
         -d '{"text":"trace-marker-smoke-test"}' | grep -i "x-request-id:" | cut -d' ' -f2 | tr -d '\r\n')
       echo "Correlation ID for LangSmith lookup: $LLM_REQ_ID"
       ```

       Anotar el correlation_id en SMOKE_E2E.md para que el operador lo busque en LangSmith.

    6. Escribir resultados parciales en `SMOKE_E2E.md`:

    ```markdown
    # Phase 1 — End-to-end smoke verification

    **Executed**: <timestamp>
    **Agent domain**: <domain>
    **Operator**: <name>

    ## Results

    | SC | Criterion | Status | Evidence |
    |---|---|---|---|
    | SC1 | GET /health returns 200 + healthy | ☐ PASS / ☐ FAIL | (see below) |
    | SC2 | POST /test/llm returns OpenRouter reply | ☐ PASS / ☐ FAIL | (see below) |
    | SC3 | Chatwoot panel accessible + admin login + create inbox | ☐ PASS / ☐ FAIL | (manual) |
    | SC4 | LangSmith trace appears for /test/llm call | ☐ PASS / ☐ FAIL | (manual via LangSmith UI) |
    | SC5 | Sentry receives synthetic error from /test/sentry | ☐ PASS / ☐ FAIL | (manual via Sentry UI) |

    ## SC1 — /health
    Command: `curl -sS https://<domain>/health`
    Response:
    ```json
    <paste response>
    ```
    Result: PASS/FAIL

    ## SC2 — /test/llm
    Command: `curl -X POST .../test/llm -d '{"text":"..."}' `
    Response:
    ```json
    <paste response, with reply truncated if long>
    ```
    Correlation ID for LangSmith lookup: `<X-Request-ID>`
    Result: PASS/FAIL
    ```

    Dejar SC3/SC4/SC5 como pendientes — los completa Task 2 (humano).
  </action>
  <verify>
    <automated>AGENT_DOMAIN=$(grep -oE "[a-z0-9-]+\.up\.railway\.app|agent\.landatech\.org" .planning/phases/01-setup-infra/01-05-SUMMARY.md | head -1) && curl -sSf https://${AGENT_DOMAIN}/health | jq -e '.status == "healthy"' && curl -sSf -X POST https://${AGENT_DOMAIN}/test/llm -H "Content-Type: application/json" -d '{"text":"ping"}' | jq -e '.reply | length > 0' && test -f .planning/phases/01-setup-infra/SMOKE_E2E.md</automated>
  </verify>
  <done>
    SC1 y SC2 PASS automatizado; /test/sentry disparado; SMOKE_E2E.md creado con SC1+SC2 documentados y SC3/SC4/SC5 pendientes de verificación humana.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2 (HUMAN): Verificar SC3 (Chatwoot login + inbox), SC4 (LangSmith trace), SC5 (Sentry event)</name>
  <what-built>
    Claude no tiene acceso a dashboards SaaS (Chatwoot, LangSmith, Sentry). Operador verifica visualmente y confirma.
  </what-built>
  <how-to-verify>
    **SC3 — Chatwoot panel**:
    1. Navegar a https://chat.landatech.org
    2. Login con admin (credenciales del plan 06)
    3. Confirmar que el dashboard carga
    4. Click "Inboxes" → "Add inbox" → seleccionar cualquier canal (e.g. "API"); completar wizard mínimo; confirmar que se crea
    5. Borrar el inbox de prueba (no queremos garbage en F2+)

    **SC4 — LangSmith trace**:
    1. Login en https://smith.langchain.com con cuenta LANDA
    2. Seleccionar proyecto `landa-agent-dev`
    3. Buscar el trace correspondiente al `correlation_id` capturado en Task 1 step 5 (LangSmith no indexa custom headers automáticamente, pero el trace debería estar dentro de los últimos N traces)
    4. Confirmar que existe al menos un trace con:
       - prompt "trace-marker-smoke-test" (o "ping" según cuál curl se haya hecho último)
       - modelo google/gemini-2.0-pro
       - status: success
       - tokens usage > 0
    5. Capturar screenshot (opcional) o un trace URL

    **SC5 — Sentry event**:
    1. Login en sentry.io, proyecto correspondiente
    2. Buscar evento reciente con título conteniendo "RuntimeError" + "synthetic test error from /test/sentry"
    3. Confirmar que tiene:
       - environment = dev
       - release = 0.1.0
       - tag `transaction` con value relacionado al endpoint /test/sentry
       - stack trace con archivo `app/main.py`
       - NO PII visible en el evento (verificar que no haya phones/credenciales en headers/breadcrumbs — el scrubber debe haber actuado; revisar la sección "request" del evento)

    Reportar al ejecutor:
    ```
    SC3 chatwoot: ok (inbox creado + borrado)
    SC4 langsmith: ok (trace ID o URL: <paste>)
    SC5 sentry: ok (event ID: <paste>) — PII check: clean / dirty (describe)
    ```

    Si alguno falla:
    - SC3 fail → revisar plan 06 (DNS, SSL, admin password)
    - SC4 fail → verificar env vars LANGSMITH_* en Railway, especialmente LANGSMITH_TRACING=true
    - SC5 fail → verificar SENTRY_DSN, scope env vs proyecto
  </how-to-verify>
  <resume-signal>
    Operador pega los 3 status. Si todos "ok", Claude actualiza SMOKE_E2E.md con los 5 PASS y cierra. Si alguno falla, Claude crea un sub-plan de gap closure o un ticket para fix.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Consolidar SMOKE_E2E.md final + cerrar Phase 1</name>
  <files>.planning/phases/01-setup-infra/SMOKE_E2E.md</files>
  <action>
    Actualizar SMOKE_E2E.md con los resultados completos de SC3/SC4/SC5 (reportados por el operador en Task 2). Marcar los 5 criteria como PASS si todos verdes.

    Añadir sección al final:

    ```markdown
    ## Phase 1 closure

    All 5 success criteria PASS. Phase 1 (Setup infra) is done.

    **What works now:**
    - Repo scaffolded with vertical-slice structure
    - Dependencies pinned in pyproject.toml + uv.lock
    - Dockerfiles produce runtime images <120MB
    - Postgres + Redis provisioned on Railway with private networking
    - LangGraph AsyncPostgresSaver wired into FastAPI lifespan with explicit __aenter__/__aexit__
    - alembic migration 0001 applied; checkpoint tables exist
    - Chatwoot self-hosted at https://chat.landatech.org with valid SSL, admin login, idle
    - LangSmith tracing active in project landa-agent-dev
    - Sentry receives errors with PII-scrubbed payloads
    - structlog JSON logs with correlation_id + PII redaction
    - OpenRouter LLM factory `get_llm(role)` cached per role
    - GET /health verifies stack health
    - POST /test/llm round-trips through OpenRouter
    - CI green: ruff + black + mypy + pytest

    **What is explicitly NOT done (deferred):**
    - WhatsApp / Meta Cloud API integration (F2)
    - SoftSeguros client (F2)
    - LangGraph state machine for the bot (F3)
    - KB content auditor (F3)
    - Security pipeline (F3-F5)
    - Audit log immutable (F5)
    - lambda-proyect handoff (F6)
    - Production cutover, real DPG traffic (F7)

    **Next**: `/gsd-plan-phase 02-softseguros-meta-cloud` to start F2.
    ```

    Si algún SC falló y no se pudo cerrar, NO escribir la sección "Phase 1 closure". En su lugar, escribir:

    ```markdown
    ## Phase 1 status: BLOCKED

    Failing criteria:
    - <SCn>: <descripción del fallo>

    Action: Run `/gsd-plan-phase 01-setup-infra --gaps` to create gap-closure plans.
    ```
  </action>
  <verify>
    <automated>grep -E "Phase 1 closure|BLOCKED" .planning/phases/01-setup-infra/SMOKE_E2E.md && grep -c "PASS" .planning/phases/01-setup-infra/SMOKE_E2E.md</automated>
  </verify>
  <done>
    SMOKE_E2E.md tiene los 5 SC marcados PASS y la sección "Phase 1 closure" (o BLOCKED si falló alguno con instrucciones de gap-closure).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator → SaaS dashboards | Auth via OAuth/PAT for Chatwoot/LangSmith/Sentry |
| /test/sentry → Sentry | Deliberate exception payload to verify scrubber doesn't leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-32 | Information disclosure | Operator pastes correlation_id with PII in SMOKE_E2E.md | mitigate | SMOKE_E2E.md commits to repo; instructions explicitly say "redact PII when pasting responses"; correlation_ids are UUIDs without PII |
| T-01-33 | Information disclosure | Sentry event contains unscrubbed phone from request body | mitigate | SC5 verification step explicitly requires checking the event for PII; if dirty, F1 is BLOCKED until scrubber tightened |

</threat_model>

<verification>
- All 5 success criteria PASS (4 automated + 1 manual chatwoot test)
- SMOKE_E2E.md exists and has "Phase 1 closure" section
- No PII leaked in Sentry event
</verification>

<success_criteria>
- SC1: /health 200 + healthy
- SC2: /test/llm 200 + reply from OpenRouter
- SC3: Chatwoot login + inbox create works
- SC4: LangSmith trace visible in landa-agent-dev project
- SC5: Sentry receives synthetic error with PII scrubbed
- SMOKE_E2E.md complete + Phase 1 marked done
</success_criteria>

<output>
Create `.planning/phases/01-setup-infra/01-07-SUMMARY.md` when done with:
- 5 success criteria status (PASS/FAIL)
- Any gaps to address (if BLOCKED)
- Link to SMOKE_E2E.md
- Recommendation: proceed to F2 or run gap-closure
</output>
