---
phase: 03-bot-q-a-inbound-chatwoot-mirror
plan: 01
type: execute
wave: 1
depends_on:
  - 03-00
files_modified:
  - app/config/settings.py
  - app/models/softseguros.py
  - app/features/qa/__init__.py
  - app/features/qa/state.py
  - app/features/qa/graph.py
  - app/features/qa/nodes.py
  - app/features/qa/tools.py
  - app/features/qa/prompts.py
  - app/features/qa/knowledge_base.py
  - app/features/qa/messages.py
  - app/security/__init__.py
  - app/security/prompt_firewall.py
  - app/security/judge.py
  - app/security/kb_auditor.py
  - app/integrations/chatwoot.py
  - knowledge/dpg_cartera.md
  - tests/conftest.py
  - .pre-commit-config.yaml
  - .env.example
autonomous: true
requirements:
  - D-04   # 5 nodos del grafo (QAState Literal field)
  - D-05   # JudgeRubric schema (forward declaration via skeleton)
  - D-09   # KB stub mini-doc 1 página
  - D-13   # Templates en messages.py (forward declaration)
  - D-16   # 8 templates lockeados (file header + module-level NotImplementedError stubs)
  - "Claude's Discretion (ChatwootSettings env_prefix)"

must_haves:
  truths:
    - "`ChatwootSettings(env_prefix='CHATWOOT_')` existe en `app/config/settings.py` con `url: str`, `api_key: SecretStr`, `account_id: int`, `inbox_id: int`; root `Settings` la compone con `chatwoot: ChatwootSettings = Field(default_factory=ChatwootSettings)`"
    - "`app/models/softseguros.py` exporta `ClienteRaw` (narrowed con TypedDict si Plan 03-00 PROBE confirmó shape, else `dict[str, Any]` con TODO), `EstadoCodigo` Enum con 8 valores de SOFTSEGUROS_API_NOTES.md, y los 4 sanitized DTOs: `SaldoResponse`, `EstadoResponse`, `CoberturasResponse`, `PolizaSummary`"
    - "`app/features/qa/state.py` exporta `QAState` TypedDict con campos: `messages` (Annotated list[BaseMessage] con add_messages reducer), `poliza_id: str | None`, `cliente_doc: str | None`, `polizas_list: list[dict[str, Any]]`, `doc_retries: int`, `judge_retries: int`, `node: Literal[5-nodes]`"
    - "Module skeletons creados con stubs que pasan `mypy --strict`: cada archivo nuevo tiene `from __future__ import annotations`, `__all__` declarado, structlog logger `log = structlog.get_logger('<dotted.path>')`, y al menos 1 callable público con signature definida pero cuerpo `raise NotImplementedError(...)` con TODO de qué plan lo implementa (`# implemented in Plan 03-NN`)"
    - "`knowledge/dpg_cartera.md` existe con ~300-500 tokens (verificado con `wc -w` retorna 250-450 palabras), 4 secciones markdown: `## Coberturas generales`, `## FAQs frecuentes`, `## Procedimientos de cartera`, `## Horarios de atención`. Contenido placeholder coherente con DPG (AUTOMÓVILES top ramo per SOFTSEGUROS_API_NOTES.md)"
    - "`tests/conftest.py` extiende `_test_env` fixture con `CHATWOOT_URL`, `CHATWOOT_API_KEY`, `CHATWOOT_ACCOUNT_ID`, `CHATWOOT_INBOX_ID` (valores dummy ok — `chat-test`, `cw-test-key`, `1`, `2`)"
    - "`.pre-commit-config.yaml` `additional_dependencies` de mypy incluye `langgraph==1.2.6`, `langchain==1.3.11`, `langsmith==0.9.3` (langchain-openai ya estaba en F1)"
    - "`.env.example` documenta `CHATWOOT_URL`, `CHATWOOT_API_KEY`, `CHATWOOT_ACCOUNT_ID`, `CHATWOOT_INBOX_ID` con comentarios explicando 'API Channel inbox, separado del WhatsApp inbox de F4'"
    - "`uv run pytest -x` pasa todo lo que pasaba al inicio (existing 66 tests siguen verdes — los skeletons no rompen imports)"
    - "`uv run mypy --strict app/` no introduce nuevos errors (skeletons pasan strict via NotImplementedError pattern)"
  artifacts:
    - path: app/config/settings.py
      provides: "ChatwootSettings + root settings.chatwoot wiring"
      contains: "class ChatwootSettings(BaseSettings)"
    - path: app/models/softseguros.py
      provides: "ClienteRaw + EstadoCodigo + 4 sanitized DTOs"
      contains: "class EstadoCodigo(str, Enum)"
    - path: app/features/qa/state.py
      provides: "QAState TypedDict for LangGraph StateGraph compile (Plan 03-05)"
      contains: "class QAState(TypedDict)"
    - path: app/features/qa/graph.py
      provides: "build_qa_graph() stub — implementation in 03-05"
      contains: "def build_qa_graph() -> StateGraph"
    - path: app/features/qa/nodes.py
      provides: "5 node function stubs — implementation in 03-05"
      contains: "async def node_identify"
    - path: app/features/qa/tools.py
      provides: "4 tool function stubs — implementation in 03-05"
      contains: "@tool"
    - path: app/features/qa/prompts.py
      provides: "system_prompt() stub — implementation in 03-05"
    - path: app/features/qa/knowledge_base.py
      provides: "load_kb() stub with @lru_cache — implementation in 03-05"
    - path: app/features/qa/messages.py
      provides: "T_01..T_08 constants stub + interpolate_t04 stub — implementation in 03-05"
    - path: app/security/prompt_firewall.py
      provides: "sanitize() + SanitizeResult dataclass stub — implementation in 03-04"
    - path: app/security/judge.py
      provides: "JudgeRubric + judge_response() + is_approved() stub — implementation in 03-04"
    - path: app/security/kb_auditor.py
      provides: "audit_kb() stub + KBAuditRubric — implementation in 03-04"
    - path: app/integrations/chatwoot.py
      provides: "ChatwootClient + get_chatwoot_client() stub — implementation in 03-03"
    - path: knowledge/dpg_cartera.md
      provides: "KB stub realistic for kb_auditor pipeline testing"
      contains: "## Coberturas generales"
  key_links:
    - from: app/config/settings.py
      to: "app/integrations/chatwoot.py (factory reads settings.chatwoot.*) + app/main.py (lifespan reads settings.chatwoot.* in 03-05)"
      via: "settings.chatwoot.{url|api_key|account_id|inbox_id}"
      pattern: "settings\\.chatwoot\\."
    - from: app/features/qa/state.py
      to: "app/features/qa/graph.py + nodes.py + tools.py"
      via: "QAState TypedDict shared shape"
      pattern: "from app\\.features\\.qa\\.state import QAState"
    - from: tests/conftest.py CHATWOOT_* env vars
      to: "all new tests in 03-03/03-04/03-05 that import settings"
      via: "_test_env fixture autouse"
      pattern: "CHATWOOT_(URL|API_KEY|ACCOUNT_ID|INBOX_ID)"

---

<objective>
Wave 1 foundation: contracts y skeletons que todo Wave 2+ consume. `ChatwootSettings`, modelos Pydantic narrowed con los findings del Probe 03-00, `QAState` TypedDict, módulo skeletons con stubs raise-NotImplementedError-pero-mypy-clean para los 14 nuevos archivos, KB stub realista, conftest extiende env vars Chatwoot, pre-commit mypy deps se actualizan.

Purpose: Definir el contract shape de TODO Phase 3 en un solo plan secuencial antes de paralelizar Wave 2. Los 3 planes de Wave 2 (03-02/03/04) van a importar de `app/models/softseguros.py`, `app/config/settings.py`, `app/features/qa/state.py` — si los contracts no están lockeados antes, los 3 planes en paralelo van a divergir.

Output: 14 new files + 4 modified files. Tests existentes siguen verdes (los stubs no se invocan en runtime aún). `uv run mypy --strict app/` pasa. Pre-commit clean.

Implementa D-04 (QAState Literal con 5 nodos), D-09 (KB stub), forward declarations de D-05/D-13/D-16 (skeletons listos para implementar en Wave 2/3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-CONTEXT.md
@.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-RESEARCH.md
@.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-PATTERNS.md
@.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/SOFTSEGUROS_API_NOTES.md
@app/config/settings.py
@app/models/softseguros.py
@app/integrations/openrouter.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatwootSettings + ClienteRaw narrowing + EstadoCodigo + sanitized DTOs + .env.example</name>
  <read_first>
    - app/config/settings.py (lines 155-182 `WhatsAppSettings` pattern — exact analog para ChatwootSettings; lines 215-260 root `Settings` composition + `__all__`)
    - app/models/softseguros.py (passthrough alias pattern + module docstring — F3 expand sin romper PolizaRaw alias existente)
    - .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md (PROBE_1_RESULTS_ITEM_KEYS_SAMPLE para narrowing de ClienteRaw)
    - .planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/SOFTSEGUROS_API_NOTES.md (8 estados enum, allowlist fields para SaldoResponse/EstadoResponse/CoberturasResponse)
    - .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-CONTEXT.md §"Tool output sanitization allowlist por endpoint SoftSeguros" (allowlist exacto por DTO)
    - .env.example (formato y comentarios existentes para WA_/SOFTSEGUROS_)
  </read_first>
  <action>
    Sub-task 1a — `ChatwootSettings` en `app/config/settings.py`:
    Copiar exactamente el patrón de `WhatsAppSettings` (lines 155-182). Construir clase `ChatwootSettings(BaseSettings)` con `model_config = SettingsConfigDict(env_prefix="CHATWOOT_", env_file=".env", extra="ignore", case_sensitive=False)`. Campos: `url: str = "https://chat.landatech.org"` (default; el operador puede override), `api_key: SecretStr` (REQUIRED, sin default — falla import si missing como WhatsAppSettings.token), `account_id: int` (REQUIRED), `inbox_id: int` (REQUIRED). Agregar `chatwoot: ChatwootSettings = Field(default_factory=ChatwootSettings)` en root `Settings` class. Agregar `"ChatwootSettings"` al `__all__`.

    Sub-task 1b — Expansion de `app/models/softseguros.py`:
    Mantener `PolizaRaw = dict[str, Any]` (no romper F2 callers). Agregar:
    - `from enum import Enum`
    - `from pydantic import BaseModel`
    - `class EstadoCodigo(str, Enum)`: enumera los 8 valores literales de SOFTSEGUROS_API_NOTES.md (VIGENTE, VENCIDA, CANCELADA, etc — consultar el doc para nombres exactos; si SOFTSEGUROS_API_NOTES.md no enumera los 8 explícitamente, usar los visibles + agregar TODO comment con `# TODO: confirm full 8-state enum from /api/estadopoliza/ probe`).
    - `ClienteRaw`: si 03-00-PROBE.md confirmó shape, narrow a `TypedDict` con los campos confirmados. Si shape no confirmado, dejar `ClienteRaw = dict[str, Any]` con comment `# Narrow when Plan 03-02 probe finalizes`.
    - `class SaldoResponse(BaseModel)`: campos `saldo_pendiente: float | None`, `proximo_pago_monto: float | None`, `proximo_pago_fecha: str | None`, `moneda: str | None` (allowlist per CONTEXT D Claude's Discretion).
    - `class EstadoResponse(BaseModel)`: `estado_poliza_nombre: str | None`, `fecha_inicio: str | None`, `fecha_fin: str | None`, `ramo_nombre: str | None`, `numero_poliza: str | None`.
    - `class CoberturasResponse(BaseModel)`: `coberturas: list[dict[str, Any]] = []` (cada dict con `nombre`, `monto_asegurado`, `deducible` — usar `list[Cobertura]` si Cobertura submodel es trivial).
    - `class PolizaSummary(BaseModel)`: `poliza_id: str`, `numero_poliza: str`, `ramo_nombre: str`, `estado: str` (para T-04 lista).
    - Actualizar `__all__` con todos los nuevos exports.

    Sub-task 1c — `.env.example`:
    Agregar bloque sección "Chatwoot" después del bloque WhatsApp existente. Documentar:
    ```
    # === Chatwoot (Phase 3 — API Channel mirror) ===
    # Self-hosted instance from F1. The inbox here is the "API Channel"
    # dedicated to bot mirror (separate from the WhatsApp native inbox of F4).
    CHATWOOT_URL=https://chat.landatech.org
    CHATWOOT_API_KEY=<user-level api_access_token from Chatwoot Profile>
    CHATWOOT_ACCOUNT_ID=<int — visible in chat URL /app/accounts/N/...>
    CHATWOOT_INBOX_ID=<int — id of the API Channel inbox created in 03-00>
    ```
  </action>
  <verify>
    <automated>uv run python -c "from app.config.settings import ChatwootSettings, settings; assert hasattr(settings, 'chatwoot'); assert settings.chatwoot.account_id == 1 or isinstance(settings.chatwoot.account_id, int)" && uv run python -c "from app.models.softseguros import ClienteRaw, EstadoCodigo, SaldoResponse, EstadoResponse, CoberturasResponse, PolizaSummary; assert len(list(EstadoCodigo)) >= 2" && grep -q "CHATWOOT_URL=" .env.example && grep -q "CHATWOOT_API_KEY=" .env.example</automated>
  </verify>
  <done>
    Imports funcionan, settings root tiene `.chatwoot`, modelos exportan los 6 nuevos símbolos, `.env.example` documenta los 4 Chatwoot env vars con comentarios explicativos.
  </done>
  <acceptance_criteria>
    - `python -c "from app.config.settings import ChatwootSettings"` exit 0
    - `python -c "from app.models.softseguros import EstadoCodigo, SaldoResponse, EstadoResponse, CoberturasResponse, PolizaSummary"` exit 0
    - `grep -c "^CHATWOOT_" .env.example` ≥ 4
    - `uv run mypy --strict app/config/settings.py app/models/softseguros.py` exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: QAState + 14 module skeletons + KB stub + conftest env vars + pre-commit deps</name>
  <read_first>
    - app/features/handoff/echo.py (analog para messages.py pure-functions pattern)
    - app/integrations/meta_cloud.py (analog para chatwoot.py: docstring, structlog logger naming, __all__, factory `@lru_cache(maxsize=1)`)
    - app/integrations/softseguros.py (analog para kb_auditor.py read-through cache pattern docstring reference)
    - app/integrations/openrouter.py (analog para judge.py: `get_llm` usage + `lru_cache` + role enforcement)
    - .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-RESEARCH.md §"Pattern 1: LangGraph StateGraph" (QAState canonical shape) + §"Pattern 5: Chatwoot Application API" (header `api_access_token`) + §"Code Examples — settings.py addition" + §"Code Examples — pre-commit mypy additional_dependencies update"
    - .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-PATTERNS.md §entire file (each section names the exact analog + line numbers for each new file)
    - .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-CONTEXT.md §"Catálogo de 8 templates lockeados" (D-16 copy exacto)
    - tests/conftest.py (lines 12-55 `_test_env` fixture pattern)
    - .pre-commit-config.yaml (existing mypy hook + additional_dependencies block from F1/F2)
  </read_first>
  <action>
    Sub-task 2a — `app/features/qa/state.py`:
    Import `from __future__ import annotations`, `from typing import Annotated, Any, Literal, TypedDict`, `from langchain_core.messages import BaseMessage`, `from langgraph.graph.message import add_messages`. Definir `QAState(TypedDict)` con los 7 campos enumerados en `must_haves`. Literal del `node` campo debe ser exactamente `Literal["awaiting_identification", "awaiting_policy_choice", "answering_qa", "escalating", "closed"]` (per D-04). `__all__ = ["QAState"]`.

    Sub-task 2b — `app/features/qa/__init__.py`: archivo vacío (package marker). `app/security/__init__.py`: igual.

    Sub-task 2c — Module skeletons (12 archivos). Cada uno con:
    - `"""Module docstring explicando role + qué plan lo implementa."""` (1-2 párrafos cortos)
    - `from __future__ import annotations`
    - imports mínimos necesarios para que mypy --strict pase
    - `log = structlog.get_logger("<dotted.path>")` donde aplique
    - Public callables con signature completa pero body `raise NotImplementedError("Implemented in Plan 03-NN")`
    - `__all__` declarado

    Skeletons:

    - `app/features/qa/graph.py`: `def build_qa_graph() -> StateGraph: raise NotImplementedError("Implemented in Plan 03-05")`. Imports: `from langgraph.graph import StateGraph`. `__all__ = ["build_qa_graph"]`.

    - `app/features/qa/nodes.py`: 5 async functions stubs `node_identify`, `node_choose_policy`, `node_answer`, `node_escalate`, `node_close`, todas con signature `async def node_X(state: QAState) -> dict[str, Any]` y body NotImplementedError. Import `QAState` desde `.state`. `__all__` con los 5.

    - `app/features/qa/tools.py`: 4 stubs `get_saldo`, `get_estado`, `get_coberturas`, `escalate_to_human`. Cada uno decorado con `@tool` de `langchain_core.tools`. Signatures correctas según RESEARCH Pattern 2 (3 con `poliza_id: Annotated[str, InjectedState("poliza_id")]`, `escalate_to_human` con `reason: str`). Body NotImplementedError. `__all__` con los 4.

    - `app/features/qa/prompts.py`: `def system_prompt(kb_content: str, poliza_id: str | None = None, l4_flags: dict[str, Any] | None = None) -> str`. NotImplementedError. `__all__ = ["system_prompt"]`.

    - `app/features/qa/knowledge_base.py`: `@lru_cache(maxsize=1)` + `def load_kb() -> str`. NotImplementedError. `__all__ = ["load_kb"]`.

    - `app/features/qa/messages.py`: declarar `T_01: str`, `T_02: str`, ..., `T_08: str` como module-level `str` annotations **con los copy exactos lockeados de CONTEXT D-16** (no NotImplementedError — los strings se quedan ya, son data no logic). Agregar `ESCAPE_REGEX: re.Pattern[str] = re.compile(r"...", re.I)` con el regex de D-15 Layer 1 (`\b(humano|agente|persona|asesor|representante|hablar con alguien|persona real)\b`). Función `def interpolate_t04(n: int, lista_numerada: str) -> str: return T_04.format(N=n, lista_numerada=lista_numerada)`. `__all__` con los 8 templates + ESCAPE_REGEX + interpolate_t04. Esto SÍ es implementación final, no stub — los strings son data lockeada.

    - `app/security/prompt_firewall.py`: `from dataclasses import dataclass`. `@dataclass class SanitizeResult: blocked: bool; reason: str = ""; cleaned: str = ""`. `def sanitize(text: str) -> SanitizeResult: raise NotImplementedError("Implemented in Plan 03-04")`. `INJECTION_PATTERNS: list[re.Pattern[str]] = []` (stub vacío, populated en 03-04). `__all__ = ["SanitizeResult", "sanitize", "INJECTION_PATTERNS"]`.

    - `app/security/judge.py`: `class JudgeRubric(BaseModel)` con los 8 bool flags + `rationale: str` EXACTAMENTE como en CONTEXT D-05 (esto es schema lockeado — no es stub, es contract). Funciones `def is_approved(rubric: JudgeRubric) -> bool: raise NotImplementedError(...)`. `async def judge_response(messages: list[Any], response: str) -> JudgeRubric | None: raise NotImplementedError(...)`. `__all__ = ["JudgeRubric", "is_approved", "judge_response"]`.

    - `app/security/kb_auditor.py`: `class KBAuditRubric(BaseModel)` con 6 bool flags + `rationale: str` + `risk_score: int` (per RESEARCH Pattern 7). `async def audit_kb(kb_path: str, redis: Any) -> int: raise NotImplementedError(...)`. `__all__ = ["KBAuditRubric", "audit_kb"]`.

    - `app/integrations/chatwoot.py`: `class ChatwootClient` con `__init__(self, http: httpx.AsyncClient, account_id: int)` (signature completa, body solo guarda los args) y 3 async method stubs (`get_or_create_conversation(phone: str) -> int`, `post_message(conversation_id: int, content: str, message_type: Literal["incoming", "outgoing"]) -> None`, `mark_resolved(conversation_id: int) -> None`) con `raise NotImplementedError(...)`. `@lru_cache(maxsize=1) def get_chatwoot_client() -> ChatwootClient: raise NotImplementedError(...)`. `__all__ = ["ChatwootClient", "get_chatwoot_client"]`.

    Sub-task 2d — `knowledge/dpg_cartera.md` (KB stub D-09):
    Crear el archivo con ~400 palabras (cap superior del rango 300-500 tokens, ~1.3 tokens/word inglés-español mix). 4 secciones exactas:
    - `## Coberturas generales` — describir cobertura típica de AUTOMÓVILES (responsabilidad civil, daños propios, asistencia 24/7) y ramos secundarios (VIDA, HOGAR). Placeholder pero realistic para DPG.
    - `## FAQs frecuentes` — 4-6 preguntas/respuestas tipo "¿Cómo verifico el estado de mi póliza?", "¿Qué hago si tuve un siniestro?", "¿Cómo modifico mi forma de pago?". Respuestas cortas.
    - `## Procedimientos de cartera` — proceso de pago, plazos de gracia, qué pasa si se vence (placeholder).
    - `## Horarios de atención` — Lunes a Viernes 8am-6pm hora Colombia, sábados 9am-1pm, número de cartera DPG (placeholder `+57 1 XXX XXXX`).

    NO incluir injection patterns, NO incluir hidden chars (zero-width, RTL override) — el stub debe ser benigno (auditor lo va a procesar y debe pasar con risk < 20).

    Sub-task 2e — `tests/conftest.py` extensión:
    Agregar al `_test_env` fixture autouse las 4 env vars con valores dummy:
    ```python
    os.environ.setdefault("CHATWOOT_URL", "https://chat-test.example.com")
    os.environ.setdefault("CHATWOOT_API_KEY", "cw-test-key")
    os.environ.setdefault("CHATWOOT_ACCOUNT_ID", "1")
    os.environ.setdefault("CHATWOOT_INBOX_ID", "2")
    ```

    Sub-task 2f — `.pre-commit-config.yaml` mypy hook:
    En el bloque del hook mypy, dentro de `additional_dependencies`, agregar (preservando orden alfabético + comentarios):
    ```
    - langgraph==1.2.6
    - langchain==1.3.11
    - langsmith==0.9.3
    ```
    (langchain-openai ya está; langgraph-checkpoint-postgres ya está si F1 lo agregó — verificar antes de duplicar.)
  </action>
  <verify>
    <automated>uv run python -c "from app.features.qa.state import QAState; from app.features.qa.graph import build_qa_graph; from app.features.qa.nodes import node_identify, node_choose_policy, node_answer, node_escalate, node_close; from app.features.qa.tools import get_saldo, get_estado, get_coberturas, escalate_to_human; from app.features.qa.prompts import system_prompt; from app.features.qa.knowledge_base import load_kb; from app.features.qa.messages import T_01, T_02, T_03, T_04, T_05, T_06, T_07, T_08, ESCAPE_REGEX, interpolate_t04; from app.security.prompt_firewall import sanitize, SanitizeResult; from app.security.judge import JudgeRubric, is_approved, judge_response; from app.security.kb_auditor import audit_kb, KBAuditRubric; from app.integrations.chatwoot import ChatwootClient, get_chatwoot_client" && test -f knowledge/dpg_cartera.md && python -c "import pathlib; w = len(pathlib.Path('knowledge/dpg_cartera.md').read_text().split()); assert 250 <= w <= 500, f'KB stub word count out of range: {w}'" && grep -q "CHATWOOT_API_KEY" tests/conftest.py && grep -q "langgraph==1.2.6" .pre-commit-config.yaml && uv run mypy --strict app/features/qa/ app/security/ app/integrations/chatwoot.py && uv run pytest -x</automated>
  </verify>
  <done>
    Todos los 14 archivos nuevos importables. QAState type-checks. Los 8 templates de D-16 son strings literales en `messages.py` (data lockeada, no stub). KB stub en rango 250-500 palabras con 4 secciones. conftest extiende env vars Chatwoot. mypy --strict pasa app/features/qa + app/security + integrations/chatwoot.py. pytest existente sigue verde (66 tests pre-F3).
  </done>
  <acceptance_criteria>
    - `python -c "from app.features.qa.messages import T_01; assert 'documento' in T_01"` exit 0 (template literal correcto)
    - `python -c "from app.features.qa.messages import ESCAPE_REGEX; assert ESCAPE_REGEX.search('quiero hablar con un humano')"` exit 0
    - `python -c "from app.security.judge import JudgeRubric; flags = [f for f, info in JudgeRubric.model_fields.items() if info.annotation is bool]; assert len(flags) == 8, f'Expected 8 bool flags, got {len(flags)}'"` exit 0
    - `grep -c "^## " knowledge/dpg_cartera.md` retorna 4
    - `wc -w knowledge/dpg_cartera.md` retorna número entre 250 y 500
    - `uv run mypy --strict app/` exit 0
    - `uv run pytest -x` exit 0 (pre-F3 baseline still green)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file → settings module | Operator-supplied env vars become process state; SecretStr renders ********** in repr/logs |
| knowledge/dpg_cartera.md → system prompt (future) | KB content injected into LLM system prompt via load_kb() in 03-05 — must be validated by kb_auditor in 03-04 |
| tests/conftest.py env vars → test runtime | Dummy values used in CI; if real CHATWOOT_API_KEY leaks into conftest accidentally, exposure via repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | ChatwootSettings.api_key in logs | mitigate | `SecretStr` per CLAUDE.md "Convenciones de código" — same pattern as WA_TOKEN, renders ********** automatically |
| T-03-01-02 | Tampering | KB stub `knowledge/dpg_cartera.md` containing injection patterns | mitigate | Stub written deliberately benign; kb_auditor in 03-04 must validate it passes with risk < 20 |
| T-03-01-03 | Information Disclosure | conftest.py committing real CHATWOOT_API_KEY value | mitigate | acceptance criteria forces `os.environ.setdefault("CHATWOOT_API_KEY", "cw-test-key")` — dummy string explicit; CI guard would catch a value matching JWT/UUID pattern |
| T-03-01-04 | Spoofing | Module skeletons importing langgraph/langchain create implicit dependency surface | accept | Versions pinned in pyproject.toml + pre-commit additional_dependencies — supply-chain controls per Phase 1 + 2 |
</threat_model>

<verification>
- 14 new files created + 4 modified (settings, models, conftest, pre-commit)
- All new modules importable without error
- KB stub passes word count + section structure check
- mypy --strict clean on entire app/
- pytest baseline (66 tests) still green — no regression
- D-16 templates are literal strings in messages.py, not stubs
</verification>

<success_criteria>
- Wave 2 plans (03-02, 03-03, 03-04) can `from app.config.settings import settings` and access `settings.chatwoot.*`
- Wave 2 plans can `from app.models.softseguros import ClienteRaw, EstadoCodigo, SaldoResponse, EstadoResponse, CoberturasResponse, PolizaSummary` without ImportError
- Wave 3 plan (03-05) can `from app.features.qa.state import QAState` and `from app.features.qa.messages import T_01, T_02, ..., T_08, ESCAPE_REGEX, interpolate_t04` — these are NOT stubs, they're data
- mypy contract is locked across the phase boundary
</success_criteria>

<output>
Crear `.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-01-SUMMARY.md` cuando done con: lista de 14 nuevos files + 4 modified, decisiones de narrowing aplicadas (ClienteRaw narrowed vs passthrough), word count + section list del KB stub, env vars añadidos a .env.example.
</output>
