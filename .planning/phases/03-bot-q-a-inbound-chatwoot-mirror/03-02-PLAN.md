---
phase: 03-bot-q-a-inbound-chatwoot-mirror
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/integrations/softseguros.py
  - app/models/softseguros.py
  - tests/test_softseguros_readonly.py
  - tests/test_integrations_softseguros.py
autonomous: true
requirements:
  - D-01   # identificación por documento (server-side requirement)
  - "READ-ONLY INVARIANT (CLAUDE.md)"
  - "RESEARCH Open Question #1 resolution (Plan 03-00 confirms shape)"

must_haves:
  truths:
    - "`SoftSegurosClient.get_clientes_by_documento(documento: str) -> ClienteRaw` existe y es async. Usa `self._cached_get(documento, 'clientes_by_doc', '<path-from-03-00-PROBE>', <param>=documento)` donde `<path>` y `<param>` vienen literal de 03-00-PROBE.md"
    - "Si 03-00-PROBE.md `DECISION=use_poliza_filter_fallback`, el método existe pero su path es `/api/poliza/` con query param `cliente_numero_documento`, retornando pólizas directamente (NO clientes); en ese caso, ClienteRaw narrowing es N/A y el método se renombra a `get_polizas_by_cliente_documento` con docstring explícita del fallback"
    - "`tests/test_softseguros_readonly.py` METHOD_ALLOWLIST contiene el nombre nuevo del método **en el MISMO commit** que agrega el método al cliente, sino el CI guard `test_softseguros_client_has_no_write_methods` falla porque el método nuevo contiene el verb 'get' (allowed) pero NO está en allowlist explícita — la convención del test es allowlist explícita por nombre exacto"
    - "Tests nuevos en `tests/test_integrations_softseguros.py` cubren: (a) cache hit con segunda call retorna mismo objeto sin httpx call upstream, (b) cache miss hace 1 GET upstream con path + query param correctos, (c) breaker open propaga `CircuitBreakerError` sin retry, (d) 404 upstream retorna respuesta vacía / lista vacía / raise — comportamiento documentado según el shape real del endpoint"
    - "`uv run pytest tests/test_softseguros_readonly.py tests/test_integrations_softseguros.py` pasa en verde"
    - "`uv run pytest` (suite completa) pasa — no regresión en los 66 tests existentes"
  artifacts:
    - path: app/integrations/softseguros.py
      provides: "get_clientes_by_documento method (READ-ONLY, cached, breaker-protected)"
      contains: "async def get_clientes_by_documento"
    - path: app/models/softseguros.py
      provides: "ClienteRaw narrowing finalized (or fallback decision documented)"
    - path: tests/test_softseguros_readonly.py
      provides: "METHOD_ALLOWLIST updated to include new method — CI guard passes"
    - path: tests/test_integrations_softseguros.py
      provides: "4 new test cases for get_clientes_by_documento"
  key_links:
    - from: app/integrations/softseguros.py get_clientes_by_documento
      to: app/features/qa/nodes.py node_identify (Plan 03-05)
      via: "client = get_softseguros_client(); polizas = await client.get_clientes_by_documento(doc)"
      pattern: "get_clientes_by_documento"
    - from: tests/test_softseguros_readonly.py METHOD_ALLOWLIST
      to: SoftSegurosClient method names
      via: "frozenset literal must list every public method"
      pattern: "METHOD_ALLOWLIST"

---

<objective>
Agregar UN solo método READ-ONLY a `SoftSegurosClient` para identificación por documento (D-01), usando el path y query param confirmados literalmente en `03-00-PROBE.md`. CI guard se actualiza en el MISMO commit para evitar build break.

Purpose: D-01 override (identificación por número de documento, no póliza) requiere este nuevo endpoint. El READ-ONLY invariant + CI guard test es la barrera de defensa que documenta el contract — agregar un método sin actualizar el allowlist rompe el build. Tests cubren cache + breaker + 404 path porque el método hereda de `_cached_get` (cache hit/miss verificable) y `_get` (tenacity outer + pybreaker inner).

Output: 1 método nuevo + allowlist update + 4 tests nuevos. Suite completa verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-CONTEXT.md
@.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-RESEARCH.md
@.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-PATTERNS.md
@.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md
@.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-01-SUMMARY.md
@app/integrations/softseguros.py
@app/models/softseguros.py
@tests/test_softseguros_readonly.py
@tests/test_integrations_softseguros.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agregar get_clientes_by_documento + actualizar CI guard + tests</name>
  <read_first>
    - app/integrations/softseguros.py (lines 196-260 — `_cached_get` + the 4 existing public methods, exact analog to copy. `get_cliente` at 240-246 is the closest 1:1 — same caching idiom; `get_pagos` at 252-260 shows the query-param pattern via `**params`)
    - app/models/softseguros.py (final state from Plan 03-01 — ClienteRaw narrowing decision)
    - tests/test_softseguros_readonly.py (lines 42-51 METHOD_ALLOWLIST + lines 54-75 test that fails on offenders)
    - tests/test_integrations_softseguros.py (lines 49-82 stub_http + stub_redis fixtures + _make_response helper)
    - .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md (exact path + query param + response shape — source of truth para los args del método)
    - CLAUDE.md §"Reglas críticas → Don't" (regla READ-ONLY y los 4 requisitos para agregar write — no aplican aquí porque es GET, pero confirmar que el verb in name is `get_` only)
  </read_first>
  <behavior>
    - Test 1 (cache hit): segundo call con mismo `documento` en <60s retorna mismo `ClienteRaw` sin nueva call upstream. `stub_http.get.call_count == 1` después de 2 invocaciones.
    - Test 2 (cache miss + correct path): primer call con documento `"12345678"` invoca `stub_http.get` exactamente 1 vez con path EXACTO confirmado en 03-00-PROBE.md y query param `<confirmed_name>=12345678`.
    - Test 3 (breaker open): si `softseguros_breaker._state` es OPEN, `await client.get_clientes_by_documento("x")` raises `pybreaker.CircuitBreakerError` sin retry (no httpx call). Verifica que el método hereda el `tenacity outer / pybreaker inner` invariant del `_get`.
    - Test 4 (CI guard updated): `from tests.test_softseguros_readonly import METHOD_ALLOWLIST; assert "get_clientes_by_documento" in METHOD_ALLOWLIST` (o el nombre que aplique si es fallback `get_polizas_by_cliente_documento`).
    - Test 5 (CI guard still passes overall): `test_softseguros_client_has_no_write_methods()` pasa sin offenders (el método nuevo está en allowlist).

    Behavior espec del método:
    - Async function on `SoftSegurosClient`.
    - Acepta `documento: str` (sin validación de formato — DRF backend valida).
    - Path y query param vienen LITERAL de 03-00-PROBE.md `PROBE_1_QUERY_PARAM` y la decisión.
    - Cache key: `softseguros:{documento}:clientes_by_doc` (TTL 60s, mismo namespace que F2 — no hay colisión porque `clientes_by_doc` es distinto query_type).
    - Si fallback path (`use_poliza_filter_fallback`), el método se llama `get_polizas_by_cliente_documento` y retorna `dict[str, Any]` con shape paginated de pólizas (no clientes).
    - Docstring referencia `READ-ONLY INVARIANT` del module docstring y nota explícita `# CI guard: name added to METHOD_ALLOWLIST in same commit (tests/test_softseguros_readonly.py)`.
  </behavior>
  <action>
    Sub-task 1a — Agregar método a `SoftSegurosClient`:
    Después del último método público (`get_pagos` en línea 260), agregar (asumiendo decision `use_listar_endpoint`; ajustar nombre/path según 03-00-PROBE.md):

    Signature: `async def get_clientes_by_documento(self, documento: str) -> ClienteRaw`.

    Docstring: bloque de 3-5 líneas que (a) menciona "GET /api/cliente/listar_cliente_por_documento/?<param>={documento}" usando el path literal del probe, (b) hace referencia al READ-ONLY INVARIANT del module docstring, (c) nota explícita "CI guard: name added to METHOD_ALLOWLIST in same commit", (d) si shape paginated nota el wrapping (`return self._cached_get(documento, "clientes_by_doc", "<path>", **{param_name: documento})`).

    Body: 1 line — return await self._cached_get(documento, "clientes_by_doc", "<path-literal>", **{param: documento}). Usa `**` para inyectar el query param con nombre dinámico-pero-locked (el nombre lo decide el probe, queda hardcoded en el método).

    Si 03-00-PROBE.md `DECISION=use_poliza_filter_fallback`:
    - Renombrar a `get_polizas_by_cliente_documento`.
    - Path: `/api/poliza/`.
    - Query param: `cliente_numero_documento`.
    - Docstring explica que el endpoint listar-clientes no funcionó/no existe en este sandbox, fallback a filter sobre poliza list.
    - El return type sigue siendo `dict[str, Any]` (paginated polizas response, no clientes) — actualizar `PolizaSummary` mapping en Plan 03-05 acordemente.

    Sub-task 1b — Actualizar `tests/test_softseguros_readonly.py`:
    Agregar `"get_clientes_by_documento"` (o nombre fallback) al `METHOD_ALLOWLIST` frozenset (lines 42-51). Mantener orden alfabético si existe.

    Sub-task 1c — Tests nuevos en `tests/test_integrations_softseguros.py`:
    Reusar el patrón de fixtures `stub_http`, `stub_redis`, `_make_response` ya existentes (lines 49-82). 4 nuevos test cases:

    ```
    async def test_get_clientes_by_documento_cache_miss_calls_upstream
    async def test_get_clientes_by_documento_cache_hit_skips_upstream
    async def test_get_clientes_by_documento_breaker_open_raises_without_retry
    def test_allowlist_includes_new_method
    ```

    Patrones a copiar:
    - Para cache hit/miss: el patrón del test análogo para `get_poliza` (buscar en el archivo `cache_hit`/`cache_miss` keywords).
    - Para breaker: mock `softseguros_breaker._state` o usar `softseguros_breaker.open()` directo antes del call.
    - Para allowlist: trivially `from tests.test_softseguros_readonly import METHOD_ALLOWLIST; assert "<method_name>" in METHOD_ALLOWLIST`.

    Sub-task 1d (opcional, condicional): si 03-00-PROBE.md devolvió `PROBE_1_RESULTS_ITEM_KEYS_SAMPLE` con campos confirmados, narrow `ClienteRaw` a `TypedDict` en `app/models/softseguros.py` (Plan 03-01 dejó passthrough con TODO). Agregar TypedDict con los campos confirmados. Tests siguen pasando porque `ClienteRaw` extends dict — pero mypy ahora type-checks usage downstream.
  </action>
  <verify>
    <automated>uv run pytest tests/test_softseguros_readonly.py tests/test_integrations_softseguros.py -v && uv run pytest -x && uv run mypy --strict app/integrations/softseguros.py app/models/softseguros.py && grep -q "get_clientes_by_documento\|get_polizas_by_cliente_documento" app/integrations/softseguros.py && grep -q "get_clientes_by_documento\|get_polizas_by_cliente_documento" tests/test_softseguros_readonly.py</automated>
  </verify>
  <done>
    Método nuevo existe, CI guard updated, 4 tests nuevos verdes, suite completa verde, mypy strict clean. ClienteRaw narrowed si shape confirmado.
  </done>
  <acceptance_criteria>
    - `uv run pytest tests/test_softseguros_readonly.py -v` exit 0 (CI guard passes)
    - `uv run pytest tests/test_integrations_softseguros.py -k "get_clientes\|get_polizas" -v` retorna >= 4 passed
    - `python -c "from app.integrations.softseguros import SoftSegurosClient; assert hasattr(SoftSegurosClient, 'get_clientes_by_documento') or hasattr(SoftSegurosClient, 'get_polizas_by_cliente_documento')"` exit 0
    - `uv run pytest --tb=short` exit 0 (suite completa green, no regression)
    - `uv run mypy --strict app/integrations/softseguros.py` exit 0
    - `grep -E "(get_clientes_by_documento|get_polizas_by_cliente_documento)" tests/test_softseguros_readonly.py` matches in METHOD_ALLOWLIST literal
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LangGraph node (03-05) → SoftSegurosClient.get_clientes_by_documento | Documento string from user input crosses here; DRF backend validates format (length, charset). No SQL-injection vector — DRF query param is safe by construction. |
| SoftSeguros API → response JSON | Untrusted JSON from upstream; passthrough dict[str, Any] until LLM consumption (tools allowlist in 03-05 sanitizes before sending to LLM) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | New write method bypassing READ-ONLY invariant | mitigate | CI guard `test_softseguros_client_has_no_write_methods` enforces by introspection; new method name contains only `get_` prefix and is added to METHOD_ALLOWLIST in same commit (sub-task 1b) |
| T-03-02-02 | Information Disclosure | Documento value leaked in structured logs | mitigate | Cache key uses documento literal but inside encoded bytes — logs use `error_type=type(exc).__name__` pattern only (BLE001 idiom from F2); add `documento_hash=hashlib.sha256(documento.encode())[:8]` if 03-05 nodes log this method's invocation |
| T-03-02-03 | Denial of Service | Brute-force enumeration of documentos via this endpoint | accept | F5 adds rate limiting per phone — F3 relies on Meta allowlist (only authorized numbers can hit the bot at all). Plus `doc_retries=1` cap per D-03 prevents per-conversation enumeration. |
| T-03-02-04 | Elevation of Privilege | Caller using this method to enumerate polizas of arbitrary clients | accept | Endpoint requires DRF token auth (already enforced by `_get_token` from F2). DPG tenant scope is server-side. The bot itself never exposes this method to LLM tools (tools in 03-05 receive `poliza_id` via InjectedState only). |
</threat_model>

<verification>
- Método existe con signature `(documento: str) -> ClienteRaw` (o variante fallback)
- METHOD_ALLOWLIST en CI guard contains new method name
- 4 tests nuevos green
- Suite completa pre-F3 sigue green (no regression on 66 tests)
- mypy strict clean
</verification>

<success_criteria>
- Plan 03-05 `node_identify` puede invocar `await client.get_clientes_by_documento(state["cliente_doc"])` y recibir ClienteRaw shape lista para iterar pólizas
- CI build no rompe por READ-ONLY guard
- Cache 60s heredado de `_cached_get` aplica automáticamente
- Breaker open propaga sin retry (mitigation T-LLM06 via T-06 template — Plan 03-05 catchea `CircuitBreakerError` en el node y devuelve T-06)
</success_criteria>

<output>
Crear `.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-02-SUMMARY.md` cuando done, sumarizando: método final name + path + query param literal, decision tree (listar endpoint vs fallback), ClienteRaw narrowing applied o no, tests verdes.
</output>
