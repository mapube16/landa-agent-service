---
phase: 03-bot-q-a-inbound-chatwoot-mirror
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md
autonomous: false
requirements:
  - D-01   # identificación por documento — endpoint shape blocker
  - D-07   # judge Gemini Flash structured output — feasibility probe
  - "Claude's Discretion (Chatwoot inbox API Channel)"
  - "RESEARCH Open Question #1 (listar_cliente_por_documento shape)"
  - "RESEARCH Open Question #2 (Chatwoot status en Railway)"
  - "RESEARCH Open Question #3 (Gemini Flash via OpenRouter structured output)"

must_haves:
  truths:
    - "03-00-PROBE.md existe en .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/ con findings sanitizados (sin tokens, sin PII)"
    - "Query-param name confirmado para /api/cliente/listar_cliente_por_documento/ (`?documento=` vs `?numero_documento=` vs otro) o fallback decision documentada (use GET /api/poliza/?cliente_numero_documento=X)"
    - "Response shape confirmado: paginated `{count, results: [...]}` vs single object vs other, incluyendo si pólizas vienen embebidas o requieren second call"
    - "Chatwoot UI accesible en chat.landatech.org (login admin operacional)"
    - "Inbox tipo 'API Channel' creado en Chatwoot UI, no es el inbox WhatsApp nativo de F4"
    - "Env vars Chatwoot recogidos: CHATWOOT_URL, CHATWOOT_API_KEY, CHATWOOT_ACCOUNT_ID, CHATWOOT_INBOX_ID"
    - "Probe de `with_structured_output(SimplePydanticModel)` contra `google/gemini-2.5-flash` via OpenRouter retorna instancia parseada (no None, no error) — confirma feasibility de judge + kb_auditor"
    - "Fallback documentado si Gemini Flash structured output falla: usar prompt-based parser con JsonOutputParser de langchain (mayor latencia aceptable F3)"
  artifacts:
    - path: .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md
      provides: "Findings literales del probe: endpoint shape + Chatwoot setup + Gemini Flash structured output feasibility"
      contains: "ENDPOINT_SHAPE_CONFIRMED + CHATWOOT_INBOX_CREATED + GEMINI_FLASH_STRUCTURED_OK"
  key_links:
    - from: 03-00-PROBE.md
      to: 03-01-PLAN.md (ClienteRaw narrowing) + 03-02-PLAN.md (get_clientes_by_documento path/param) + 03-04-PLAN.md (judge structured output strategy)
      via: "literal findings inform Pydantic model fields + HTTP method signatures"
      pattern: "query_param_name|response_shape|gemini_flash_structured"

---

<objective>
Cerrar las 3 Open Questions de RESEARCH.md (#1 listar_cliente shape, #2 Chatwoot operativo, #3 Gemini Flash structured output) **antes** de modelar Pydantic schemas o escribir código. Sin estos findings literales, el `ClienteRaw` schema (03-01), el método `get_clientes_by_documento` (03-02), y la estrategia del judge (03-04) son guesswork.

Purpose: Hardware-reality check. La probe del endpoint cuesta 5 min al operador y elimina horas de rebote si el shape difiere de lo asumido. Crear el inbox Chatwoot del lado UI es operator-only (no hay API de admin para crear inboxes). El probe de Gemini Flash structured output via OpenRouter confirma que el judge + kb_auditor pipeline van a funcionar — alternativa fallback documentada si falla.

Output: `03-00-PROBE.md` con outputs sanitizados (sin tokens, sin PII real DPG) que Plans 03-01..03-04 consumen como source of truth.

Implementa D-01 (identificación por documento — confirmar shape) y D-07 (judge model feasibility).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-CONTEXT.md
@.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-RESEARCH.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/SOFTSEGUROS_API_NOTES.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1 (HUMAN): Probe `/api/cliente/listar_cliente_por_documento/` contra sandbox DPG</name>
  <read_first>
    - .planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/SOFTSEGUROS_API_NOTES.md (schema de cliente endpoint, 122 campos del shape /api/cliente/{id}/)
    - .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-RESEARCH.md §"Open Questions" #1 + §"Environment Availability"
    - app/integrations/softseguros.py (auth pattern `_get_token` + token DRF format)
    - CLAUDE.md (línea referenciando "listar_cliente_por_documento" como fallback de UX)
  </read_first>
  <what-built>
    Endpoint mencionado en CLAUDE.md pero NUNCA probado en F2. F3 lo necesita confirmado para modelar `ClienteRaw` Pydantic (Plan 03-01) y `get_clientes_by_documento` (Plan 03-02). Claude no tiene creds DPG directas — el operador (Maxi) las usa contra sandbox.
  </what-built>
  <how-to-verify>
    Operador ejecuta los 3 probes siguientes en orden y pega outputs sanitizados (sin token, sin PII real — usar documento de prueba o redactar campos personales con `<redacted>`):

    1. **Probe principal — query param `?documento=`** (hipótesis más probable):
       ```bash
       # 1.1. Obtener token DRF
       TOKEN=$(curl -s -X POST https://app.softseguros.com/api-token-auth/ \
         -H "Content-Type: application/json" \
         -d '{"username":"<DPG_USERNAME>","password":"<DPG_PASSWORD>"}' \
         | jq -r .token)

       # 1.2. Probar el endpoint con un documento real DPG
       curl -s -X GET "https://app.softseguros.com/api/cliente/listar_cliente_por_documento/?documento=<DOCUMENTO_REAL>" \
         -H "Authorization: Token $TOKEN" \
         | jq .
       ```
       Capturar status HTTP + shape (paginated `{count, next, previous, results: [...]}` vs single object vs lista directa).

    2. **Si Probe 1 retorna 400/404 con error sobre param name, intentar variantes**:
       - `?numero_documento=<DOCUMENTO>`
       - `?nro_documento=<DOCUMENTO>`
       - `?cedula=<DOCUMENTO>`
       - `?documento_cliente=<DOCUMENTO>`

       Capturar cuál (si alguno) retorna 200.

    3. **Fallback probe — si NINGUNA variante funciona, probar el filter sobre poliza list**:
       ```bash
       curl -s -X GET "https://app.softseguros.com/api/poliza/?cliente_numero_documento=<DOCUMENTO_REAL>" \
         -H "Authorization: Token $TOKEN" \
         | jq .
       ```
       Documentar si retorna pólizas filtradas directamente (skips el cliente intermediate step).

    Output esperado del operador (sanitizado, formato literal en chat — Claude lo escribirá a 03-00-PROBE.md):

    ```
    PROBE_1_STATUS=<200|400|404|other>
    PROBE_1_QUERY_PARAM=<documento|numero_documento|nro_documento|cedula|documento_cliente|none>
    PROBE_1_RESPONSE_SHAPE=<paginated|single_object|list|other>
    PROBE_1_RESPONSE_KEYS_TOP_LEVEL=<lista de keys top-level, e.g. "count,next,previous,results">
    PROBE_1_RESULTS_ITEM_KEYS_SAMPLE=<10-15 keys del primer objeto en results — sirve para narrowing ClienteRaw>
    PROBE_1_HAS_EMBEDDED_POLIZAS=<true|false> # ¿el cliente trae polizas[] embebidas o requiere call separada?

    FALLBACK_PROBE_STATUS=<200|other>
    FALLBACK_PROBE_PATH=/api/poliza/?cliente_numero_documento=...
    FALLBACK_PROBE_RESPONSE_SHAPE=<paginated polizas|other>

    DECISION=<use_listar_endpoint|use_poliza_filter_fallback>
    confirm: probe done
    ```
  </how-to-verify>
  <resume-signal>Operador pega el bloque `PROBE_1_*` + `DECISION=` + `confirm: probe done`. Si operador escribe `aborted` (DPG no respondió, creds caducaron, etc.), Claude se detiene y reporta. NO usar workflow.auto_advance — checkpoint blocking-human.</resume-signal>
  <acceptance_criteria>
    - Operator response contiene `PROBE_1_STATUS=`, `PROBE_1_QUERY_PARAM=`, `PROBE_1_RESPONSE_SHAPE=`, `DECISION=`, y `confirm: probe done`.
    - Si `DECISION=use_listar_endpoint`, los campos `PROBE_1_RESULTS_ITEM_KEYS_SAMPLE` y `PROBE_1_HAS_EMBEDDED_POLIZAS` están presentes.
    - Si `DECISION=use_poliza_filter_fallback`, `FALLBACK_PROBE_STATUS=200` y `FALLBACK_PROBE_RESPONSE_SHAPE` están presentes.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 (HUMAN): Verificar Chatwoot up + crear inbox "API Channel" + recoger env vars</name>
  <read_first>
    - .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-RESEARCH.md §"Pattern 5: Chatwoot Application API" + §"Pitfall 4: Chatwoot inbox WhatsApp vs API Channel"
    - .planning/phases/01-setup-infra/01-04-SUMMARY.md (Chatwoot self-hosted deployment notes — buscar credenciales admin si están documentadas)
  </read_first>
  <what-built>
    Chatwoot desplegado en F1 a chat.landatech.org pero **idle** (sin tráfico, posiblemente sin admin login validado). F3 necesita: (a) admin login funciona, (b) inbox API Channel dedicado para mirror (separado del WhatsApp inbox que F4 va a crear), (c) `api_access_token` user-level del operador para autenticación de Application API.
  </what-built>
  <how-to-verify>
    Operador ejecuta en orden:

    1. **Login admin Chatwoot** — abrir https://chat.landatech.org en browser. Si nunca se hizo login antes, usar Setup Wizard. Documentar email admin.

    2. **Crear inbox "API Channel"** — Settings → Inboxes → New Inbox → "API" channel type:
       - Name: `landa-agent-mirror`
       - Webhook URL: **dejar en blanco** (mirror es one-way, no recibimos callbacks de Chatwoot en F3 — F4 los wirea para escalación bidireccional)
       - Save → Chatwoot retorna `inbox_id` (visible en URL o settings)

    3. **Obtener `api_access_token` del user operador**:
       - Profile (top-right avatar) → Profile Settings → Access Token → Show / Copy
       - Este es el token user-level usado en header `api_access_token` (NO Bearer)

    4. **Identificar `account_id`**:
       - Visible en URL: `chat.landatech.org/app/accounts/{account_id}/...` (típicamente `1` en single-tenant Chatwoot)

    5. **Smoke API call** — confirmar credenciales funcionan:
       ```bash
       curl -s -X GET "https://chat.landatech.org/api/v1/accounts/<ACCOUNT_ID>/inboxes" \
         -H "api_access_token: <API_KEY>" | jq '.payload[] | {id, name, channel_type}'
       ```
       Debería listar al menos el inbox `landa-agent-mirror` recién creado con `channel_type` igual a `"Channel::Api"`.

    Output esperado del operador (sanitizado — Claude lo escribirá a `03-00-PROBE.md`):

    ```
    CHATWOOT_URL=https://chat.landatech.org
    CHATWOOT_ADMIN_LOGIN=<email>
    CHATWOOT_ACCOUNT_ID=<int>
    CHATWOOT_INBOX_ID=<int>
    CHATWOOT_INBOX_NAME=landa-agent-mirror
    CHATWOOT_INBOX_CHANNEL_TYPE=Channel::Api
    CHATWOOT_API_KEY=<paste real — quedará en chat transcript para Railway setup, NO se commitea a git>
    SMOKE_API_CALL_STATUS=<200|other>
    confirm: chatwoot ready
    ```
  </how-to-verify>
  <resume-signal>Operador pega el bloque `CHATWOOT_*` + `SMOKE_API_CALL_STATUS=200` + `confirm: chatwoot ready`. Si chat.landatech.org no responde, operador escribe `aborted: chatwoot down` y Claude reporta blocker (operator debe redeploy stack Chatwoot antes de seguir).</resume-signal>
  <acceptance_criteria>
    - Operator response contiene todos los `CHATWOOT_*` fields y `SMOKE_API_CALL_STATUS=200`.
    - `CHATWOOT_INBOX_CHANNEL_TYPE=Channel::Api` (NO `Channel::Whatsapp` — ése es F4).
    - `CHATWOOT_API_KEY` está en transcript (Claude lo usará en Plan 03-06 para `railway variable set`; NO se commitea a .env.example ni a docs).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Probe Gemini Flash structured output via OpenRouter</name>
  <read_first>
    - app/config/llm.py (get_llm factory + ROLE_ALIASES)
    - app/integrations/openrouter.py (ChatOpenAI construction, base_url, headers)
    - .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-RESEARCH.md §"Pattern 3: with_structured_output" + §"Open Questions" #3
  </read_first>
  <action>
    Crear archivo temporal `scripts/probe_gemini_flash_structured.py` (NO se commitea — se borra después de capturar findings) con un script que:

    1. Importa `get_llm` de `app.integrations.openrouter`.
    2. Define un Pydantic v2 BaseModel mínimo (3 campos: `is_safe: bool`, `confidence: int`, `reason: str`) — equivalente reducido del `JudgeRubric` real.
    3. Crea `judge_llm = get_llm("judge").with_structured_output(SimpleRubric)`.
    4. Ejecuta `result = await judge_llm.ainvoke("Evalúa esta respuesta: 'hola, mucho gusto'")`.
    5. Print `type(result).__name__`, `result.model_dump() if result else None`, y `time elapsed`.

    Correr el script (`uv run python scripts/probe_gemini_flash_structured.py`) con las env vars de dev (.env). Capturar output literal.

    Si `result` es instancia Pydantic parseada → Gemini Flash structured output funciona vía OpenRouter, judge pipeline en 03-04 procede como diseñado.

    Si `result` es `None` o lanza error → documentar fallback: en 03-04 usar `JsonOutputParser` de langchain con prompt-based parsing en vez de `with_structured_output`. NO bloquear Wave 1 — la decisión queda escrita en 03-00-PROBE.md.

    Borrar `scripts/probe_gemini_flash_structured.py` después de capturar (no contaminar repo con probes one-shot).
  </action>
  <verify>
    <automated>uv run python -c "import importlib; importlib.import_module('app.integrations.openrouter')" && test -f .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md && grep -E "GEMINI_FLASH_STRUCTURED_OK=(true|false)" .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md</automated>
  </verify>
  <done>
    `03-00-PROBE.md` contiene una línea `GEMINI_FLASH_STRUCTURED_OK=true` (si result es Pydantic instance) o `GEMINI_FLASH_STRUCTURED_OK=false` (si None/error) + `FALLBACK_STRATEGY=<JsonOutputParser|none_needed>`. Script temporal `scripts/probe_gemini_flash_structured.py` NO existe en el repo (borrado tras probe).
  </done>
  <acceptance_criteria>
    - `grep -E "^GEMINI_FLASH_STRUCTURED_OK=(true|false)$" 03-00-PROBE.md` retorna match
    - `grep -E "^FALLBACK_STRATEGY=" 03-00-PROBE.md` retorna match
    - `test ! -f scripts/probe_gemini_flash_structured.py` exit 0 (script borrado)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Consolidar findings en 03-00-PROBE.md</name>
  <read_first>
    - Outputs literales de Tasks 1-3 capturados en chat (Tasks 1+2 del operador, Task 3 del script)
    - .planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-04-SUMMARY.md (formato de SMOKE file de referencia para consistencia)
  </read_first>
  <action>
    Crear `.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md` con secciones:

    1. **Header** — phase, plan, fecha, status (Ready for downstream consumption).

    2. **Section "SoftSeguros listar_cliente_por_documento Probe"** — copia literal del bloque `PROBE_1_*` + `FALLBACK_*` + `DECISION=` del operador (Task 1). Si `DECISION=use_listar_endpoint`, agregar sub-sección con `Inferred ClienteRaw fields` que enumera los 10-15 keys que el operador reportó en `PROBE_1_RESULTS_ITEM_KEYS_SAMPLE` — Plan 03-01 usa esto para narrowing del Pydantic model.

    3. **Section "Chatwoot Setup"** — copia literal del bloque `CHATWOOT_*` (Task 2) **MENOS `CHATWOOT_API_KEY`** (que queda solo en transcript del chat para `railway variable set` en 03-06; NO se commitea). Documentar inbox_id, account_id, name, channel_type.

    4. **Section "Gemini Flash Structured Output Probe"** — output literal del script (Task 3) + decisión `GEMINI_FLASH_STRUCTURED_OK` + `FALLBACK_STRATEGY`.

    5. **Section "Downstream Impact"** — bullet list mapping findings a planes:
       - "Plan 03-01: `ClienteRaw = dict[str, Any]` se narrow a `class ClienteRaw(TypedDict)` con campos {lista de Task 1}, o se deja como `dict[str, Any]` si fallback (polizas via poliza filter)"
       - "Plan 03-02: `get_clientes_by_documento` usa path `/api/cliente/listar_cliente_por_documento/` + query param `<confirmed_name>` (o fallback path si `DECISION=use_poliza_filter_fallback`)"
       - "Plan 03-03: ChatwootSettings(env_prefix='CHATWOOT_') con account_id={...}, inbox_id={...}"
       - "Plan 03-04: judge usa `with_structured_output(JudgeRubric)` (si OK=true) o `JsonOutputParser` con prompt template (si OK=false)"

    Sanitización obligatoria:
    - NO incluir `CHATWOOT_API_KEY`, `SOFTSEGUROS_PASSWORD`, ni token raw.
    - NO incluir documento real, número de póliza real, ni nombre real de cliente DPG. Reemplazar con `<redacted>` si aparecen en outputs.

    Verificar con `grep` que el archivo NO contiene patterns sensibles:
    ```
    grep -E "(api_key|password|token)=[A-Za-z0-9]" 03-00-PROBE.md  # debe ser empty
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md && grep -c "PROBE_1_STATUS=" .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md | grep -E "^[1-9]" && grep -c "CHATWOOT_INBOX_ID=" .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md | grep -E "^[1-9]" && grep -c "GEMINI_FLASH_STRUCTURED_OK=" .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md | grep -E "^[1-9]" && ! grep -qE "(CHATWOOT_API_KEY|SOFTSEGUROS_PASSWORD)=[A-Za-z0-9]+" .planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-PROBE.md</automated>
  </verify>
  <done>
    `03-00-PROBE.md` existe, contiene las 5 secciones requeridas, NO contiene secrets en plain text, y `git diff` muestra solo este archivo añadido (no commits accidentales de scripts temporales).
  </done>
  <acceptance_criteria>
    - File existe en path canonical
    - Contiene secciones (heading match): "## SoftSeguros listar_cliente_por_documento Probe", "## Chatwoot Setup", "## Gemini Flash Structured Output Probe", "## Downstream Impact"
    - `grep -cE "(CHATWOOT_API_KEY|SOFTSEGUROS_PASSWORD|Token [A-Z][a-z]{20})" 03-00-PROBE.md` retorna 0
    - `git status` muestra el archivo como new file, no muestra `scripts/probe_*` (probe script borrado en Task 3)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → chat transcript | Tokens y credenciales DPG / Chatwoot cruzan vía pegada manual; transcript no es commit-safe pero queda en runtime memory |
| local probe script → OpenRouter | Llamada LLM con prompt mínimo de prueba; sin PII; consume costo desestimable |
| local probe → SoftSeguros sandbox | Authenticated GET con documento de prueba; READ-ONLY (cumple invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00-01 | Information Disclosure | 03-00-PROBE.md commit | mitigate | Task 4 sanitización obligatoria + grep guard que falla si encuentra patterns sensibles (`CHATWOOT_API_KEY=<plaintext>`, `Token <hex>`, etc.) |
| T-03-00-02 | Information Disclosure | scripts/probe_gemini_flash_structured.py committed accidentally | mitigate | Task 3 explícitamente borra el script tras probe; Task 4 acceptance verifica `test ! -f scripts/probe_*` |
| T-03-00-03 | Tampering | Probe contra prod SoftSeguros con documento real | accept | Operator usa documento de prueba si tiene; si usa real, redacta antes de pegar. READ-ONLY GET cumple invariant — no muta data |
| T-03-00-04 | Spoofing | Token DPG capturado en transcript reutilizado | accept | Token DRF de SoftSeguros queda en transcript del operator; revocación manual si compromise sospechado. F5 agrega audit log de credential use. |
</threat_model>

<verification>
- 03-00-PROBE.md existe y contiene las 5 secciones esperadas
- Findings literales de los 3 probes presentes (sin secrets en plain text)
- `git status` clean salvo por 03-00-PROBE.md added
- Operador confirmó ambos checkpoints blocking-human
</verification>

<success_criteria>
- Plans 03-01, 03-02, 03-03, 03-04 pueden referenciar 03-00-PROBE.md y obtener: query param exacto del endpoint listar-clientes, ClienteRaw field set, Chatwoot inbox_id/account_id, judge structured output strategy
- Sin esta probe, Plan 03-01 modela `ClienteRaw = dict[str, Any]` (passthrough) por safety — pero ese path bloquea cualquier validación estricta downstream
</success_criteria>

<output>
Crear `.planning/phases/03-bot-q-a-inbound-chatwoot-mirror/03-00-SUMMARY.md` cuando done, sumarizando: tres probes ejecutadas, decisiones lockeadas para Wave 1+, blockers resueltos.
</output>
