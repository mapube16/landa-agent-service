---
phase: 02-integraci-n-softseguros-whatsapp-cloud-api
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - .planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-SMOKE.md
  - .planning/phases/01-setup-infra/RAILWAY_AGENT_NOTES.md
autonomous: false
requirements:
  - REQ-2.1   # Q&A inbound — verificación de /test/poliza/{poliza_real_dpg}
  - REQ-2.2   # SoftSeguros tiempo real — verificación cache hit en segunda call
  - REQ-2.3   # HMAC X-Hub-Signature-256 — verificación con Meta enviando webhook real
  - REQ-2.4   # Idempotencia — verificación que reentrega de Meta NO duplica echo
estimated_complexity: S

must_haves:
  truths:
    - "WA_TOKEN rotado en Meta dashboard (token capturado en CONTEXT D-06 quedó en transcript) y reemplazado en Railway antes del wire-up final"
    - "Railway env vars seteadas en agent-service: WA_TOKEN (nuevo), WA_PHONE_ID=1267241483129092, WA_BUSINESS_ACCOUNT_ID=1451322196454283, WA_WEBHOOK_SECRET=e556d909fcb2d4dd3e573a28eafccda7, WA_VERIFY_TOKEN=96715a9c2658c915544f2faf735b98c0, WA_ECHO_ALLOWLIST=+<números operator>, SOFTSEGUROS_BASE_URL=https://app.softseguros.com/, SOFTSEGUROS_USERNAME=<DPG>, SOFTSEGUROS_PASSWORD=<DPG>, LANGSMITH_WORKSPACE_ID=<opcional>"
    - "Meta webhook suscrito a https://landa-agent-service-production.up.railway.app/webhooks/meta con eventos 'messages' + 'message_status' (D-04, D-05). El GET challenge devolvió 200 + hub.challenge — Meta dashboard muestra 'Verified'"
    - "Operador envió WhatsApp 'hola test' desde un número en WA_ECHO_ALLOWLIST al +16415416615 → recibió back 'echo: hola test' en < 5s. Logs de Railway muestran: webhook.hmac.ok → webhook.echo.sent con result=echo_sent + outbound_wamid + phone_hash (no raw phone)"
    - "Operador envió 2da vez el MISMO message_id (simulado con Meta webhook tester o re-enviando manualmente — Meta puede reentregar real) → segundo POST retorna 200 + log result=ignored_duplicate; NO se envía echo doble"
    - "Operador envió WhatsApp desde un número NO en allowlist → log result=ignored_not_allowlisted; el número NO recibe echo (verificado mirando el WhatsApp del número del operador no-allowlist)"
    - "curl https://landa-agent-service-production.up.railway.app/test/poliza/{poliza_real_dpg} retorna 200 + JSON crudo con shape DPG (saldo, estado, etc.); latency_ms < 3000"
    - "Segunda call a misma /test/poliza/{poliza_id} dentro de 60s retorna 200 + same JSON + latency_ms < 100 (cache hit confirmado — sin call upstream a DPG)"
    - ".planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-SMOKE.md documenta cada verificación con timestamp, comando ejecutado, output redacted (sin tokens, sin PII de pólizas), pass/fail, y la captura sanitizada de Meta dashboard (verified status)"
    - "RAILWAY_AGENT_NOTES.md actualizado con nota: 'worker count = 1 por convención v1 (pybreaker per-process state — RESEARCH Pitfall 6). Distributed breaker state migrará a CircuitRedisStorage en F5 cuando horizontal scaling sea necesario.'"
  artifacts:
    - path: .planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-SMOKE.md
      provides: "Smoke E2E verification report con 6 checkpoints documentados y sus outputs sanitizados"
    - path: .planning/phases/01-setup-infra/RAILWAY_AGENT_NOTES.md
      provides: "Nota appended sobre worker count = 1 + LANGSMITH_WORKSPACE_ID en Railway (Phase 1 follow-up cerrado)"
  key_links:
    - from: Meta dashboard
      to: Railway public URL
      via: "webhook subscription operator-configured"
      pattern: "landa-agent-service-production\\.up\\.railway\\.app/webhooks/meta"
    - from: Railway env vars
      to: app.state.meta + app.state.softseguros
      via: "lifespan startup reads settings.whatsapp.* + settings.softseguros.*"
      pattern: "WA_|SOFTSEGUROS_"
---

<objective>
Cerrar F2 con verificación E2E **operator-driven** contra Railway live: rotar token, setear env vars finales, suscribir webhook en Meta, enviar WhatsApp real desde número en allowlist, verificar echo recibido + idempotencia + allowlist-filter; hacer `/test/poliza/{real_dpg_poliza}` con creds DPG; documentar todo en `02-SMOKE.md`. **Sin esto, F2 NO está done aunque el código pase tests offline.**

Purpose: Tests unitarios offline (Plans 02-02 y 02-03) prueban architecture; este plan prueba realidad. Dos checkpoints humanos críticos: (1) rotación de token expuesto en transcript (D-06) y (2) verificación visual del round-trip en WhatsApp del operador.

Output: `02-SMOKE.md` verde + nota appended a `RAILWAY_AGENT_NOTES.md` sobre worker count y LANGSMITH_WORKSPACE_ID. Si todo verde, F2 cierra y F3 puede arrancar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-CONTEXT.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-02-SUMMARY.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-03-SUMMARY.md
@.planning/phases/01-setup-infra/01-05-SUMMARY.md
@.planning/phases/01-setup-infra/RAILWAY_AGENT_NOTES.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1 (HUMAN): Rotar WA_TOKEN expuesto + recoger SOFTSEGUROS_USERNAME / PASSWORD + WA_ECHO_ALLOWLIST + LANGSMITH_WORKSPACE_ID + 1 poliza_id real de DPG</name>
  <what-built>
    El operador (Maxi) tiene control sobre la cuenta Meta (`landa-messaging` app, App ID `1909364299769910`), la cuenta SoftSeguros (DPG), y Railway. Claude no puede rotar el token, ni acceder a SoftSeguros con creds DPG, ni a Meta dashboard. Estos pasos requieren acción humana antes del wire-up final.
  </what-built>
  <how-to-verify>
    Operador ejecuta (en orden) y pega al ejecutor en chat **un único mensaje** con todos los outputs cuando termine:

    1. **Rotar `WA_TOKEN`** (CONTEXT D-06 dice: token capturado en transcript del 2026-06-28 → debe rotarse antes del wire-up final):
       - Ir a Meta Business Suite → System Users → `landa-messaging-system-user` (o equivalente).
       - Revocar el token viejo (`EAAbIjoT...kAZDZD`).
       - Generar nuevo token con scopes: `whatsapp_business_messaging`, `whatsapp_business_management`, `whatsapp_business_manage_events`.
       - Expiration: `Never`.
       - Copiar el nuevo token (single-show).

    2. **Recoger `SOFTSEGUROS_USERNAME` y `SOFTSEGUROS_PASSWORD`** (CONTEXT D-01 pending operator):
       - Usuario DRF de DPG en SoftSeguros (mismas creds que lambda-proyect/cobranza usa, si es posible).
       - Si no hay un user dedicado para LANDA todavía, crear uno con read-only access (DPG admin lo puede hacer).

    3. **Decidir `WA_ECHO_ALLOWLIST`** (CONTEXT D-02 pending operator):
       - Lista de 1-3 números E.164 con `+` prefix (formato: `+5491134567890`).
       - Sugerencia: tu número personal + 1-2 números de testing internos LANDA.
       - **NO incluir números de clientes reales DPG** — el echo es ruidoso.

    4. **Recoger `LANGSMITH_WORKSPACE_ID`** (RESEARCH Pitfall 9 — Phase 1 follow-up):
       - smith.langchain.com → Settings → Workspaces → tu workspace activo → copiar el UUID.
       - Si no aplica (single workspace por default), pegar `none` y se omite.

    5. **Recoger 1 `poliza_id` real de DPG** para el smoke `/test/poliza/{id}`:
       - Un número de póliza activo en DPG (cualquiera; idealmente uno que el operador tenga acceso a verificar).
       - Sirve para confirmar end-to-end que el cliente SoftSeguros funciona contra prod-real.

    Output esperado del operador (formato exacto, **redacted** — la línea `WA_TOKEN=` SE QUEDA en transcript del chat para que Claude ejecute `railway variable set`, pero NO se commitea a git):

    ```
    WA_TOKEN=EAAbI...nuevo-token-rotado...XYZ
    SOFTSEGUROS_USERNAME=dpg_landa_readonly
    SOFTSEGUROS_PASSWORD=<paste real>
    WA_ECHO_ALLOWLIST=+5491134567890,+15555550100
    LANGSMITH_WORKSPACE_ID=<uuid> | none
    DPG_TEST_POLIZA_ID=<número de póliza real>
    confirm: rotated, ready
    ```
  </how-to-verify>
  <resume-signal>Operador pega el block formateado arriba con `confirm: rotated, ready` al final. Si el operador escribe `aborted` o describe un blocker (e.g., DPG no respondió, SoftSeguros user pending), Claude se detiene y reporta al usuario.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Setear env vars en Railway via CLI + restart agent-service + verify lifespan startup limpio</name>
  <files>(railway-side, no repo edits)</files>
  <action>
    Con los valores que el operador pegó en Task 1, ejecutar (NO commitear; los valores literales viven solo en Railway):

    ```
    # Set WA_* env vars (using values from Task 1 output)
    railway variable set --service landa-agent-service --skip-deploys \
      WA_TOKEN="<rotated-token>" \
      WA_PHONE_ID="1267241483129092" \
      WA_BUSINESS_ACCOUNT_ID="1451322196454283" \
      WA_WEBHOOK_SECRET="e556d909fcb2d4dd3e573a28eafccda7" \
      WA_VERIFY_TOKEN="96715a9c2658c915544f2faf735b98c0" \
      WA_ECHO_ALLOWLIST="<from-operator>"

    # Set SOFTSEGUROS_* env vars
    railway variable set --service landa-agent-service --skip-deploys \
      SOFTSEGUROS_BASE_URL="https://app.softseguros.com/" \
      SOFTSEGUROS_USERNAME="<from-operator>" \
      SOFTSEGUROS_PASSWORD="<from-operator>"

    # Set LANGSMITH_WORKSPACE_ID iff operator provided a real UUID (skip if 'none')
    # railway variable set --service landa-agent-service --skip-deploys \
    #   LANGSMITH_WORKSPACE_ID="<uuid>"

    # Trigger restart (Railway re-deploys on env-var change but --skip-deploys
    # batches; one redeploy now applies all 8-11 vars in a single boot)
    railway redeploy --service landa-agent-service --yes
    ```

    Esperar ~60s a que el redeploy complete. Verificar:

    ```
    # 1. Service status SUCCESS (not FAILED)
    railway status --service landa-agent-service

    # 2. Logs muestran lifespan.startup.complete sin errores
    railway logs --service landa-agent-service --tail 50 | grep -E "lifespan\\.|softseguros|meta"

    # Expected log lines:
    #   lifespan.startup
    #   lifespan.startup.complete
    # NO lines with: ValidationError, missing env var, ConnectionError
    ```

    Si `Settings()` levanta `ValidationError` en boot (ej. operador no proveyó alguna var), el service falla — diagnose con `railway logs` y pedir al operador la var faltante.

    Si todo verde: pasar a Task 3.
  </action>
  <verify>
    <automated>echo "Verificación manual: railway status muestra SUCCESS + railway logs muestra lifespan.startup.complete sin errores"</automated>
  </verify>
  <done>
    `railway status` muestra `landa-agent-service: SUCCESS`; logs muestran `lifespan.startup.complete`; no `ValidationError` en boot; las 8-11 env vars están seteadas (verificable con `railway variable list --service landa-agent-service` — pero NO pegar output al chat porque incluye secrets).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3 (HUMAN): Suscribir webhook Meta en dashboard + Verified status</name>
  <what-built>
    Claude no tiene CLI auth a Meta Business Suite. La suscripción del webhook URL + eventos `messages` + `message_status` debe hacerla el operador desde el dashboard. Una vez hecho, Meta dispara un `GET /webhooks/meta?hub.mode=subscribe&hub.verify_token=...&hub.challenge=...` y nuestro handler (Plan 02-02) lo valida con `verify_token`; si pasa, Meta muestra `Verified`.
  </what-built>
  <how-to-verify>
    Operador ejecuta (en Meta dashboard):

    1. Ir a Meta Business Suite → WhatsApp → `LandaTech` (WABA `1451322196454283`) → Configuration → Webhooks.

    2. Click "Edit" en el current webhook (si existe) o "Add Webhook":
       - **Callback URL:** `https://landa-agent-service-production.up.railway.app/webhooks/meta`
       - **Verify Token:** `96715a9c2658c915544f2faf735b98c0` (D-17 — el mismo que está en `WA_VERIFY_TOKEN` de Railway)
       - Click "Verify and Save"

    3. Meta dispara el GET challenge contra nuestra URL. Si nuestro handler responde 200 + `hub.challenge`, Meta muestra `Verified` (verde) en el dashboard.

    4. Suscribirse a fields (D-05):
       - Click "Manage" al lado del webhook
       - Tickar `messages` (mensajes entrantes)
       - Tickar `message_status` (delivery / read receipts)
       - Click "Save"

    Output esperado del operador:
    ```
    Meta webhook status: Verified ✅
    Subscribed fields: messages, message_status
    confirm: webhook live
    ```

    Si Meta muestra `Failed to verify`, diagnose:
    - `railway logs --service landa-agent-service --tail 20 | grep webhook` debería mostrar el GET challenge attempt.
    - 403 → `WA_VERIFY_TOKEN` en Railway no matchea el verify_token pegado en Meta dashboard.
    - timeout → service no responde; chequear `railway status`.
  </how-to-verify>
  <resume-signal>Operador pega `Meta webhook status: Verified ✅` y `confirm: webhook live`. Si `Failed to verify`, Claude pide el dashboard error message y ayuda a diagnose.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (HUMAN): Verificar round-trip WhatsApp real (operator-allowlisted) + dedup + non-allowlist filtering</name>
  <what-built>
    Backend ready + webhook subscribed. Ahora el operador valida el path real: envía WhatsApp desde el número en allowlist y verifica que el echo llega. También prueba dedup (Meta puede reentregar el mismo message_id) y allowlist filtering (número no allowlisted NO recibe echo).
  </what-built>
  <how-to-verify>
    Operador ejecuta (con su teléfono real):

    1. **Echo path (happy)**:
       - Desde el número en `WA_ECHO_ALLOWLIST` (e.g. `+5491134567890`), abrir WhatsApp.
       - Enviar `hola test` al `+16415416615` (LandaTech business number).
       - **Esperar < 5s.** Recibir back `echo: hola test`.
       - En paralelo, Claude monitorea: `railway logs --service landa-agent-service --tail 50` y debería mostrar:
         ```
         {"event":"webhook.echo.sent","message_id":"wamid.XXX","phone_hash":"abc12345","reply_len":15,"outbound_wamid":"wamid.YYY","result":"echo_sent",...}
         ```
       - Confirmar: NO raw phone en log (solo `phone_hash`).
       - Confirmar: `result=echo_sent`.

    2. **Dedup test (Meta reentrega o operator reenvía manualmente)**:
       - Difícil testear "real" sin tocar Redis (Meta no reentrega en demanda).
       - Aproximación: operador puede usar Meta Webhook Test Tool (en dashboard → Configuration → Webhooks → "Test") para enviar el mismo payload dos veces. La segunda vez:
         ```
         {"event":"webhook.dedup.skip","message_id":"wamid.XXX","message_type":"text","result":"ignored_duplicate"}
         ```
       - Confirmar: `result=ignored_duplicate`; meta.send_text NO llamado segunda vez.
       - Alternativa: dejar este check como "verificado por test unitario `test_post_duplicate_message_id_skips_echo` de Plan 02-02" + opcional manual con webhook tester.

    3. **Allowlist filtering**:
       - Desde un número NO en `WA_ECHO_ALLOWLIST` (e.g. teléfono de un amigo), enviar `hola` al `+16415416615`.
       - **NO debería recibir respuesta.** Esperar ~10s para confirmar.
       - Logs deberían mostrar:
         ```
         {"event":"webhook.ignored.not_allowlisted","message_id":"wamid.XXX","message_type":"text","phone_hash":"def67890","result":"ignored_not_allowlisted"}
         ```
       - Confirmar: result=`ignored_not_allowlisted`; el teléfono no-allowlisted NO recibió ningún mensaje.

    4. **Media test (opcional, nice-to-have)**:
       - Desde número en allowlist, enviar una imagen (cualquier foto random) al `+16415416615`.
       - Recibir back `echo: [image] received`.
       - Logs: `result=echo_media_sent`, `message_type=image`.

    Operador reporta al ejecutor:
    ```
    1. Echo happy: OK (received "echo: hola test" in <Xs)
    2. Dedup: OK via Meta webhook tester (or "deferred to unit test verification")
    3. Allowlist filter: OK (non-allowlisted number did NOT receive echo)
    4. Media echo: OK (received "echo: [image] received") | SKIPPED
    confirm: round-trip verified
    ```

    Si echo NO llega: diagnose:
    - `railway logs` debería mostrar el inbound webhook hit + dispatch path.
    - Si `webhook.hmac.invalid`: `WA_WEBHOOK_SECRET` en Railway no matchea el que Meta envía. Re-verify CONTEXT D-16 value.
    - Si `webhook.echo.error`: `MetaCloudClient.send_text` falló. Mostrar el `error_type` log.
    - Si NO hay log de webhook: el GET challenge no se completó o Meta no está reentregando. Volver a Task 3.
  </how-to-verify>
  <resume-signal>Operador pega `confirm: round-trip verified`. Si falla cualquier check, Claude help diagnose vía `railway logs`.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Smoke /test/poliza/{poliza_real_dpg} contra prod + cache hit verification + documentar en 02-SMOKE.md + actualizar RAILWAY_AGENT_NOTES.md</name>
  <files>.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-SMOKE.md, .planning/phases/01-setup-infra/RAILWAY_AGENT_NOTES.md</files>
  <action>
    Con `DPG_TEST_POLIZA_ID` del operador (Task 1 output):

    1. **SoftSeguros happy path**:
       ```
       POLIZA_ID="<from-operator>"
       AGENT_DOMAIN="landa-agent-service-production.up.railway.app"

       echo "=== Call 1 (cold cache) ==="
       time curl -sS "https://${AGENT_DOMAIN}/test/poliza/${POLIZA_ID}" | jq .
       # Capture: latency_ms, response shape (top-level keys), HTTP status
       ```
       Esperar 200 + JSON. Capturar:
       - `latency_ms` (debería ser <3000 para cold call — incluye token fetch + httpx GET + r.json())
       - Top-level keys de `.poliza` (e.g. `id`, `numero`, `saldo`, `cliente_id`, etc.) — REDACTED (no pegar valores en `02-SMOKE.md`, solo nombres de keys)

    2. **Cache hit (segunda call dentro de 60s)**:
       ```
       echo "=== Call 2 (warm cache, within 60s) ==="
       time curl -sS "https://${AGENT_DOMAIN}/test/poliza/${POLIZA_ID}" | jq .latency_ms
       # Should be < 100ms because cache hit bypasses httpx + DPG
       ```
       Esperar 200 + `latency_ms < 100` (cache hit). Si no, diagnose:
       - Si latency similar (cold): cache Redis no está funcionando. Verificar logs `softseguros.cache.read_error`.
       - Si HTTP 500: probablemente Redis no conectó. Verify `app.state.redis` en lifespan.

    3. **404 path (poliza inexistente — opcional)**:
       ```
       echo "=== Call 3 (non-existent poliza) ==="
       curl -sS -w "HTTP %{http_code}\\n" "https://${AGENT_DOMAIN}/test/poliza/999999999999"
       # Should return 500 (httpx raises HTTPStatusError on DPG 404 → FastAPI default 500)
       # F3+ can wire 404 → bot escalation flow; F2 acepta 500
       ```

    4. **Crear `.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-SMOKE.md`**:

       Estructura:
       ```
       # Phase 2 — Smoke E2E Verification Report

       **Date:** <YYYY-MM-DD HH:MM>
       **Verifier:** Claude (executor) + Maxi (operator, in-loop)
       **Status:** ✅ ALL PASS | ❌ FAIL on check X

       ## SC-1 — Round-trip WhatsApp echo (allowlisted)
       - Operator phone (allowlisted, REDACTED): `+5491...XXXX`
       - Sent: `"hola test"` at <HH:MM:SS>
       - Received: `"echo: hola test"` at <HH:MM:SS> (latency ~<X>s)
       - Railway log line (sanitized): `{"event":"webhook.echo.sent","phone_hash":"abcd1234","result":"echo_sent",...}`
       - Status: PASS

       ## SC-2 — HMAC verification
       - Test: Meta dashboard "Verify and Save" against /webhooks/meta with WA_VERIFY_TOKEN
       - Result: Meta dashboard shows "Verified ✅"
       - Status: PASS

       ## SC-3 — Idempotency (dedup)
       - Test: <"Meta Webhook Test sent same payload 2x" | "verified by unit test test_post_duplicate_message_id_skips_echo">
       - Result: 2nd POST returned 200 + log `result=ignored_duplicate`, NO outbound echo
       - Status: PASS

       ## SC-4 — Allowlist filtering
       - Non-allowlisted phone (REDACTED): `+57...XXXX`
       - Sent: `"hola"` from non-allowlisted phone
       - Result: NO echo received on sender's WhatsApp; log `result=ignored_not_allowlisted`
       - Status: PASS

       ## SC-5 — SoftSeguros /test/poliza/{id} cold cache
       - poliza_id (REDACTED): `XXXXX`
       - Call 1 latency: <X>ms
       - Response shape (top-level keys, values REDACTED): [id, numero, ...]
       - Status: PASS

       ## SC-6 — SoftSeguros cache hit (warm)
       - Call 2 latency: <X>ms (expected <100ms)
       - Cache hit confirmed: yes (latency_ms drop)
       - Status: PASS

       ## Optional — Media echo
       - Sent: image from allowlisted phone
       - Received: "echo: [image] received"
       - Status: PASS | SKIPPED

       ## Logs sample (sanitized, last 20 webhook lines from Railway)

       ```
       <paste sanitized log block — strip any token-looking strings, raw phones, full message_ids if sensitive>
       ```

       ## Verdict

       Phase 2 done. All success criteria (REQ-2.1, REQ-2.2, REQ-2.3, REQ-2.4) verified end-to-end against Railway live. Cleared to start Phase 3 (Bot Q&A inbound + Chatwoot mirror).

       ## Notes / Deferred follow-ups

       - LANGSMITH_WORKSPACE_ID: <set | not yet — operator returned 'none'>
       - F3 owns: removing /test/poliza/{id} (or gating with internal token) — D-10 says deferred to F5
       - F4 owns: cartera allowlist (echo allowlist es precursor pattern)
       ```

    5. **Actualizar `.planning/phases/01-setup-infra/RAILWAY_AGENT_NOTES.md`** appendiendo una nota al final (DESPUÉS del último contenido existente, sin reemplazar nada):

       ```
       ---

       ## Phase 2 follow-ups (appended 2026-MM-DD)

       - **Worker count = 1 assumption**: `pybreaker.CircuitBreaker` mantiene estado per-process (RESEARCH Pitfall 6). Para v1 con ~50 msg/día, 1 worker es suficiente. Si Railway escala horizontal en F5+, migrar a `pybreaker.CircuitRedisStorage` para shared state across workers. Current Railway worker count: 1 (default uvicorn single-process).
       - **LANGSMITH_WORKSPACE_ID**: <SET | NOT SET — operator returned 'none' en Plan 02-04 Task 1>. La var es opcional (`LangSmithSettings.workspace_id: SecretStr | None = None`); ausencia no causa boot failure.
       - **WA_TOKEN rotación**: nuevo token rotated 2026-MM-DD; el viejo (capturado en CONTEXT D-06 transcript 2026-06-28) revocado en Meta dashboard.
       - **SoftSeguros credentials**: dedicated user `<username>` con read-only scope contra DPG; encriptado en Railway env (no en tenant_configs table — F5+ migrará si multi-tenant).
       ```

    Commit final:
    ```
    git add .planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-SMOKE.md \
            .planning/phases/01-setup-infra/RAILWAY_AGENT_NOTES.md
    git commit -m "docs(02): smoke E2E verification + Railway notes for Phase 2 follow-ups"
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-SMOKE.md && grep -q "ALL PASS" .planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-SMOKE.md && grep -q "Phase 2 follow-ups" .planning/phases/01-setup-infra/RAILWAY_AGENT_NOTES.md && echo "smoke + notes OK"</automated>
  </verify>
  <done>
    `/test/poliza/{poliza_real_dpg}` retorna 200 con JSON crudo de DPG; segunda call dentro de 60s muestra `latency_ms < 100` (cache hit); `02-SMOKE.md` documenta los 6 success criteria (REQ-2.1, REQ-2.2, REQ-2.3, REQ-2.4 + cache hit + allowlist filter) con status PASS; `RAILWAY_AGENT_NOTES.md` updated con notas de worker count + LANGSMITH_WORKSPACE_ID + WA_TOKEN rotation + SoftSeguros creds; commit con conventional message landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator phone → Meta → Railway webhook | Tráfico real cruza Internet; depende de HMAC enforcement de Plan 02-02 |
| Railway → SoftSeguros con DPG creds | Tráfico cruza egress; depende de SecretStr + TLS de Plan 02-03 |
| Operator → Railway CLI | Operator authentication (railway login session) gates env var writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-20 | Information disclosure | `WA_TOKEN` viejo sigue activo después de rotation (operador olvida revoke) | mitigate | Task 1 explicit: "Revocar el token viejo" — operator manual action. Documentar revocation timestamp en `02-SMOKE.md` |
| T-02-21 | Spoofing | Atacante adivina `WA_VERIFY_TOKEN` y completa el GET challenge para hijack del webhook | accept | verify_token 32-char hex (D-17) generado por operador; entropy suficiente para v1. F5+ podría rotar |
| T-02-22 | Information disclosure | `02-SMOKE.md` commitea raw phone, raw poliza data, real WA_TOKEN | mitigate | Task 5 explícito: REDACTED phones, REDACTED poliza values, log lines sanitized. Operator revisa antes del commit |
| T-02-23 | Tampering | `DPG_TEST_POLIZA_ID` es de un cliente real → /test/poliza/{id} expone PII del cliente al ejecutor + a Railway logs | accept (F2) → mitigate (F5) | F2 trade-off: necesitamos verificar el path real con datos reales (sin esto, F2 no está verificado). F5 gateará /test/poliza con internal token + redaction. Operator confirma usar un poliza_id de DPG con consentimiento implícito de DPG (single-tenant v1) |
</threat_model>

<verification>
- `02-SMOKE.md` existe con los 6 success criteria documented y status PASS.
- `RAILWAY_AGENT_NOTES.md` tiene la sección "Phase 2 follow-ups" appended.
- `git log --oneline -5` muestra el commit `docs(02): smoke E2E verification...`.
- `railway status --service landa-agent-service` muestra SUCCESS.
- `curl -sS https://landa-agent-service-production.up.railway.app/health | jq .status` retorna `"healthy"` (cuando todo está conectado).
- Meta dashboard muestra webhook `Verified ✅` con `messages` + `message_status` subscribed.
- WhatsApp del operador muestra el echo recibido.
</verification>

<success_criteria>
- WA_TOKEN rotado; viejo revocado en Meta dashboard.
- 8-11 env vars seteadas en Railway agent-service.
- Meta webhook Verified + subscribed a `messages` + `message_status`.
- Round-trip WhatsApp: operador envía desde allowlist → recibe echo en <5s.
- Idempotencia verified (unit test o Meta webhook tester).
- Allowlist filtering: non-allowlisted phone NO recibe echo.
- `/test/poliza/{poliza_real}` cold call retorna 200 + JSON (<3s).
- `/test/poliza/{poliza_real}` warm call retorna 200 (<100ms — cache hit).
- `02-SMOKE.md` documenta los 6 checkpoints con outputs sanitizados.
- `RAILWAY_AGENT_NOTES.md` updated con worker count + LANGSMITH_WORKSPACE_ID + WA_TOKEN rotation + SoftSeguros creds notes.
- Commit `docs(02): smoke E2E verification` landed.
</success_criteria>

<output>
Plan 02-04 produce:
- `.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-SMOKE.md` (smoke report) — created in Task 5.
- `.planning/phases/01-setup-infra/RAILWAY_AGENT_NOTES.md` (appended Phase 2 follow-ups) — modified in Task 5.

NO crear un `02-04-SUMMARY.md` separado — el `02-SMOKE.md` ES el summary de este plan. La gracia de Plan 02-04 es la verificación operacional documented, no un summary post-hoc.

Después del commit final, el ejecutor reporta al usuario:

```
Phase 2 cerrada — F2 done.

SC verificados (en 02-SMOKE.md):
- SC-1: Round-trip WhatsApp echo (allowlisted) ✅
- SC-2: HMAC verification ✅
- SC-3: Idempotency dedup ✅
- SC-4: Allowlist filtering ✅
- SC-5: SoftSeguros cold cache ✅
- SC-6: SoftSeguros warm cache hit ✅

Stack live en Railway:
- agent-service: SUCCESS
- webhook URL: https://landa-agent-service-production.up.railway.app/webhooks/meta
- /test/poliza/{id}: gated/removed en F5 (D-10)

Próximo paso: /gsd-plan-phase 03 (Bot Q&A inbound + Chatwoot mirror)
```
</output>
