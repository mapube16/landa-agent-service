---
phase: 02-integraci-n-softseguros-whatsapp-cloud-api
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/integrations/meta_cloud.py
  - app/webhooks/meta.py
  - app/features/handoff/echo.py
  - app/main.py
  - tests/test_integrations_meta_cloud.py
  - tests/test_webhooks_meta.py
  - tests/test_features_handoff_echo.py
autonomous: true
requirements:
  - REQ-2.3   # HMAC X-Hub-Signature-256 en webhooks Meta (D-16)
  - REQ-2.4   # Idempotencia por message_id (D-14, D-15)
estimated_complexity: M

must_haves:
  truths:
    - "MetaCloudClient.send_text(to, body) hace POST a https://graph.facebook.com/v21.0/{settings.whatsapp.phone_id}/messages con Authorization: Bearer {WA_TOKEN}, body = OutboundText.model_dump() (RESEARCH 'Code Examples — Outbound text message'). Retorna wamid de la response. Raisea httpx.HTTPError en 4xx/5xx (deja al caller decidir; F2 NO retrya outbound — F4+ con tenacity si hace falta)"
    - "MetaCloudClient.send_media_ack(to, media_type) llama send_text(to, format_media_echo(media_type)) por convención (D-02 + CONTEXT Specifics — 'echo: [media type] received')"
    - "get_meta_client() retorna MetaCloudClient cached con @lru_cache(maxsize=1); httpx.AsyncClient construido con base_url=META_BASE_URL, headers={Authorization: Bearer ...} (de SecretStr.get_secret_value()), timeout 10s, limits sanos (max_keepalive=20, max_connections=50, keepalive_expiry=30) — patrón openrouter.py:96-128 + RESEARCH Pattern 1"
    - "GET /webhooks/meta lee query params hub.mode + hub.verify_token + hub.challenge (aliases por '.' literal — RESEARCH 'GET challenge'). Si hub.mode == 'subscribe' AND hub.verify_token == settings.whatsapp.verify_token.get_secret_value(): retorna 200 con PlainTextResponse(hub.challenge). Sino: HTTPException 403 (D-09, D-17)"
    - "POST /webhooks/meta sigue el orden invariante HMAC → parse → dedup → allowlist → echo (D-15). Función _verify_signature(raw_body, header_sig, secret) usa hmac.compare_digest (NO ==) sobre 'sha256=' + hmac.new(secret.encode(), raw_body, hashlib.sha256).hexdigest() (D-16)"
    - "Si HMAC inválido o header ausente: HTTP 401 + structured log webhook.hmac.invalid (sin pegar raw_body en log — solo header_present bool). Si HMAC ok pero JSON malformado: HTTP 200 + log webhook.malformed (NO 422 — Meta retrya 24h en 5xx, malformed no se arregla con retry, RESEARCH Pitfall 7)"
    - "Idempotency dedup: await request.app.state.redis.set(f'wa:msg:{message_id}'.encode(), b'1', nx=True, ex=86400). True → first-see, continuar. None → log 'webhook.dedup.skip' + skip al siguiente mensaje SIN side effect (echo NO se envía). (D-14, D-15)"
    - "Allowlist gate: is_echo_allowed(msg.from_) usa _normalize_e164 en ambos lados (Meta envía sin '+', env var con '+' — Pitfall 8). Si False: log webhook.ignored.not_allowlisted + skip, SIN side effect (D-02)"
    - "Echo dispatch: text → format_echo(msg.text.body) → meta.send_text(to=msg.from_, body=reply). Image/audio/sticker/video/document/location → format_media_echo(msg.type) → meta.send_text. Otros types ('contacts'/'interactive'/'button'/'unknown'): log webhook.unsupported_type + skip (defensive)"
    - "Status updates (entry.changes.value.statuses presente): log webhook.status.received con status payload + skip (D-05 dice F2 los acknowledge pero NO procesa)"
    - "Logging structured incluye: message_id, message_type, phone_hash=sha256(from_)[:8] (NUNCA raw phone — Pitfall 9 + PATTERNS.md), result ∈ {echo_sent, echo_media_sent, ignored_not_allowlisted, ignored_duplicate, ignored_unsupported_type, status_received, error}"
    - "app/main.py lifespan: app.state.meta = get_meta_client() después del checkpointer setup (NO async-resource-heavy — Pitfall 1 de PATTERNS.md). Sin teardown explícito; httpx.AsyncClient muere con el process"
    - "app/main.py include_router(meta_router) DESPUÉS de health_router (main.py:146 pattern). Import noqa:E402 porque va después de init_sentry()"
    - "tests/test_integrations_meta_cloud.py: factory cache identity (get_meta_client() is get_meta_client()), META_API_VERSION constant == 'v21.0', send_text POST shape verificado con respx o monkeypatched httpx.AsyncClient.post (assert called_with url terminando en '/messages', json={messaging_product, recipient_type, to, type, text:{body}})"
    - "tests/test_webhooks_meta.py: HMAC valid (200 + echo invocado), invalid (401), missing-header (401); idempotency (mismo message_id dos veces → primera vez echo, segunda vez 200 sin echo); GET challenge valid (200 + body == hub.challenge), wrong verify_token (403); allowlist (non-allowlisted phone → 200 + log ignored, meta.send_text NO llamado); media echo (image type → meta.send_text llamado con 'echo: [image] received'); status update (statuses array → 200 SIN dispatch); malformed JSON (200, NO 422)"
    - "tests/test_features_handoff_echo.py: is_echo_allowed('+15555550100') True, is_echo_allowed('15555550100') True (normalization), is_echo_allowed('+99999999999') False; _normalize_e164('+1234') == '+1234', _normalize_e164('1234') == '+1234'; format_echo('hola') == 'echo: hola'; format_media_echo('image') == 'echo: [image] received'"
  artifacts:
    - path: app/integrations/meta_cloud.py
      provides: "MetaCloudClient class real + get_meta_client() factory implementado (reemplaza skeleton de Plan 02-01)"
      exports: ["MetaCloudClient", "get_meta_client", "META_API_VERSION", "META_BASE_URL"]
    - path: app/webhooks/meta.py
      provides: "GET /webhooks/meta (challenge) + POST /webhooks/meta (raw-body HMAC → dedup → allowlist → echo) implementados"
      exports: ["router"]
    - path: app/features/handoff/echo.py
      provides: "_normalize_e164 + is_echo_allowed + format_echo + format_media_echo implementados"
      exports: ["format_echo", "format_media_echo", "is_echo_allowed"]
    - path: app/main.py
      provides: "Lifespan registra app.state.meta vía get_meta_client(); meta_router incluido"
      contains: "app.state.meta"
      contains: "meta_router"
    - path: tests/test_integrations_meta_cloud.py
      provides: "Factory cache identity + send_text POST shape + META_API_VERSION constant"
    - path: tests/test_webhooks_meta.py
      provides: "HMAC valid/invalid/missing + idempotency dup-skip + GET challenge + allowlist + media echo + status update + malformed JSON"
    - path: tests/test_features_handoff_echo.py
      provides: "is_echo_allowed truth table + E.164 normalization + format_* output shapes"
  key_links:
    - from: app/webhooks/meta.py
      to: app/integrations/meta_cloud.py
      via: "request.app.state.meta.send_text(...)"
      pattern: "request\\.app\\.state\\.meta"
    - from: app/webhooks/meta.py
      to: app/features/handoff/echo.py
      via: "is_echo_allowed(msg.from_) + format_echo(msg.text.body) + format_media_echo(msg.type)"
      pattern: "is_echo_allowed|format_echo|format_media_echo"
    - from: app/webhooks/meta.py
      to: app.state.redis
      via: "request.app.state.redis.set(key, b'1', nx=True, ex=86400) para dedup"
      pattern: "request\\.app\\.state\\.redis"
    - from: app/main.py
      to: app/integrations/meta_cloud.py
      via: "app.state.meta = get_meta_client() en lifespan"
      pattern: "get_meta_client"
    - from: app/main.py
      to: app/webhooks/meta.py
      via: "include_router(meta_router)"
      pattern: "meta_router"
---

<objective>
Implementar el slice Meta Cloud API end-to-end: cliente outbound (`MetaCloudClient.send_text`), webhook receiver (`GET` challenge + `POST` con HMAC + idempotency + allowlist + echo dispatch), echo helpers (E.164 normalization + format functions), integración en lifespan + router de `app/main.py`, y tests exhaustivos (HMAC matrix, idempotency dup-skip, allowlist truth table, media echo, status update acknowledge, malformed JSON tolerancia).

Purpose: Cerrar las capas 7 (HMAC), 9 (idempotency) y 8 (allowlist embrionario) del PROJECT §"Seguridad" — D-02, D-09, D-14, D-15, D-16, D-17. Cuando el operador (Plan 02-04) envíe WhatsApp desde un número en allowlist, el round-trip funciona en < 3s.

Output: Stack Meta funcionando contra Railway en cuanto los env vars existan (Plan 02-04 los wirea), con tests offline pasando 100% sin necesidad de mocks de Meta o Redis en vivo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-CONTEXT.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-RESEARCH.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-PATTERNS.md
@CLAUDE.md
@app/config/settings.py
@app/integrations/openrouter.py
@app/healthcheck.py
@app/main.py
@app/models/meta.py
@app/integrations/meta_cloud.py
@app/webhooks/meta.py
@app/features/handoff/echo.py
@tests/conftest.py
@tests/test_health.py
@tests/test_llm_factory.py
</context>

<merge_strategy>
Plan 02-02 corre en Wave 2 paralelo a Plan 02-03. Ambos modifican `app/main.py`. Por convención lexicográfica, Plan 02-02 corre primero. Cambios en `app/main.py`:
1. Añadir import `from app.integrations.meta_cloud import get_meta_client  # noqa: E402` después del existente `get_llm` (línea 52).
2. Añadir import `from app.webhooks.meta import router as meta_router  # noqa: E402` debajo del anterior.
3. Dentro de `lifespan` después de `await app.state.checkpointer.setup()` (línea 81): `app.state.meta = get_meta_client()`.
4. Después de `app.include_router(health_router)` (línea 146): `app.include_router(meta_router)`.

Plan 02-03 rebaseará sobre estos cambios cuando añada `app.state.softseguros` + `/test/poliza/{id}`.
</merge_strategy>

<tasks>

<task type="auto">
  <name>Task 1: Implementar MetaCloudClient + get_meta_client factory + echo helpers</name>
  <files>app/integrations/meta_cloud.py, app/features/handoff/echo.py, tests/test_integrations_meta_cloud.py, tests/test_features_handoff_echo.py</files>
  <action>
    **Archivo 1 — `app/integrations/meta_cloud.py`** (reemplaza skeleton de Plan 02-01, ~110 líneas finales):

    Mantener docstring + constantes `META_API_VERSION = "v21.0"` + `META_BASE_URL` del skeleton. Implementar:

    Imports añadidos: `from typing import Any, Final` (Final ya existe), `httpx`, `structlog`, `from app.models.meta import OutboundText, OutboundTextBody`.

    Logger: `log = structlog.get_logger("integrations.meta_cloud")` (PATTERNS.md naming convention).

    Class `MetaCloudClient`:
    - `__init__(self, http: httpx.AsyncClient, phone_id: str)` — guarda `self._http`, `self._phone_id`.
    - `async def send_text(self, to: str, body: str) -> str`:
      - Construir `payload = OutboundText(to=to, text=OutboundTextBody(body=body)).model_dump(mode="json")` (shape de RESEARCH "Code Examples — Outbound text message").
      - `r = await self._http.post(f"/{self._phone_id}/messages", json=payload)`.
      - `r.raise_for_status()` — F2 NO retrya outbound (CLAUDE.md "Don't ❌" no aplica retry directo acá; Plan F4+ envuelve con tenacity si métricas lo justifican). Caller atrapará y logueará.
      - Parsear `data = r.json()` → `wamid = data["messages"][0]["id"]` (RESEARCH "Code Examples — Success response").
      - `log.info("meta.send_text.ok", to_hash=_hash_phone(to), wamid=wamid, body_len=len(body))`. **NEVER loguear `body` ni `to` en raw** (PII).
      - Return `wamid`.
    - `async def send_media_ack(self, to: str, media_type: str) -> str`:
      - Delegado: `from app.features.handoff.echo import format_media_echo; return await self.send_text(to, format_media_echo(media_type))`. Conveniencia para el handler.

    Helper privado top-level:
    ```
    import hashlib
    def _hash_phone(phone: str) -> str:
        """Return first 8 hex chars of sha256(phone) — non-reversible correlation token (Pitfall 9)."""
        return hashlib.sha256(phone.encode()).hexdigest()[:8]
    ```

    Factory:
    ```
    @lru_cache(maxsize=1)
    def get_meta_client() -> MetaCloudClient:
        """Return the cached MetaCloudClient singleton.

        NEVER instantiate httpx clients to Meta elsewhere — always go through
        this factory so connection pool warmth and Authorization header
        consistency are preserved.
        """
        limits = httpx.Limits(max_keepalive_connections=20, max_connections=50, keepalive_expiry=30.0)
        timeout = httpx.Timeout(10.0, connect=3.0, read=10.0, write=3.0, pool=2.0)
        http = httpx.AsyncClient(
            base_url=META_BASE_URL,
            headers={"Authorization": f"Bearer {settings.whatsapp.token.get_secret_value()}"},
            timeout=timeout,
            limits=limits,
        )
        return MetaCloudClient(http=http, phone_id=settings.whatsapp.phone_id)
    ```

    Mantener `__all__ = ["META_API_VERSION", "META_BASE_URL", "MetaCloudClient", "get_meta_client"]`.

    **Archivo 2 — `app/features/handoff/echo.py`** (reemplaza skeleton, ~40 líneas finales):

    Mantener module docstring. Implementar:

    ```
    def _normalize_e164(raw: str) -> str:
        """Return ``raw`` always prefixed with '+'. Idempotent.

        Meta sends ``from`` as ``"16505551234"`` (no leading ``+``); operators
        store allowlist entries as ``+16505551234`` (with ``+``). This helper
        normalises both sides to ``+`` form so set membership works (Pitfall 8).
        """
        raw = raw.strip()
        return raw if raw.startswith("+") else "+" + raw


    def is_echo_allowed(phone: str) -> bool:
        """Return True iff ``phone`` (E.164-normalised) is in ``WA_ECHO_ALLOWLIST``."""
        normalized = _normalize_e164(phone)
        allowed = {_normalize_e164(p) for p in settings.whatsapp.echo_allowlist}
        return normalized in allowed


    def format_echo(text: str) -> str:
        """Return ``"echo: <text>"`` — the literal echo response for text messages (D-02)."""
        return f"echo: {text}"


    def format_media_echo(media_type: str) -> str:
        """Return ``"echo: [<media_type>] received"`` — echo response for non-text messages (CONTEXT Specifics)."""
        return f"echo: [{media_type}] received"
    ```

    Mantener `__all__ = ["format_echo", "format_media_echo", "is_echo_allowed"]`.

    **Archivo 3 — `tests/test_features_handoff_echo.py`** (nuevo, ~50 líneas, patrón de `test_llm_factory.py`):

    Tests (sync, sin async):

    - `test_normalize_e164_adds_plus_if_missing`: `_normalize_e164("16505551234") == "+16505551234"`.
    - `test_normalize_e164_idempotent_when_plus_present`: `_normalize_e164("+5491134567890") == "+5491134567890"`.
    - `test_normalize_e164_strips_whitespace`: `_normalize_e164("  +1 234  ".strip()) == "+1 234"` (al menos la parte de strip — strip-only).
    - `test_is_echo_allowed_true_for_allowlisted_with_plus`: conftest setea `WA_ECHO_ALLOWLIST=+15555550100,+15555550101`. `is_echo_allowed("+15555550100") is True`.
    - `test_is_echo_allowed_true_for_allowlisted_without_plus`: `is_echo_allowed("15555550100") is True` (Meta-style).
    - `test_is_echo_allowed_false_for_unknown`: `is_echo_allowed("+19999999999") is False`.
    - `test_format_echo_prefixes_text`: `format_echo("hola") == "echo: hola"`.
    - `test_format_echo_preserves_unicode`: `format_echo("¿qué tal?") == "echo: ¿qué tal?"` (Pydantic-safe round-trip cuando se serializa).
    - `test_format_media_echo_image`: `format_media_echo("image") == "echo: [image] received"`.
    - `test_format_media_echo_audio`: `format_media_echo("audio") == "echo: [audio] received"`.

    Import: `from app.features.handoff.echo import _normalize_e164, format_echo, format_media_echo, is_echo_allowed`.

    Nota: tests `is_echo_allowed_*` dependen del autouse `_test_env` de conftest que ya setea `WA_ECHO_ALLOWLIST=+15555550100,+15555550101` (Plan 02-01 Task 1).

    **Archivo 4 — `tests/test_integrations_meta_cloud.py`** (nuevo, ~70 líneas, patrón de `test_llm_factory.py`):

    Tests (mix sync + async):

    Sync tests:
    - `test_meta_api_version_is_v21`: `from app.integrations.meta_cloud import META_API_VERSION; assert META_API_VERSION == "v21.0"`.
    - `test_meta_base_url_composition`: `from app.integrations.meta_cloud import META_BASE_URL; assert META_BASE_URL == "https://graph.facebook.com/v21.0"`.
    - `test_get_meta_client_is_singleton`: `a = get_meta_client(); b = get_meta_client(); assert a is b`.
    - `test_get_meta_client_uses_phone_id_from_settings`: `client = get_meta_client(); assert client._phone_id == "1267241483129092"` (placeholder de conftest Plan 02-01).
    - `test_get_meta_client_uses_token_in_auth_header`: `client = get_meta_client(); assert client._http.headers["Authorization"] == "Bearer wa-test-token"` (no leak en repr; este test sí accede explícito para verificar wiring).

    Async tests (usar `respx==0.21.1` si está disponible; sino, `monkeypatch` sobre `httpx.AsyncClient.post` con `AsyncMock`):
    - `test_send_text_posts_to_messages_endpoint`: stub `client._http.post` para capturar args, llamar `await client.send_text(to="16505551234", body="hola")`, assert URL `f"/{client._phone_id}/messages"`, assert body JSON `{"messaging_product": "whatsapp", "recipient_type": "individual", "to": "16505551234", "type": "text", "text": {"body": "hola"}}`.
    - `test_send_text_returns_wamid_from_response`: stub post para retornar `{"messages":[{"id":"wamid.XYZ"}]}`, assert return value == `"wamid.XYZ"`.
    - `test_send_text_raises_on_4xx`: stub post → `httpx.HTTPStatusError`, assert `pytest.raises(httpx.HTTPStatusError)`.
    - `test_send_media_ack_delegates_to_send_text_with_media_echo`: stub `send_text` (no `_http.post`), llamar `await client.send_media_ack(to="x", media_type="image")`, assert `send_text` llamado con `body="echo: [image] received"`.

    Para stubbing async-clean:
    ```
    from unittest.mock import AsyncMock
    import pytest, httpx

    @pytest.fixture
    def stubbed_client(monkeypatch):
        from app.integrations.meta_cloud import get_meta_client
        client = get_meta_client()
        mock_post = AsyncMock()
        mock_post.return_value = httpx.Response(
            200, json={"messages":[{"id":"wamid.XYZ"}]}, request=httpx.Request("POST", "http://x")
        )
        monkeypatch.setattr(client._http, "post", mock_post)
        return client, mock_post
    ```

    NOTA: `get_meta_client` cachea — si tests modifican el mismo cliente, usar `get_meta_client.cache_clear()` en setup/teardown si interfiere.
  </action>
  <verify>
    <automated>uv run pytest tests/test_features_handoff_echo.py tests/test_integrations_meta_cloud.py -v</automated>
  </verify>
  <done>
    `tests/test_features_handoff_echo.py` 10 tests pasan; `tests/test_integrations_meta_cloud.py` 9 tests pasan; `MetaCloudClient.send_text` POSTea con el body exacto que Meta espera (RESEARCH "Code Examples"); `get_meta_client()` retorna singleton cached con headers Authorization + base_url `v21.0`; `_hash_phone` no leak raw phones a logs; mypy `--strict` pasa.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementar webhook handler app/webhooks/meta.py — GET challenge + POST raw-body HMAC + idempotency + allowlist + echo dispatch</name>
  <files>app/webhooks/meta.py, tests/test_webhooks_meta.py</files>
  <action>
    **Archivo 1 — `app/webhooks/meta.py`** (reemplaza skeleton, ~160 líneas finales — RESEARCH Patterns 3, 4 + Pitfalls 1, 7, 8, 9, 10):

    Mantener docstring + `router = APIRouter(prefix="/webhooks", tags=["meta"])` + logger del skeleton.

    Imports añadidos: `import hmac, hashlib, json`, `from typing import Any`, `from fastapi import Request, Response, HTTPException, Query`, `from fastapi.responses import PlainTextResponse`, `from pydantic import ValidationError`, `from app.models.meta import InboundEnvelope, InboundMessage`, `from app.features.handoff.echo import is_echo_allowed, format_echo, format_media_echo`, `from app.integrations.meta_cloud import _hash_phone`.

    Helper top-level:
    ```
    def _verify_signature(raw_body: bytes, header_value: str, secret: str) -> bool:
        """Constant-time HMAC SHA-256 verify for Meta X-Hub-Signature-256 (D-16).

        NEVER use ``==``: that leaks the prefix-match length via timing. Always
        ``hmac.compare_digest``. D-16 makes this a hard rule, not a preference.
        """
        expected = "sha256=" + hmac.new(secret.encode("utf-8"), raw_body, hashlib.sha256).hexdigest()
        return hmac.compare_digest(expected, header_value)
    ```

    **GET handler** (D-09, D-17 — RESEARCH "Code Examples — GET challenge"):
    ```
    @router.get("/meta")
    async def verify(
        hub_mode: str = Query("", alias="hub.mode"),
        hub_verify_token: str = Query("", alias="hub.verify_token"),
        hub_challenge: str = Query("", alias="hub.challenge"),
    ) -> Response:
        """GET challenge per Meta webhook subscription flow.

        Meta sends ``hub.mode=subscribe`` once when the operator configures the
        webhook URL in Meta dashboard. We echo back ``hub.challenge`` as plain
        text iff ``hub.verify_token`` matches the secret-stored verify token.
        Plain-stdlib ``==`` is acceptable here because ``verify_token`` is set
        by the operator and exists only at webhook-setup time — there is no
        timing-attack vector on a one-shot config-time check.
        """
        if hub_mode == "subscribe" and hub_verify_token == settings.whatsapp.verify_token.get_secret_value():
            return PlainTextResponse(content=hub_challenge, status_code=200)
        log.warning("webhook.verify.invalid", mode=hub_mode, has_token=bool(hub_verify_token))
        raise HTTPException(status_code=403, detail="forbidden")
    ```

    **POST handler** (RESEARCH Patterns 3, 4 — orden invariante HMAC → parse → dedup → allowlist → echo):
    ```
    @router.post("/meta")
    async def receive(request: Request) -> Response:
        """Receive Meta webhook events.

        Order (D-15 — INVARIANT, do not reorder):
          1. ``await request.body()`` — capture raw bytes BEFORE any json parsing
             (Pitfall 1 + 10: re-reading body fails; HMAC must hash raw bytes).
          2. HMAC verify via ``_verify_signature`` (D-16). Fail → 401.
          3. ``InboundEnvelope.model_validate_json(raw)``. Malformed → 200 +
             log (NOT 422 — Meta retries 5xx for ~24h, malformed won't fix
             itself, Pitfall 7).
          4. For each message in each entry.changes:
             a. Status update (statuses array): log + skip (D-05 acknowledge only).
             b. Idempotency: ``redis.set('wa:msg:{id}', b'1', nx=True, ex=86400)``
                (D-14, D-15). None → duplicate → skip side effects.
             c. Allowlist gate: ``is_echo_allowed(msg.from_)`` (D-02 + Pitfall 8).
                False → log + skip.
             d. Echo dispatch: ``text`` → ``send_text(format_echo(...))``; media
                types → ``send_text(format_media_echo(...))``; unsupported → log + skip.
          5. Always return HTTP 200 once HMAC passes — see Pitfall 7.
        """
        # 1. Raw body (must come FIRST — Pitfall 1, 10).
        raw = await request.body()

        # 2. HMAC verify (D-16).
        header_sig = request.headers.get("X-Hub-Signature-256", "")
        if not header_sig or not _verify_signature(
            raw, header_sig, settings.whatsapp.webhook_secret.get_secret_value()
        ):
            log.warning("webhook.hmac.invalid", header_present=bool(header_sig))
            raise HTTPException(status_code=401, detail="invalid signature")

        # 3. Parse Pydantic (only after HMAC succeeded).
        try:
            envelope = InboundEnvelope.model_validate_json(raw)
        except ValidationError as exc:
            log.warning("webhook.malformed", error=type(exc).__name__, error_count=exc.error_count())
            return Response(status_code=200)

        # 4. Dispatch each message (D-15 order).
        meta = request.app.state.meta
        redis = request.app.state.redis

        for entry in envelope.entry:
            for change in entry.changes:
                value = change.value
                # 4a. Status updates — acknowledge only (D-05).
                if value.statuses is not None:
                    for status in value.statuses:
                        log.info(
                            "webhook.status.received",
                            status=status.get("status"),
                            recipient_hash=_hash_phone(str(status.get("recipient_id", ""))),
                            result="status_received",
                        )
                    continue

                for msg in (value.messages or []):
                    await _dispatch_message(msg=msg, meta=meta, redis=redis)

        return Response(status_code=200)


    async def _dispatch_message(*, msg: InboundMessage, meta: Any, redis: Any) -> None:
        """Apply dedup → allowlist → echo, in that order (D-15).

        ``meta`` and ``redis`` are typed ``Any`` here because their concrete types
        live in app.state (MetaCloudClient and redis.asyncio.Redis) and importing
        them creates a circular dependency at the module level. Functional contract
        is enforced by test stubs.
        """
        # 4b. Idempotency check (D-14, D-15 — MUST precede side effect).
        key = f"wa:msg:{msg.id}".encode("utf-8")
        first_see = await redis.set(key, b"1", nx=True, ex=86400)
        if first_see is None:
            log.info(
                "webhook.dedup.skip",
                message_id=msg.id,
                message_type=msg.type,
                result="ignored_duplicate",
            )
            return

        phone_hash = _hash_phone(msg.from_)

        # 4c. Allowlist (D-02 + Pitfall 8).
        if not is_echo_allowed(msg.from_):
            log.info(
                "webhook.ignored.not_allowlisted",
                message_id=msg.id,
                message_type=msg.type,
                phone_hash=phone_hash,
                result="ignored_not_allowlisted",
            )
            return

        # 4d. Echo dispatch.
        if msg.type == "text" and msg.text is not None:
            reply = format_echo(msg.text.body)
            try:
                wamid = await meta.send_text(to=msg.from_, body=reply)
            except Exception as exc:  # noqa: BLE001
                log.exception(
                    "webhook.echo.error",
                    message_id=msg.id,
                    phone_hash=phone_hash,
                    error_type=type(exc).__name__,
                    result="error",
                )
                return
            log.info(
                "webhook.echo.sent",
                message_id=msg.id,
                phone_hash=phone_hash,
                reply_len=len(reply),
                outbound_wamid=wamid,
                result="echo_sent",
            )
            return

        if msg.type in {"image", "audio", "sticker", "video", "document", "voice", "location"}:
            try:
                wamid = await meta.send_media_ack(to=msg.from_, media_type=msg.type)
            except Exception as exc:  # noqa: BLE001
                log.exception(
                    "webhook.echo.error",
                    message_id=msg.id,
                    phone_hash=phone_hash,
                    error_type=type(exc).__name__,
                    result="error",
                )
                return
            log.info(
                "webhook.echo.media.sent",
                message_id=msg.id,
                phone_hash=phone_hash,
                message_type=msg.type,
                outbound_wamid=wamid,
                result="echo_media_sent",
            )
            return

        # Unsupported types (contacts/interactive/button/unknown).
        log.info(
            "webhook.unsupported_type",
            message_id=msg.id,
            message_type=msg.type,
            phone_hash=phone_hash,
            result="ignored_unsupported_type",
        )
    ```

    Mantener `__all__ = ["router"]`.

    **Archivo 2 — `tests/test_webhooks_meta.py`** (nuevo, ~250 líneas, patrón de `test_health.py`):

    Helpers + fixtures top:

    ```
    import hmac, hashlib, json
    from typing import Any
    from unittest.mock import AsyncMock, MagicMock

    import pytest
    from httpx import AsyncClient


    WEBHOOK_SECRET = "test-webhook-secret-do-not-use-in-prod"  # matches conftest placeholder


    def _sign(raw_body: bytes, secret: str = WEBHOOK_SECRET) -> str:
        return "sha256=" + hmac.new(secret.encode(), raw_body, hashlib.sha256).hexdigest()


    def _inbound_text_payload(message_id: str = "wamid.test1", from_: str = "15555550100", text: str = "hola") -> bytes:
        """Minimal valid Meta inbound text webhook payload (subset of RESEARCH 'Code Examples')."""
        return json.dumps({
            "object": "whatsapp_business_account",
            "entry": [{
                "id": "1451322196454283",
                "changes": [{
                    "value": {
                        "messaging_product": "whatsapp",
                        "metadata": {"display_phone_number": "16415416615", "phone_number_id": "1267241483129092"},
                        "messages": [{
                            "from": from_,
                            "id": message_id,
                            "timestamp": "1749416383",
                            "type": "text",
                            "text": {"body": text},
                        }],
                    },
                    "field": "messages",
                }],
            }],
        }).encode("utf-8")


    def _inbound_image_payload(message_id: str = "wamid.img1", from_: str = "15555550100") -> bytes:
        return json.dumps({
            "object": "whatsapp_business_account",
            "entry": [{"id": "x", "changes": [{
                "value": {
                    "messaging_product": "whatsapp",
                    "metadata": {},
                    "messages": [{
                        "from": from_, "id": message_id, "timestamp": "1", "type": "image",
                    }],
                }, "field": "messages",
            }]}],
        }).encode("utf-8")


    def _inbound_status_payload() -> bytes:
        return json.dumps({
            "object": "whatsapp_business_account",
            "entry": [{"id": "x", "changes": [{
                "value": {
                    "messaging_product": "whatsapp",
                    "metadata": {},
                    "statuses": [{"id": "wamid.s1", "status": "delivered", "timestamp": "1", "recipient_id": "15555550100"}],
                }, "field": "messages",
            }]}],
        }).encode("utf-8")


    @pytest.fixture
    def stub_app_state(monkeypatch):
        """Inject mock meta + redis onto app.state so the handler doesn't need lifespan.

        Returns (meta_mock, redis_mock) so each test can assert call shape.
        """
        from app.main import app as fastapi_app

        meta_mock = MagicMock()
        meta_mock.send_text = AsyncMock(return_value="wamid.outbound")
        meta_mock.send_media_ack = AsyncMock(return_value="wamid.outbound.media")

        redis_mock = MagicMock()
        redis_mock.set = AsyncMock(return_value=True)  # default: first-see

        fastapi_app.state.meta = meta_mock
        fastapi_app.state.redis = redis_mock
        return meta_mock, redis_mock
    ```

    Tests (todos `async def test_X(client, stub_app_state)`):

    1. `test_get_challenge_returns_challenge_when_verify_token_matches`:
       `r = await client.get("/webhooks/meta?hub.mode=subscribe&hub.verify_token=test-verify-token-do-not-use-in-prod&hub.challenge=CHALLENGE123")`.
       Assert `r.status_code == 200`, `r.text == "CHALLENGE123"`.

    2. `test_get_challenge_returns_403_when_verify_token_mismatches`:
       `r = await client.get("/webhooks/meta?hub.mode=subscribe&hub.verify_token=wrong&hub.challenge=X")`.
       Assert `r.status_code == 403`.

    3. `test_get_challenge_returns_403_when_mode_not_subscribe`:
       `r = await client.get("/webhooks/meta?hub.mode=unsubscribe&hub.verify_token=test-verify-token-do-not-use-in-prod&hub.challenge=X")`.
       Assert `r.status_code == 403`.

    4. `test_post_valid_hmac_text_message_triggers_echo`:
       `body = _inbound_text_payload(text="hola"); sig = _sign(body)`.
       `r = await client.post("/webhooks/meta", content=body, headers={"X-Hub-Signature-256": sig, "Content-Type": "application/json"})`.
       Assert `r.status_code == 200`; meta_mock.send_text llamado 1x con `to="15555550100"`, `body="echo: hola"`; redis_mock.set llamado con `key=b"wa:msg:wamid.test1"`, `nx=True`, `ex=86400`.

    5. `test_post_invalid_hmac_returns_401`:
       `body = _inbound_text_payload(); bad_sig = "sha256=" + "0" * 64`.
       Assert `r.status_code == 401`; meta_mock.send_text NO llamado.

    6. `test_post_missing_signature_header_returns_401`:
       Sin header `X-Hub-Signature-256`. Assert 401; send_text NO llamado.

    7. `test_post_duplicate_message_id_skips_echo`:
       `redis_mock.set = AsyncMock(side_effect=[True, None])` (primera vez first-see, segunda vez dup).
       `body = _inbound_text_payload(message_id="wamid.dup")`.
       Enviar el mismo body dos veces. Assert ambas responses 200; meta_mock.send_text llamado EXACTAMENTE 1x.

    8. `test_post_non_allowlisted_sender_skips_echo`:
       `body = _inbound_text_payload(from_="19999999999")` (no en allowlist).
       Sign + send. Assert 200; meta_mock.send_text NO llamado; redis_mock.set SÍ llamado (dedup gate runs siempre).

    9. `test_post_image_message_triggers_send_media_ack`:
       `body = _inbound_image_payload()`. Sign + send. Assert 200; meta_mock.send_media_ack llamado con `to="15555550100"`, `media_type="image"`; send_text NO llamado directamente.

    10. `test_post_status_update_acknowledged_without_dispatch`:
        `body = _inbound_status_payload()`. Sign + send. Assert 200; meta_mock.send_text NO llamado; meta_mock.send_media_ack NO llamado; redis_mock.set NO llamado (no message_id en statuses path).

    11. `test_post_malformed_json_returns_200_not_422`:
        `body = b'{"object":"whatsapp_business_account","entry": "BROKEN"}'`. Sign (la firma valida sobre raw bytes — passes HMAC). Assert `r.status_code == 200`; meta_mock.send_text NO llamado.

    12. `test_post_e164_normalization_meta_no_plus_matches_allowlist_with_plus`:
        `body = _inbound_text_payload(from_="15555550100", text="hi")` (Meta envía sin `+`, conftest allowlist `+15555550100`). Sign + send. Assert send_text llamado (normalización works).

    13. `test_post_unsupported_type_logged_and_skipped`:
        Crear payload con `"type":"interactive"`. Sign + send. Assert 200; send_text + send_media_ack ambos NO llamados.

    Notas para implementación tests:
    - El `client` fixture de `conftest.py` ya existe y no arranca lifespan — perfecto.
    - Para las funciones helper que envían bytes raw, usar `httpx.AsyncClient.post(..., content=body, headers=...)` (NO `json=` — eso re-serializa y rompe HMAC).
    - El fixture `stub_app_state` muta `app.state` antes de cada test; pytest_asyncio garantiza limpieza vía `monkeypatch` si lo declaramos como dependency, sino el efecto persiste entre tests — usar `monkeypatch.setattr(fastapi_app.state, "meta", meta_mock)` para auto-cleanup.

    Refactor del fixture para usar monkeypatch (preferido):
    ```
    @pytest.fixture
    def stub_app_state(monkeypatch):
        from app.main import app as fastapi_app
        meta_mock = MagicMock()
        meta_mock.send_text = AsyncMock(return_value="wamid.outbound")
        meta_mock.send_media_ack = AsyncMock(return_value="wamid.outbound.media")
        redis_mock = MagicMock()
        redis_mock.set = AsyncMock(return_value=True)
        monkeypatch.setattr(fastapi_app.state, "meta", meta_mock, raising=False)
        monkeypatch.setattr(fastapi_app.state, "redis", redis_mock, raising=False)
        return meta_mock, redis_mock
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/test_webhooks_meta.py -v</automated>
  </verify>
  <done>
    13 tests pasan; HMAC valida via `compare_digest`; orden HMAC → parse → dedup → allowlist → echo respetado; idempotencia bloquea segundo echo; allowlist filtra correctamente; status updates acknowledged sin dispatch; malformed JSON retorna 200 (no 422); media echo dispatch funciona; `grep -n "==" app/webhooks/meta.py | grep -v "compare_digest"` no encuentra `==` en path HMAC.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire-up en app/main.py — lifespan registra app.state.meta + include_router(meta_router)</name>
  <files>app/main.py</files>
  <action>
    Cambios mínimos sobre el `app/main.py` actual (ver `<merge_strategy>` arriba — Plan 02-03 rebaseará sobre esto):

    1. **Línea 52** (después del existente `from app.integrations.openrouter import get_llm  # noqa: E402`):
       Añadir `from app.integrations.meta_cloud import get_meta_client  # noqa: E402` y `from app.webhooks.meta import router as meta_router  # noqa: E402`.

    2. **Dentro de `lifespan`**, después de `await app.state.checkpointer.setup()` (línea 81), antes del `log.info("lifespan.startup.complete")`:
       ```
       # 4. Meta Cloud API client (httpx singleton — NOT async-resource-heavy,
       #    no __aenter__/__aexit__ needed per PATTERNS.md Pitfall 1).
       app.state.meta = get_meta_client()
       ```

    3. **Después de `app.include_router(health_router)`** (línea 146):
       ```
       app.include_router(meta_router)
       ```

    NO cambiar nada más. NO añadir teardown explícito para `app.state.meta` (httpx.AsyncClient muere con el process).

    Verificar después del edit: `app.main:app` carga sin errores; las rutas `/webhooks/meta` GET + POST aparecen en `app.routes`.
  </action>
  <verify>
    <automated>uv run python -c "from app.main import app; routes = {(r.path, getattr(r, 'methods', None)) for r in app.routes}; assert ('/webhooks/meta', frozenset({'GET'})) in routes or any('/webhooks/meta' == p and m and 'GET' in m for p, m in routes); assert any('/webhooks/meta' == p and m and 'POST' in m for p, m in routes); print('routes OK:', [(p, sorted(m) if m else None) for p, m in routes if '/webhooks' in p])" && uv run pytest tests/test_webhooks_meta.py tests/test_integrations_meta_cloud.py tests/test_features_handoff_echo.py tests/test_health.py tests/test_llm_factory.py tests/test_settings.py -v</automated>
  </verify>
  <done>
    `/webhooks/meta` GET + POST registradas en el app; `app.state.meta` se setea al iniciar lifespan (verificable cuando arranca real, no en tests offline); todos los tests existentes + nuevos (≥30 tests totales) pasan; mypy `--strict app/` verde; pre-commit verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Internet → POST /webhooks/meta | Untrusted JSON + signature header; HMAC es la primera defensa antes de cualquier parsing |
| Internet → GET /webhooks/meta | Untrusted query params; verify_token comparison gates |
| Webhook handler → Redis | Idempotency state; binary-safe (b'1' bytes) |
| Webhook handler → MetaCloudClient | Outbound POST a graph.facebook.com cruza egress; SecretStr.get_secret_value() solo en builder del header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-04 | Tampering, Spoofing | Atacante envía POST a `/webhooks/meta` con payload arbitrario | mitigate | `_verify_signature` con `hmac.compare_digest` (D-16); 401 + structured log (sin body raw) en mismatch |
| T-02-05 | Information disclosure | Timing attack sobre HMAC verify revela prefix length | mitigate | `hmac.compare_digest` es constant-time (Pitfall 4) |
| T-02-06 | Tampering | Atacante replay del mismo webhook payload para forzar echo duplicado | mitigate | `redis.set('wa:msg:{id}', nx=True, ex=86400)` retorna None en duplicate → skip side effects (D-14, D-15) |
| T-02-07 | Spoofing, Information disclosure | LANDA echoea texto a un número arbitrario que un atacante engaña a Meta para reenviar | mitigate | `is_echo_allowed` allowlist gate (D-02 + Pitfall 8). Fail-closed: empty allowlist → nada se echoea |
| T-02-08 | Information disclosure | Raw phone number aparece en structlog JSON output | mitigate | `_hash_phone` siempre (PATTERNS.md cross-cutting + RESEARCH Pitfall 9); structlog PHONE_RE como segunda línea de defensa (logging.py:67) |
| T-02-09 | Denial of service | Atacante envía 1000 POSTs con HMAC inválido — costo: 1000 hashlib.sha256 + Redis miss | accept | HMAC + SHA-256 son cheap (~µs); rate-limit a nivel infra (Meta + Railway) y multi-nivel app-side queda diferido a F5 |
| T-02-10 | Information disclosure | Excepción dentro del handler escapa a stderr con stack trace que contiene secret | mitigate | `_verify_signature` raisea boolean (no secret en exc); el `try/except` en echo dispatch loguea `type(exc).__name__` solo (patrón T-01-15 reutilizado) |
| T-02-11 | Information disclosure | `body` o `to` del outbound `send_text` aparece en log | mitigate | `MetaCloudClient.send_text` loguea solo `to_hash=_hash_phone(to)`, `body_len=len(body)`, `wamid` — nunca `body` o `to` raw |
| T-02-12 | Tampering | `MetaError` parsing falla y la excepción enmascara el código real → operador no ve el error de Meta | accept | F2 deja `r.raise_for_status()` directamente; F4 puede envolver con `MetaError.model_validate_json(r.content)` para logging estructurado del código Meta. Out of scope F2 |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced en F2 (todos ya estaban en pyproject.toml + RESEARCH Package Legitimacy Audit aprobado en Plan 02-01) |
</threat_model>

<verification>
- `uv run pytest -v` → ≥30 tests verdes (12 de F1 + 10 echo + 5 sync meta + 4 async meta + 13 webhook).
- `uv run mypy --strict app/` verde.
- `uv run python -c "from app.main import app; print(sorted({r.path for r in app.routes}))"` lista `/webhooks/meta` además de las existentes.
- `grep -nE '\\b==\\b' app/webhooks/meta.py | grep -v "compare_digest\\|hub_verify_token =="` retorna 0 hits en path HMAC (verify_token `==` es explícitamente OK — no es HMAC, solo config-time check).
- `grep -n "from_phone" app/webhooks/meta.py app/integrations/meta_cloud.py` retorna 0 hits — no raw phone logging.
</verification>

<success_criteria>
- `MetaCloudClient.send_text(to, body)` retorna `wamid` y POSTea a `/v21.0/{phone_id}/messages` con body `OutboundText` shape.
- `get_meta_client()` cached singleton; `META_API_VERSION == "v21.0"`.
- `GET /webhooks/meta` responde challenge en plain text cuando verify_token match; 403 cuando no.
- `POST /webhooks/meta` valida HMAC con `compare_digest`; rechaza 401 en mismatch o header ausente; tolera malformed JSON con 200; respeta orden HMAC → parse → dedup → allowlist → echo.
- Idempotencia Redis bloquea segundo echo del mismo `message_id` por 24h.
- Allowlist E.164-normaliza ambos lados (Meta sin `+`, env var con `+`).
- Media echo dispatch para image/audio/sticker/video/document/voice/location.
- Status updates acknowledged sin dispatch.
- `app/main.py` registra `app.state.meta` en lifespan y monta `meta_router`.
- 32+ tests verdes (12 existentes F1 + 10 echo + 9 meta + 13 webhook); mypy `--strict` verde; pre-commit verde.
</success_criteria>

<output>
Create `.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-02-SUMMARY.md` cuando done con:
- Output de `pytest -v` (test count, pass/fail por archivo)
- Output de `grep -nE '\\b==\\b' app/webhooks/meta.py` (debería ser 0 hits en HMAC path, solo verify_token comparison config-time)
- Lista de rutas registradas en `app` (incluyendo `/webhooks/meta`)
- Confirmación: orden HMAC → parse → dedup → allowlist → echo verificado por test `test_post_duplicate_message_id_skips_echo`
- Confirmación: ningún `raw phone` aparece en logs (grep `from_phone` retorna 0)
- Nota para Plan 02-03: este plan ya tocó `app/main.py` (líneas X-Y diff); Plan 02-03 rebasea sumando `app.state.softseguros` + endpoint `/test/poliza/{id}` debajo de lo agregado acá. No remover nada.
- Nota para Plan 02-04: la URL pública del webhook (`https://landa-agent-service-production.up.railway.app/webhooks/meta`) ya está lista para que el operador la suscriba en Meta dashboard con `WA_VERIFY_TOKEN` que ya está en CONTEXT
</output>
