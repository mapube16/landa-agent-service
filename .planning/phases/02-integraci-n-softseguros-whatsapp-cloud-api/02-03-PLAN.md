---
phase: 02-integraci-n-softseguros-whatsapp-cloud-api
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/integrations/_circuit.py
  - app/integrations/softseguros.py
  - app/main.py
  - tests/test_integrations_softseguros.py
  - tests/test_softseguros_readonly.py
  - CLAUDE.md
autonomous: true
requirements:
  - REQ-2.1   # Q&A inbound: identificación cliente por póliza (consumo SoftSeguros)
  - REQ-2.2   # SoftSeguros tiempo real (cache + circuit breaker)
estimated_complexity: M

must_haves:
  truths:
    - "async_call(breaker, coro_fn, *args, **kwargs) en app/integrations/_circuit.py: (1) si breaker.current_state == 'open' raisea pybreaker.CircuitBreakerError; (2) await coro_fn(*args, **kwargs); (3) on success: with breaker._lock: breaker.state.on_success(); on failure: breaker.state.on_failure(exc) + re-raise (RESEARCH Pattern 2 + Pitfall 2)"
    - "SoftSegurosClient SOLO tiene _get(path, **params) como primitivo HTTP — operator READ-ONLY directive. NO existen _post / _put / _patch / _delete / create_* / update_* / modify_* / set_* en la clase. (Excepción: el módulo top-level tiene _refresh_token que hace POST a /api-token-auth/ — eso es auth bootstrap, no modifica datos del cliente.)"
    - "Public methods (D-13): get_poliza(poliza_id) → GET /api/poliza/{id}/, get_cliente(cliente_id) → GET /api/cliente/{id}/, get_estado(poliza_id) → GET /api/estadopoliza/{id}/, get_pagos(poliza_id) → GET /api/pagopoliza/?poliza_id={id}. Todos pasan por cache → tenacity-retry → async_call(breaker) → _get → httpx"
    - "Cache (D-11): Redis read-through con key softseguros:{poliza_id}:{query_type} TTL 60s. Hit → return JSON decoded. Miss → upstream call → set NX EX 60 → return. Cache failure (Redis down) → bypass cache, log warning, continue con upstream (cache no debe romper el path crítico)"
    - "Tenacity outer (RESEARCH Pattern 2): @retry(retry=retry_if_exception_type((httpx.HTTPError, httpx.TimeoutException)), stop=stop_after_attempt(3), wait=wait_exponential(multiplier=0.5, max=4), reraise=True). pybreaker.CircuitBreakerError NO está en retry_if list → breaker open bubbles instantly. **CRITICAL** — documentar este orden con comment explicit en el código"
    - "pybreaker (D-11): softseguros_breaker = CircuitBreaker(fail_max=5, reset_timeout=30, name='softseguros'). Module-level singleton. Cuando abre: log.error('softseguros.breaker.open', reset_in=30) — Sentry captura via structlog→Sentry bridge ya wireado en Phase 1"
    - "Token auth (D-12, RESEARCH Pattern 5): _token_holder = {'v': None} module-level dict mutable. _token_lock = asyncio.Lock(). _get_token() con double-check inside lock (Pitfall 5). _refresh_token_on_401() invalida + re-obtiene. POST a /api-token-auth/ con json={'username': ..., 'password': ...}, response.json()['token'] (DRF shape, RESEARCH 'Code Examples — SoftSeguros token request')"
    - "Auth header en cada request: 'Authorization': f'Token {token}' (NO Bearer — DRF TokenAuthentication, RESEARCH 'Note: Token <space> <value>'). En _get: si response.status_code == 401, invocar _refresh_token_on_401, retry once. Si segundo intento también 401: raise (tenacity NO retrya 401 — auth issue, no transient)"
    - "GET /test/poliza/{poliza_id} en app/main.py (D-10): mismo patrón que /test/llm (main.py:149-164). Llama request.app.state.softseguros.get_poliza(poliza_id), retorna {poliza, latency_ms}. NO LLM, NO transformation. Para verificación operacional, gated/removed en F5"
    - "app/main.py lifespan registra app.state.softseguros = get_softseguros_client() después del checkpointer setup (NO async-resource-heavy — PATTERNS Pitfall 1). Plan 02-02 ya añadió app.state.meta — este plan rebasea sumando .softseguros debajo"
    - "tests/test_softseguros_readonly.py (CI GUARD — operator special request): introspecciona SoftSegurosClient con inspect.getmembers, falla si cualquier method name (case-insensitive) contiene 'post', 'put', 'patch', 'delete', 'create', 'update', 'set_', 'modify_'. Excepción explícita para '_get' (el único HTTP primitive permitido). Test debe correr siempre — no marked como slow / skip"
    - "tests/test_integrations_softseguros.py: factory cache identity, _get_token cached + refresh on 401, retry transitorio (httpx.HTTPError 3 veces → 4th vez ok), breaker open after 5 failures, cache hit no llama httpx, cache miss llama httpx + set Redis"
    - "CLAUDE.md actualizado: bajo '## Reglas críticas → Don't' agregar línea: '❌ No agregar métodos write (POST/PUT/PATCH/DELETE) en `SoftSegurosClient`. El bot es READ-ONLY contra SoftSeguros por diseño. Adding write methods requires explicit ADR + threat model + PROJECT.md scope update.'"
  artifacts:
    - path: app/integrations/_circuit.py
      provides: "async_call(breaker, coro_fn) wrapper implementado — pybreaker asyncio compensator"
      exports: ["async_call"]
    - path: app/integrations/softseguros.py
      provides: "SoftSegurosClient real (READ-ONLY) + get_softseguros_client + module-level breaker + token holder + asyncio.Lock"
      exports: ["SoftSegurosClient", "get_softseguros_client"]
      contains: "softseguros_breaker"
      contains: "_token_lock"
    - path: app/main.py
      provides: "Lifespan registra app.state.softseguros; GET /test/poliza/{poliza_id} endpoint"
      contains: "app.state.softseguros"
      contains: "/test/poliza"
    - path: tests/test_integrations_softseguros.py
      provides: "Unit tests: factory cache, token refresh, retry, breaker, cache hit/miss"
    - path: tests/test_softseguros_readonly.py
      provides: "CI GUARD — fails build if SoftSegurosClient ever grows a write method (operator special request)"
    - path: CLAUDE.md
      provides: "Don't rule añadida: NO write methods en SoftSegurosClient sin ADR + threat model + scope update"
  key_links:
    - from: app/integrations/softseguros.py
      to: app/integrations/_circuit.py
      via: "async_call(softseguros_breaker, fn) en cada _get"
      pattern: "async_call\\(softseguros_breaker"
    - from: app/integrations/softseguros.py
      to: app/config/settings.py
      via: "settings.softseguros.username/password/base_url en _refresh_token + factory"
      pattern: "settings\\.softseguros"
    - from: app/integrations/softseguros.py
      to: app.state.redis
      via: "cache read/write via Redis client inyectado en constructor"
      pattern: "redis\\.(get|set)"
    - from: app/main.py
      to: app/integrations/softseguros.py
      via: "app.state.softseguros = get_softseguros_client(); request.app.state.softseguros.get_poliza(...)"
      pattern: "get_softseguros_client|app\\.state\\.softseguros"
---

<objective>
Implementar el cliente SoftSeguros end-to-end (httpx async + tenacity + pybreaker + Redis cache + asyncio.Lock token refresh) con el invariante operator-requested **READ-ONLY**: SOLO el primitivo `_get` existe; ningún `_post`/`_put`/`_patch`/`_delete` sobre datos del cliente. CI guard test `tests/test_softseguros_readonly.py` enforcea el invariante via introspección. CLAUDE.md actualizado con regla explícita. Endpoint operacional `GET /test/poliza/{poliza_id}` para Plan 02-04 smoke verification.

Purpose: Cubrir REQ-2.1 (Q&A inbound — base de identificación por póliza) + REQ-2.2 (caché + circuit breaker) sin tocar lógica del bot. Garantizar via código + CI + docs que el bot **nunca** puede modificar datos en SoftSeguros, incluso si futuro feature work intenta agregar un método write (CI lo bloquea, CLAUDE.md lo previene, código architecture lo dificulta).

Output: Cliente SoftSeguros funcional + READ-ONLY enforcement triple (architecture + CI + docs), `/test/poliza/{id}` ready para smoke con credenciales reales en Plan 02-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-CONTEXT.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-RESEARCH.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-PATTERNS.md
@CLAUDE.md
@app/config/settings.py
@app/config/redis.py
@app/integrations/openrouter.py
@app/integrations/_circuit.py
@app/integrations/softseguros.py
@app/main.py
@app/models/softseguros.py
@tests/conftest.py
@tests/test_llm_factory.py
</context>

<merge_strategy>
Plan 02-03 corre en Wave 2 paralelo a Plan 02-02. Ambos modifican `app/main.py`. Plan 02-02 corre primero por convención (registra `app.state.meta` + `meta_router`). Plan 02-03 rebasea sumando, NO remueve nada:

1. **Imports** (después de los que añadió Plan 02-02): `from app.integrations.softseguros import get_softseguros_client  # noqa: E402`.
2. **Lifespan** (después de `app.state.meta = get_meta_client()` que Plan 02-02 añadió):
   ```
   # 5. SoftSeguros client (httpx singleton; reads Redis from app.state.redis
   #    inside its methods — no direct dependency here on the lifespan side).
   app.state.softseguros = get_softseguros_client()
   ```
3. **Endpoint** (después de `app.include_router(meta_router)` que Plan 02-02 añadió, junto a los `/test/llm` + `/test/sentry` existentes):
   ```
   @app.get("/test/poliza/{poliza_id}")
   async def test_poliza(poliza_id: str, request: Request) -> dict[str, Any]:
       """Verify SoftSeguros client end-to-end (D-10). Gated/removed in F5."""
       t0 = time.perf_counter()
       client = request.app.state.softseguros
       poliza = await client.get_poliza(poliza_id)
       return {"poliza": poliza, "latency_ms": round((time.perf_counter() - t0) * 1000, 1)}
   ```

NO cambiar las firmas ni el orden de los imports que Plan 02-02 añadió. NO remover `meta_router` ni `app.state.meta`. Si el merge da conflicto en `app/main.py`, ambos changes son aditivos y disjuntos en líneas — el resolver es "keep both".
</merge_strategy>

<tasks>

<task type="auto">
  <name>Task 1: Implementar async_call wrapper en app/integrations/_circuit.py + SoftSegurosClient real (READ-ONLY) en app/integrations/softseguros.py</name>
  <files>app/integrations/_circuit.py, app/integrations/softseguros.py</files>
  <action>
    **Archivo 1 — `app/integrations/_circuit.py`** (reemplaza skeleton de Plan 02-01, ~50 líneas finales — RESEARCH Pattern 2 + Pitfall 2):

    Mantener docstring del skeleton. Implementar:

    Imports: `from __future__ import annotations`, `from collections.abc import Awaitable, Callable`, `from typing import Any, TypeVar`, `import pybreaker`.

    ```
    T = TypeVar("T")


    async def async_call(
        breaker: pybreaker.CircuitBreaker,
        coro_fn: Callable[..., Awaitable[T]],
        *args: Any,
        **kwargs: Any,
    ) -> T:
        """Run an async function under a (sync-API) pybreaker.

        pybreaker 1.4.1 has no native asyncio support — ``breaker.call(coro_fn, *args)``
        would call ``coro_fn`` synchronously (returning a coroutine object), store
        the coroutine as the "return value" without ever awaiting it, and mark
        the call as success even when the upstream actually fails. This wrapper:

          1. Checks ``breaker.current_state``; raises ``CircuitBreakerError`` if
             the circuit is open BEFORE awaiting the coroutine (fail-fast).
          2. Awaits the coroutine.
          3. On success: calls ``breaker.state.on_success()`` under the breaker's
             internal lock so counters/state stay consistent.
          4. On exception: calls ``breaker.state.on_failure(exc)`` and re-raises
             so the caller (tenacity, etc.) can decide whether to retry.

        ``breaker.state.on_success()`` / ``on_failure(exc)`` are pseudo-public
        hooks (no leading underscore on the methods themselves; pybreaker's own
        ``call()`` invokes them internally). Stable across pybreaker 1.x but
        not contractually guaranteed — if a future pybreaker minor breaks these,
        migrate to ``purgatory`` (native async circuit breaker).
        """
        if breaker.current_state == "open":
            raise pybreaker.CircuitBreakerError(f"Circuit '{breaker.name}' is open")
        try:
            result = await coro_fn(*args, **kwargs)
        except Exception as exc:
            with breaker._lock:
                breaker.state.on_failure(exc)
            raise
        else:
            with breaker._lock:
                breaker.state.on_success()
            return result


    __all__ = ["async_call"]
    ```

    **Archivo 2 — `app/integrations/softseguros.py`** (reemplaza skeleton, ~220 líneas finales — RESEARCH Patterns 2, 5 + operator READ-ONLY):

    Mantener el docstring del skeleton **incluyendo el bloque READ-ONLY INVARIANT obligatorio**.

    Imports: `from __future__ import annotations`, `import asyncio, hashlib, json`, `from functools import lru_cache`, `from typing import Any`, `import httpx, pybreaker, structlog`, `from tenacity import retry, retry_if_exception_type, stop_after_attempt, wait_exponential`, `from app.config.settings import settings`, `from app.integrations._circuit import async_call`, `from app.models.softseguros import PolizaRaw`.

    Logger: `log = structlog.get_logger("integrations.softseguros")`.

    **Module-level state (singletons):**
    ```
    # Circuit breaker — module singleton. Per-process state (Pitfall 6) is
    # acceptable for v1 (1 worker on Railway, ~50 msg/day).
    softseguros_breaker = pybreaker.CircuitBreaker(
        fail_max=5,
        reset_timeout=30,
        name="softseguros",
    )

    # Token holder + lock (Pattern 5 — token cached in process memory,
    # refreshed on 401, protected by asyncio.Lock to avoid thundering herd).
    _token_holder: dict[str, str | None] = {"v": None}
    _token_lock = asyncio.Lock()
    ```

    **Auth helpers (top-level, NOT methods — RESEARCH Pattern 5):**
    ```
    async def _get_token(http: httpx.AsyncClient) -> str:
        """Return a cached DRF auth token; fetch if missing (Pattern 5).

        Double-check inside the lock so concurrent callers don't stampede the
        upstream ``/api-token-auth/`` endpoint (Pitfall 5 — thundering herd).
        """
        if _token_holder["v"]:
            return _token_holder["v"]
        async with _token_lock:
            if _token_holder["v"]:
                return _token_holder["v"]
            r = await http.post(
                "/api-token-auth/",
                json={
                    "username": settings.softseguros.username.get_secret_value(),
                    "password": settings.softseguros.password.get_secret_value(),
                },
            )
            r.raise_for_status()
            token = r.json()["token"]
            _token_holder["v"] = token
            log.info("softseguros.token.fetched", token_hash=hashlib.sha256(token.encode()).hexdigest()[:8])
            return token


    async def _refresh_token_on_401(http: httpx.AsyncClient) -> str:
        """Invalidate the cached token and fetch a new one (after a 401)."""
        async with _token_lock:
            _token_holder["v"] = None
        return await _get_token(http)
    ```

    **Class `SoftSegurosClient`** (READ-ONLY — operator directive):
    ```
    class SoftSegurosClient:
        """READ-ONLY HTTP client for SoftSeguros (DRF backend).

        See module docstring for the READ-ONLY INVARIANT. CI guard
        ``tests/test_softseguros_readonly.py`` introspects this class and
        fails the build if write verbs appear in method names.
        """

        def __init__(self, http: httpx.AsyncClient, redis: Any | None = None) -> None:
            """Construct the client.

            ``redis`` is optional in tests (cache bypasses cleanly when None).
            In production, it is set in the factory below from
            ``app.state.redis`` via a lazy accessor — but for compatibility with
            unit tests we accept it via constructor and the factory injects it
            on first access.
            """
            self._http = http
            self._redis = redis  # may be None in tests; production sets it via factory init

        # ------------------------------------------------------------------
        # Internal HTTP primitive — the ONLY one. Adding _post / _put / etc.
        # is prohibited; CI guard test fails the build if it happens.
        # ------------------------------------------------------------------

        @retry(
            retry=retry_if_exception_type((httpx.HTTPError, httpx.TimeoutException)),
            stop=stop_after_attempt(3),
            wait=wait_exponential(multiplier=0.5, max=4),
            reraise=True,
        )
        async def _get(self, path: str, **params: Any) -> dict[str, Any]:
            """Authenticated GET with retry (outer) + breaker (inner).

            Order is INVARIANT:
              - tenacity @retry wraps the method (handles transient httpx errors).
              - async_call(softseguros_breaker, ...) is the inner call — when
                the breaker is OPEN it raises CircuitBreakerError, which is NOT
                in tenacity's retry_if list, so tenacity stops retrying instantly.
              - On 401 → refresh token + retry ONCE (auth issue is not transient,
                tenacity should not loop on it).
            """
            async def _do() -> dict[str, Any]:
                token = await _get_token(self._http)
                headers = {"Authorization": f"Token {token}"}  # DRF format: 'Token <hex>' (NOT Bearer).
                r = await self._http.get(path, params=params, headers=headers)
                if r.status_code == 401:
                    # Refresh once + retry the request manually (do NOT let tenacity loop).
                    new_token = await _refresh_token_on_401(self._http)
                    headers = {"Authorization": f"Token {new_token}"}
                    r = await self._http.get(path, params=params, headers=headers)
                r.raise_for_status()
                return r.json()  # type: ignore[no-any-return]

            return await async_call(softseguros_breaker, _do)

        # ------------------------------------------------------------------
        # Cache layer — Redis read-through (D-11, CLAUDE.md "Cachea consultas
        # SoftSeguros en Redis con TTL 60s clave (poliza_id, query_type)").
        # ------------------------------------------------------------------

        async def _cached_get(self, poliza_id: str, query_type: str, path: str, **params: Any) -> PolizaRaw:
            """Read-through cache wrapper around _get.

            Key: ``softseguros:{poliza_id}:{query_type}``. TTL 60s (D-11).
            If Redis is unavailable, log warning and bypass cache (do NOT fail
            the upstream call — cache outage must not break the bot).
            """
            cache_key = f"softseguros:{poliza_id}:{query_type}".encode("utf-8")
            cached: bytes | None = None
            if self._redis is not None:
                try:
                    cached = await self._redis.get(cache_key)
                except Exception as exc:  # noqa: BLE001
                    log.warning("softseguros.cache.read_error", error_type=type(exc).__name__)
            if cached is not None:
                return json.loads(cached)  # type: ignore[no-any-return]

            data = await self._get(path, **params)

            if self._redis is not None:
                try:
                    await self._redis.set(cache_key, json.dumps(data).encode("utf-8"), ex=60)
                except Exception as exc:  # noqa: BLE001
                    log.warning("softseguros.cache.write_error", error_type=type(exc).__name__)
            return data

        # ------------------------------------------------------------------
        # Public READ methods (D-13). NO write counterparts — see CI guard.
        # ------------------------------------------------------------------

        async def get_poliza(self, poliza_id: str) -> PolizaRaw:
            """GET /api/poliza/{poliza_id}/."""
            return await self._cached_get(poliza_id, "poliza", f"/api/poliza/{poliza_id}/")

        async def get_cliente(self, cliente_id: str) -> PolizaRaw:
            """GET /api/cliente/{cliente_id}/.

            Note: caching is keyed by cliente_id under the same namespace
            (collision-free because cliente_id ≠ poliza_id in practice).
            """
            return await self._cached_get(cliente_id, "cliente", f"/api/cliente/{cliente_id}/")

        async def get_estado(self, poliza_id: str) -> PolizaRaw:
            """GET /api/estadopoliza/{poliza_id}/."""
            return await self._cached_get(poliza_id, "estado", f"/api/estadopoliza/{poliza_id}/")

        async def get_pagos(self, poliza_id: str) -> PolizaRaw:
            """GET /api/pagopoliza/?poliza_id={poliza_id}."""
            return await self._cached_get(poliza_id, "pagos", "/api/pagopoliza/", poliza_id=poliza_id)
    ```

    **Factory** (PATTERNS.md):
    ```
    @lru_cache(maxsize=1)
    def get_softseguros_client() -> SoftSegurosClient:
        """Return the cached SoftSegurosClient singleton.

        Redis is injected lazily on first access via app.state — see lifespan
        wiring in app/main.py (Plan 02-03 Task 2). Tests bypass the factory
        and construct SoftSegurosClient directly with stubbed http + redis.

        NEVER instantiate httpx clients to SoftSeguros elsewhere — always go
        through this factory so cache + circuit breaker + token refresh stay
        coherent.
        """
        limits = httpx.Limits(max_keepalive_connections=10, max_connections=20, keepalive_expiry=30.0)
        timeout = httpx.Timeout(10.0, connect=3.0, read=10.0, write=3.0, pool=2.0)
        http = httpx.AsyncClient(
            base_url=settings.softseguros.base_url,
            timeout=timeout,
            limits=limits,
            headers={"User-Agent": "landa-agent-service/0.1.0"},
        )
        return SoftSegurosClient(http=http, redis=None)  # redis bound in lifespan
    ```

    `__all__ = ["SoftSegurosClient", "get_softseguros_client"]`.

    NOTAS críticas:
    - El módulo top-level `_get_token` hace POST a `/api-token-auth/` — eso es **auth bootstrap**, no escritura de datos del cliente. El CI guard chequea métodos en `SoftSegurosClient` (instance methods); las top-level functions están exentas.
    - Si tests fallan porque `Redis` no está disponible para la factory, eso es esperado: la factory deja `redis=None` y `_cached_get` bypassa cleanly. Plan 02-03 Task 2 inyecta `redis` en lifespan después del `get_softseguros_client()` call.
  </action>
  <verify>
    <automated>uv run python -c "import inspect; from app.integrations.softseguros import SoftSegurosClient, get_softseguros_client, softseguros_breaker; forbidden = {'post','put','patch','delete','create','update','set_','modify_'}; methods = [n for n,_ in inspect.getmembers(SoftSegurosClient, predicate=inspect.isfunction)]; offenders = {m for m in methods if any(v in m.lower() and m != '_get' for v in forbidden)}; assert not offenders, f'WRITE METHODS DETECTED: {offenders}'; assert '_get' in methods; assert 'get_poliza' in methods; assert 'get_cliente' in methods; assert 'get_estado' in methods; assert 'get_pagos' in methods; assert softseguros_breaker.name == 'softseguros'; assert softseguros_breaker.fail_max == 5; print('READ-ONLY invariant OK + breaker config OK')" && uv run mypy --strict app/integrations/_circuit.py app/integrations/softseguros.py</automated>
  </verify>
  <done>
    `async_call` implementado con state.on_success/on_failure under breaker._lock; `SoftSegurosClient` solo expone `_get` como HTTP primitive (zero write verbs); 4 public read methods (poliza/cliente/estado/pagos); module-level breaker `fail_max=5 reset_timeout=30`; token holder + asyncio.Lock con double-check; cache read-through con bypass tolerante a Redis down; factory cached con httpx.AsyncClient pool warm; mypy `--strict` verde en ambos archivos.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire app.state.softseguros en lifespan + GET /test/poliza/{poliza_id} endpoint en app/main.py</name>
  <files>app/main.py</files>
  <action>
    Aplicar el `<merge_strategy>` definido arriba — cambios aditivos sobre lo que Plan 02-02 ya tocó (Plan 02-02 corre primero por convención lex):

    1. **Imports** (debajo de los que Plan 02-02 añadió, manteniendo `# noqa: E402`):
       ```
       from app.integrations.softseguros import get_softseguros_client  # noqa: E402
       ```

    2. **Dentro de `lifespan`**, después de `app.state.meta = get_meta_client()` (que Plan 02-02 añadió como "step 4"):
       ```
       # 5. SoftSeguros client (httpx singleton; factory leaves redis=None,
       #    we bind it from app.state.redis here so the cache layer is wired).
       app.state.softseguros = get_softseguros_client()
       app.state.softseguros._redis = app.state.redis  # late-binding cache backend
       ```

    NOTA: el acceso a `_redis` (private attr) está justificado en este single call site — la alternativa sería un setter público `bind_redis(redis)` pero agrega API surface sin valor, ya que es uso interno de lifespan. Documentar con un comment `# late-binding allowed: lifespan is the single owner of singletons`.

    3. **Endpoint** después de `app.include_router(meta_router)` (que Plan 02-02 añadió), junto a los `/test/*` existentes:
       ```
       @app.get("/test/poliza/{poliza_id}")
       async def test_poliza(poliza_id: str, request: Request) -> dict[str, Any]:
           """Verify SoftSeguros client end-to-end (D-10).

           Returns raw upstream JSON without LLM transformation. NOT for
           production — gate or remove in F5.
           """
           t0 = time.perf_counter()
           client = request.app.state.softseguros
           poliza = await client.get_poliza(poliza_id)
           return {
               "poliza": poliza,
               "latency_ms": round((time.perf_counter() - t0) * 1000, 1),
           }
       ```

    NO modificar el orden de los `/test/llm` y `/test/sentry` existentes (líneas 149-174). NO remover nada que Plan 02-02 haya añadido.

    Verificar post-edit:
    - `from app.main import app` carga sin errores.
    - Ruta `/test/poliza/{poliza_id}` registrada (`GET`).
    - `app.state.softseguros` se setea en lifespan startup.
  </action>
  <verify>
    <automated>uv run python -c "from app.main import app; routes = {(r.path, frozenset(getattr(r, 'methods', set()) or set())) for r in app.routes}; assert any(p == '/test/poliza/{poliza_id}' and 'GET' in m for p, m in routes), f'route not registered. routes={sorted({(p, sorted(m)) for p, m in routes if \"test\" in p or \"webhook\" in p})}'; print('routes OK')" && uv run mypy --strict app/main.py</automated>
  </verify>
  <done>
    `app/main.py` registra `app.state.softseguros = get_softseguros_client()` en lifespan + late-binds `._redis = app.state.redis`; expone `GET /test/poliza/{poliza_id}`; mypy `--strict` verde; cambios de Plan 02-02 preservados (meta_router, app.state.meta).
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests — tests/test_integrations_softseguros.py (unit) + tests/test_softseguros_readonly.py (CI guard) + actualizar CLAUDE.md con regla Don't write-methods</name>
  <files>tests/test_integrations_softseguros.py, tests/test_softseguros_readonly.py, CLAUDE.md</files>
  <action>
    **Archivo 1 — `tests/test_softseguros_readonly.py`** (NUEVO — CI GUARD, operator special request, ~50 líneas):

    Este test es el corazón del READ-ONLY enforcement. Falla el CI si alguien añade `_post`, `_put`, `_patch`, `_delete`, `create_*`, `update_*`, `set_*`, `modify_*` en `SoftSegurosClient`.

    ```
    """CI guard: SoftSegurosClient must remain READ-ONLY (operator directive).

    Per CLAUDE.md '## Reglas críticas → Don't', adding a write method
    (POST/PUT/PATCH/DELETE over client data) to SoftSegurosClient requires:
      1. ADR in .planning/adr/
      2. Threat model update in PROJECT.md §"Seguridad"
      3. Scope update approved by operator

    This test introspects the class and fails the build if any method name
    (case-insensitive) contains one of the forbidden verbs. The single
    exception is ``_get`` — the only HTTP primitive allowed.

    Top-level functions like ``_get_token`` and ``_refresh_token_on_401`` are
    NOT inspected here; they live at module scope, hit ``/api-token-auth/``
    (auth bootstrap, not client-data writes), and are documented in the
    module docstring's READ-ONLY INVARIANT block.
    """

    from __future__ import annotations

    import inspect


    FORBIDDEN_VERBS: frozenset[str] = frozenset({
        "post", "put", "patch", "delete",
        "create", "update", "set_", "modify_",
    })

    # Methods that are part of the public/internal READ contract — explicit allowlist.
    # Any method NOT here that matches a forbidden verb fails the build.
    METHOD_ALLOWLIST: frozenset[str] = frozenset({
        "_get",          # the ONLY HTTP primitive (verb 'get', not in forbidden list)
        "_cached_get",   # cache wrapper around _get
        "get_poliza",
        "get_cliente",
        "get_estado",
        "get_pagos",
    })


    def test_softseguros_client_has_no_write_methods() -> None:
        """Fail if any method name contains a forbidden write verb."""
        from app.integrations.softseguros import SoftSegurosClient

        methods = [
            name for name, _ in inspect.getmembers(SoftSegurosClient, predicate=inspect.isfunction)
            if not name.startswith("__")
        ]

        offenders: list[str] = []
        for name in methods:
            lname = name.lower()
            if any(verb in lname for verb in FORBIDDEN_VERBS):
                if name not in METHOD_ALLOWLIST:
                    offenders.append(name)

        assert not offenders, (
            f"SoftSegurosClient has unauthorized write method(s): {offenders!r}. "
            f"Adding writes requires ADR + threat model + scope update. See CLAUDE.md."
        )


    def test_softseguros_client_has_only_one_http_primitive() -> None:
        """Sanity: confirm _get is the only underscore-prefixed HTTP primitive."""
        from app.integrations.softseguros import SoftSegurosClient

        http_verbs = {"_get", "_post", "_put", "_patch", "_delete", "_head", "_options"}
        methods = {
            name for name, _ in inspect.getmembers(SoftSegurosClient, predicate=inspect.isfunction)
        }
        primitives = methods & http_verbs

        assert primitives == {"_get"}, (
            f"Expected ONLY '_get' as HTTP primitive on SoftSegurosClient; found {primitives!r}. "
            f"_cached_get is allowed (cache wrapper). All write verbs are forbidden."
        )


    def test_softseguros_module_docstring_declares_readonly() -> None:
        """Ensure the READ-ONLY INVARIANT block is present in the module docstring."""
        from app.integrations import softseguros

        doc = softseguros.__doc__ or ""
        assert "READ-ONLY INVARIANT" in doc, (
            "app/integrations/softseguros.py module docstring must include the "
            "'READ-ONLY INVARIANT' block. See CLAUDE.md '## Reglas críticas → Don't'."
        )
    ```

    **Archivo 2 — `tests/test_integrations_softseguros.py`** (NUEVO — unit tests, ~180 líneas):

    Imports:
    ```
    import asyncio
    import json
    from unittest.mock import AsyncMock, MagicMock

    import httpx
    import pybreaker
    import pytest
    ```

    Tests:

    1. **Factory cache identity** (sync):
       ```
       def test_get_softseguros_client_is_singleton() -> None:
           from app.integrations.softseguros import get_softseguros_client
           a = get_softseguros_client()
           b = get_softseguros_client()
           assert a is b
       ```

    2. **Factory http base_url + headers** (sync):
       ```
       def test_get_softseguros_client_uses_settings() -> None:
           from app.integrations.softseguros import get_softseguros_client
           c = get_softseguros_client()
           assert str(c._http.base_url) == "https://app.softseguros.com/"
           assert "landa-agent-service" in c._http.headers["User-Agent"]
       ```

    3. **Token fetch caches in module-level holder** (async):
       Construir un `SoftSegurosClient` con `http` stubbed. Stub `_http.post("/api-token-auth/", ...)` para retornar `{"token": "abc123"}`. Llamar `_get_token(c._http)` dos veces; assert post llamado solo 1x; assert `_token_holder["v"] == "abc123"`.
       NOTA: cleanup del `_token_holder` entre tests vía fixture `monkeypatch.setattr(softseguros, "_token_holder", {"v": None})`.

    4. **Token thundering-herd protection** (async):
       Stub post con `side_effect` que duerme 0.05s antes de retornar. Lanzar 10 concurrent `await _get_token(http)` con `asyncio.gather`. Assert post llamado solo 1x (double-check inside lock funciona — Pitfall 5).

    5. **_get retries on transient httpx errors** (async):
       Stub `_http.get` con `side_effect=[httpx.ConnectError("boom"), httpx.ConnectError("boom"), httpx.Response(200, json={"ok":True}, request=...)]`. Llamar `await client._get("/x")`. Assert get llamado 3x; result == `{"ok":True}`.
       Reset breaker en setup: `softseguros_breaker.close()`.

    6. **_get gives up after 3 attempts (tenacity stop_after_attempt)** (async):
       Stub `_http.get` con `side_effect=[httpx.ConnectError("boom")] * 4`. Assert `pytest.raises(httpx.ConnectError)`; get llamado exactamente 3 veces.

    7. **Breaker open short-circuits the call** (async):
       Forzar `softseguros_breaker.open()` (estado abierto). Stub `_http.get`. Llamar `await client._get("/x")`. Assert `pytest.raises(pybreaker.CircuitBreakerError)`; `_http.get` NUNCA llamado (fail-fast).
       Teardown: `softseguros_breaker.close()`.

    8. **Cache hit bypasses upstream** (async):
       Stub `redis.get` para retornar `b'{"saldo": 100}'`. Construir client con redis mock. Llamar `await client.get_poliza("123")`. Assert `_http.get` NUNCA llamado; result == `{"saldo": 100}`.

    9. **Cache miss writes back** (async):
       Stub `redis.get` → `None`. Stub `_http.get` → `{"saldo": 200}`. Llamar `await client.get_poliza("123")`. Assert `redis.set` llamado con key `b"softseguros:123:poliza"`, value `b'{"saldo": 200}'`, `ex=60`.

    10. **Cache failure falls through to upstream (no crash)** (async):
        Stub `redis.get` → `side_effect=ConnectionError("redis down")`. Stub `_http.get` → `{"x":1}`. Llamar `await client.get_poliza("123")`. Assert result == `{"x":1}`; NO exception propagada del cache layer.

    11. **401 triggers refresh + retry once** (async):
        Stub `_http.get` con `side_effect=[httpx.Response(401, request=...), httpx.Response(200, json={"ok":True}, request=...)]`. Stub `_http.post("/api-token-auth/", ...)` para retornar nuevo token. Llamar `await client._get("/x")`. Assert get llamado 2x; post llamado al menos 1x (refresh); result `{"ok":True}`.

    12. **Get methods route to correct paths** (async, paramétrico):
        ```
        @pytest.mark.parametrize("method,expected_path,kwargs", [
            ("get_poliza", "/api/poliza/123/", {}),
            ("get_cliente", "/api/cliente/456/", {}),
            ("get_estado", "/api/estadopoliza/123/", {}),
            ("get_pagos", "/api/pagopoliza/", {"poliza_id": "123"}),
        ])
        ```
        Stub `_http.get` con captured args; llamar el método con `"123"` o `"456"`; assert URL + params.

    Fixtures (top del archivo):
    ```
    @pytest.fixture(autouse=True)
    def _reset_singletons(monkeypatch):
        """Reset token holder + breaker between tests."""
        from app.integrations import softseguros
        monkeypatch.setattr(softseguros, "_token_holder", {"v": None}, raising=False)
        monkeypatch.setattr(softseguros, "_token_lock", asyncio.Lock(), raising=False)
        try:
            softseguros.softseguros_breaker.close()
        except Exception:
            pass


    @pytest.fixture
    def mocked_client():
        """SoftSegurosClient with stubbed httpx + redis."""
        from app.integrations.softseguros import SoftSegurosClient
        http = MagicMock(spec=httpx.AsyncClient)
        http.get = AsyncMock()
        http.post = AsyncMock()
        redis = MagicMock()
        redis.get = AsyncMock(return_value=None)
        redis.set = AsyncMock(return_value=True)
        return SoftSegurosClient(http=http, redis=redis)
    ```

    **Archivo 3 — `CLAUDE.md`** — actualizar la sección `## Reglas críticas → ### Don't`:

    Localizar en `CLAUDE.md` la sección `### Don't` (línea ~145 — la lista que arranca con `- ❌ **No hardcodear modelos LLM**`). Después del último item (`- ❌ **No exponer al LLM tools de tipo \`list_all_*\` o \`search_*\`**`), añadir el nuevo item:

    ```
    - ❌ **No agregar métodos write (POST/PUT/PATCH/DELETE) en `SoftSegurosClient`**. El bot es READ-ONLY contra SoftSeguros por diseño. Adding write methods requires:
      1. ADR documentado en `.planning/adr/`
      2. Threat model actualizado en PROJECT.md §"Seguridad"
      3. PROJECT.md scope explícitamente actualizado para incluir el write
      4. Operator approval explícito
      CI guard `tests/test_softseguros_readonly.py` falla el build automáticamente si aparecen verbos prohibidos (`post`/`put`/`patch`/`delete`/`create_`/`update_`/`set_`/`modify_`) en method names de `SoftSegurosClient`. Excepción: top-level `_get_token` y `_refresh_token_on_401` POSTean a `/api-token-auth/` (auth bootstrap, no escritura de datos del cliente) — esto está documentado en el módulo docstring READ-ONLY INVARIANT.
    ```

    NO cambiar nada más en CLAUDE.md. NO tocar el preámbulo, ni el stack table, ni el "Last updated" footer (a menos que el ejecutor lo considere apropiado al cierre del Plan).
  </action>
  <verify>
    <automated>uv run pytest tests/test_softseguros_readonly.py tests/test_integrations_softseguros.py -v && grep -q "No agregar métodos write" CLAUDE.md && echo "CLAUDE.md updated"</automated>
  </verify>
  <done>
    `tests/test_softseguros_readonly.py` 3 tests pasan (incluyendo `test_softseguros_client_has_no_write_methods`); `tests/test_integrations_softseguros.py` 12+ tests pasan (factory + token + retry + breaker + cache hit/miss/error + 401 refresh + paramétrico paths); CLAUDE.md actualizado con la nueva regla Don't (grep confirma); mypy `--strict` verde; pre-commit verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| FastAPI app → SoftSeguros (graph) | TLS por default httpx (verify=True); creds en SecretStr |
| SoftSeguros response → cache (Redis) | JSON-serialized, binary; no PII redaction de cache content (vive 60s in-process) |
| LLM (F3+) → SoftSegurosClient | LLM nunca puede invocar write methods porque no existen (architectural enforcement + CI guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-13 | Tampering (of business logic) | LLM-induced write operation contra SoftSeguros (póliza modificada, pago confirmado upstream sin aprobación) | mitigate | Operator special request: arquitectura + CI guard + CLAUDE.md rule. Triple enforcement. `SoftSegurosClient` simplemente NO TIENE métodos write — no es policy en runtime, es ausencia de superficie de ataque |
| T-02-14 | Spoofing | Atacante inyecta credenciales falsas via env var, cliente intenta auth contra `/api-token-auth/` con username arbitrario | accept | Operator es único actor con write access a Railway env vars. Si compromise de Railway, hay problemas más grandes que SoftSeguros |
| T-02-15 | Information disclosure | Token de auth aparece en log al hacer refresh | mitigate | `_get_token` loguea `token_hash=sha256(token)[:8]` (non-reversible correlation id), nunca el token en raw |
| T-02-16 | Denial of service | Token-refresh thundering herd (50 concurrent 401s) | mitigate | `asyncio.Lock` + double-check (Pitfall 5) → exactly-1 refresh per process |
| T-02-17 | Tampering | Stale data from cache después de outage real de SoftSeguros (cache 60s sobrevive al outage breve) | accept | TTL 60s es deliberado (D-11). Si SoftSeguros se cae > 60s, breaker abre + escala — F3+ wirea el escalation |
| T-02-18 | Denial of service | Atacante envía 1000 polizas distintas, cada uno hit upstream, satura DPG | mitigate (parcial) | F2 acepta este vector; rate limiting multi-nivel viene en F5 (D-04 deferred). Breaker abre tras 5 failures consecutivos si DPG empieza a 429-ear |
| T-02-19 | Information disclosure | `r.json()` en `_get` puede retornar PII del cliente en JSON que después llega al log si caller hace `log.info(...,data=result)` | mitigate (defense in depth) | Plan 02-03 NO loguea el body de la response. Plan 02-04 endpoint `/test/poliza/{id}` retorna data al caller HTTP — esto es deliberado para verificación operacional, gated en F5 |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced en F2 (tenacity + pybreaker ya en pyproject.toml + RESEARCH Package Legitimacy Audit aprobado en Plan 02-01) |
</threat_model>

<verification>
- `uv run pytest tests/test_integrations_softseguros.py tests/test_softseguros_readonly.py -v` → ≥15 tests verdes.
- `uv run pytest -v` → ≥45 tests verdes globales (12 F1 + 10 echo + 9 meta + 13 webhook + 15 softseguros).
- `uv run mypy --strict app/` verde.
- `uv run python -c "from app.main import app; routes = sorted({r.path for r in app.routes}); print(routes)"` lista `/health`, `/test/llm`, `/test/sentry`, `/test/poliza/{poliza_id}`, `/webhooks/meta`.
- `grep -nE 'async def (_post|_put|_patch|_delete|create_|update_|modify_|set_)' app/integrations/softseguros.py` retorna 0 hits.
- `grep -c "No agregar métodos write" CLAUDE.md` retorna 1.
- `grep -c "READ-ONLY INVARIANT" app/integrations/softseguros.py` retorna ≥1.
- `pre-commit run --all-files` verde (tenacity + pybreaker ya estaban en `additional_dependencies` desde Plan 02-01).
</verification>

<success_criteria>
- `async_call(breaker, coro_fn)` wrapper implementado correctamente (fail-fast on open + state.on_success/on_failure under lock).
- `SoftSegurosClient` con `_get` como ÚNICO HTTP primitive; cero verbos write en method names; 4 public read methods (`get_poliza`, `get_cliente`, `get_estado`, `get_pagos`).
- Tenacity outer (3 retries exp backoff) + pybreaker inner (5 failures → 30s open) stack working.
- Token auth `Token <hex>` (DRF, NO Bearer); refresh on 401; `asyncio.Lock` + double-check protege contra thundering herd.
- Redis cache read-through (TTL 60s) con bypass tolerante a Redis down.
- `app/main.py` registra `app.state.softseguros` en lifespan + expone `GET /test/poliza/{poliza_id}`.
- `tests/test_softseguros_readonly.py` 3 tests pasan (READ-ONLY invariant, single HTTP primitive, docstring).
- `tests/test_integrations_softseguros.py` 12+ tests pasan.
- `CLAUDE.md` `## Reglas críticas → Don't` incluye la regla nueva con los 4 prerrequisitos para agregar writes.
- mypy `--strict` verde; pre-commit verde.
</success_criteria>

<output>
Create `.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-03-SUMMARY.md` cuando done con:
- Output de `pytest -v` (test count, pass/fail por archivo, énfasis en `test_softseguros_readonly.py`)
- Output de `grep -nE 'async def (_post|_put|_patch|_delete|create_|update_|modify_|set_)' app/integrations/softseguros.py` (debería ser 0 hits)
- Output de `grep -c "No agregar métodos write" CLAUDE.md` (debería ser 1)
- Lista de rutas de `app/main.py` (debe incluir `/test/poliza/{poliza_id}` + las añadidas por Plan 02-02 `/webhooks/meta`)
- Confirmación: orden tenacity outer / pybreaker inner verificado por test `test_breaker_open_short_circuits_the_call` (breaker open bubble instantly, NO se llama `_http.get`)
- Confirmación: token holder se resetea entre tests vía autouse fixture; thundering-herd test pasa con 10 concurrent + 1 post upstream
- Nota para Plan 02-04: `GET /test/poliza/{poliza_id}` está listo; el operador necesita setear `SOFTSEGUROS_USERNAME` y `SOFTSEGUROS_PASSWORD` en Railway (Plan 02-04 Task X) antes de poder hacer un curl real
- Nota para futuros desarrolladores: cualquier intento de añadir `_post`/`_put`/`_patch`/`_delete` en `SoftSegurosClient` será bloqueado por el CI guard `test_softseguros_client_has_no_write_methods`. Para legítimamente añadir un write: ADR + threat model update + PROJECT.md scope + operator approval — y entonces actualizar el `METHOD_ALLOWLIST` del CI guard explicitamente
</output>
