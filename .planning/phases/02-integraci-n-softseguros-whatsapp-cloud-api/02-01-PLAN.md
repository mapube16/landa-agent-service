---
phase: 02-integraci-n-softseguros-whatsapp-cloud-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config/settings.py
  - app/models/meta.py
  - app/models/softseguros.py
  - app/integrations/_circuit.py
  - app/integrations/meta_cloud.py
  - app/integrations/softseguros.py
  - app/webhooks/meta.py
  - app/features/handoff/echo.py
  - .pre-commit-config.yaml
  - tests/conftest.py
autonomous: true
requirements:
  - REQ-2.1   # Q&A inbound: identificación cliente por póliza (settings carry-forward — exposes SoftSeguros creds)
  - REQ-2.3   # HMAC X-Hub-Signature-256 (settings carry-forward — exposes webhook_secret)
  - REQ-2.4   # Idempotencia por message_id (settings carry-forward — exposes Redis is already wired via Phase 1)
estimated_complexity: M

must_haves:
  truths:
    - "settings.whatsapp.token / phone_id / business_account_id / webhook_secret / verify_token / echo_allowlist cargan desde env vars con prefix WA_ (D-01 D-02 D-06 D-08 D-17)"
    - "settings.softseguros.base_url / username / password cargan desde env vars con prefix SOFTSEGUROS_ (D-01)"
    - "settings.langsmith.workspace_id: SecretStr | None = None acepta env var LANGSMITH_WORKSPACE_ID opcional (RESEARCH Pitfall 9 — Phase 1 follow-up)"
    - "WA_ECHO_ALLOWLIST=+1...,+2... parsea a list[str] vía el mismo Annotated[list[str], NoDecode] + _split_csv pattern de LLMSettings.fallbacks_conversation (settings.py:102-113)"
    - "Pydantic v2 models en app/models/meta.py: InboundEnvelope (con entry[].changes[].value.messages[]: from, id, timestamp, type, text.body | image | audio | sticker | location discriminator), OutboundText (messaging_product=whatsapp, recipient_type=individual, to, type=text, text.body), MetaError (error.message, type, code, error_subcode, fbtrace_id) — todos solo con los campos que el echo handler / outbound send literalmente leen, sin sobre-modelar (PATTERNS.md Pitfall 10)"
    - "app/integrations/meta_cloud.py declara META_API_VERSION: Final[str] = 'v21.0' y META_BASE_URL: Final[str] = f'https://graph.facebook.com/{META_API_VERSION}' como constantes módulo-level (D-08 + PATTERNS.md)"
    - "Módulos skeleton existen con docstring + __all__ + un único raise NotImplementedError en cada función pública (las firmas finales las completan plans 02-02 y 02-03): app/integrations/_circuit.py (async_call), app/integrations/meta_cloud.py (MetaCloudClient + get_meta_client), app/integrations/softseguros.py (SoftSegurosClient + get_softseguros_client), app/webhooks/meta.py (router), app/features/handoff/echo.py (is_echo_allowed, format_echo, format_media_echo), app/models/meta.py, app/models/softseguros.py"
    - ".pre-commit-config.yaml mypy additional_dependencies incluye tenacity==9.1.4 + pybreaker==1.4.1 (mismas versiones que pyproject.toml — patrón de plans 01-02 y 01-04 — RESEARCH Pre-commit section)"
    - "tests/conftest.py _test_env autouse fixture incluye placeholders para WA_TOKEN, WA_PHONE_ID, WA_BUSINESS_ACCOUNT_ID, WA_WEBHOOK_SECRET, WA_VERIFY_TOKEN, WA_ECHO_ALLOWLIST, SOFTSEGUROS_BASE_URL, SOFTSEGUROS_USERNAME, SOFTSEGUROS_PASSWORD — Settings() instancia clean en tests"
    - "uv run pytest pasa (los tests existentes siguen verdes); uv run mypy --strict app pasa sin errores; pre-commit run --all-files pasa"
  artifacts:
    - path: app/config/settings.py
      provides: "WhatsAppSettings + SoftSegurosSettings agregados al root Settings; LangSmithSettings.workspace_id añadido"
      contains: "class WhatsAppSettings"
      contains: "class SoftSegurosSettings"
      contains: "workspace_id"
    - path: app/models/meta.py
      provides: "Pydantic v2 InboundEnvelope, OutboundText, MetaError — solo campos que el handler / sender leen"
      exports: ["InboundEnvelope", "OutboundText", "MetaError"]
    - path: app/models/softseguros.py
      provides: "PolizaRaw passthrough (dict[str, Any] wrapper) — sin modelar fields hasta tener una response real (RESEARCH Open Question #1, A1)"
      exports: ["PolizaRaw"]
    - path: app/integrations/_circuit.py
      provides: "async_call(breaker, fn) wrapper skeleton — implementación final en Plan 02-03"
      exports: ["async_call"]
    - path: app/integrations/meta_cloud.py
      provides: "Skeleton con META_API_VERSION + META_BASE_URL + MetaCloudClient class + get_meta_client factory cached singleton (implementación final en Plan 02-02)"
      exports: ["MetaCloudClient", "get_meta_client", "META_API_VERSION", "META_BASE_URL"]
    - path: app/integrations/softseguros.py
      provides: "Skeleton con SoftSegurosClient class (solo _get HTTP primitive declarado — NO _post/_put/_patch/_delete) + get_softseguros_client factory (implementación final en Plan 02-03)"
      exports: ["SoftSegurosClient", "get_softseguros_client"]
    - path: app/webhooks/meta.py
      provides: "Skeleton con APIRouter(prefix='/webhooks', tags=['meta']) y stubs GET/POST (implementación final en Plan 02-02)"
      exports: ["router"]
    - path: app/features/handoff/echo.py
      provides: "Skeleton con is_echo_allowed / format_echo / format_media_echo stubs (implementación final en Plan 02-02)"
      exports: ["is_echo_allowed", "format_echo", "format_media_echo"]
    - path: .pre-commit-config.yaml
      provides: "mypy additional_dependencies extendido con tenacity==9.1.4 + pybreaker==1.4.1"
    - path: tests/conftest.py
      provides: "_test_env extendido con placeholders WA_* + SOFTSEGUROS_*"
  key_links:
    - from: app/integrations/meta_cloud.py
      to: app/config/settings.py
      via: "settings.whatsapp.token / phone_id durante construcción del httpx client (skeleton)"
      pattern: "settings\\.whatsapp\\."
    - from: app/integrations/softseguros.py
      to: app/config/settings.py
      via: "settings.softseguros.base_url / username / password en _refresh_token (skeleton)"
      pattern: "settings\\.softseguros\\."
    - from: app/webhooks/meta.py
      to: app/config/settings.py
      via: "settings.whatsapp.webhook_secret / verify_token (skeleton)"
      pattern: "settings\\.whatsapp\\."
    - from: app/features/handoff/echo.py
      to: app/config/settings.py
      via: "settings.whatsapp.echo_allowlist (skeleton)"
      pattern: "settings\\.whatsapp\\.echo_allowlist"
---

<objective>
Ground-truth todos los contracts que Plans 02-02 y 02-03 van a consumir: settings (`WhatsAppSettings`, `SoftSegurosSettings`, `LangSmithSettings.workspace_id`), modelos Pydantic v2 (`InboundEnvelope`, `OutboundText`, `MetaError`, `PolizaRaw`), módulos skeleton con firmas finales en su lugar pero cuerpo `raise NotImplementedError`, pre-commit mypy `additional_dependencies` extendido con `tenacity` + `pybreaker`, y test fixtures con env vars placeholders. Wave 1 — desbloquea Wave 2.

Purpose: interface-first execution. Plans 02-02 y 02-03 reciben en el plan-set las firmas exactas a implementar (no scavenger hunt). El pre-commit fix se aplica acá para evitar el fail-on-first-commit dance que ya pasó en plans 01-02 y 01-04.

Output: 8 archivos nuevos en `app/` + 2 modified (`app/config/settings.py`, `.pre-commit-config.yaml`, `tests/conftest.py`); todos los tests del repo siguen pasando; mypy `--strict` verde; pre-commit verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-CONTEXT.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-RESEARCH.md
@.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-PATTERNS.md
@CLAUDE.md
@app/config/settings.py
@app/integrations/openrouter.py
@.pre-commit-config.yaml
@tests/conftest.py
@.planning/phases/01-setup-infra/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender app/config/settings.py con WhatsAppSettings + SoftSegurosSettings + LangSmithSettings.workspace_id, y tests/conftest.py con env-var placeholders</name>
  <files>app/config/settings.py, tests/conftest.py</files>
  <action>
    **Archivo 1 — `app/config/settings.py`**:

    Agregar dos `BaseSettings` subclases nuevas (D-01, D-02, D-08, D-17) siguiendo exactamente el patrón existente (settings.py:43-66 `PostgresSettings`, settings.py:81-113 `LLMSettings._split_csv`). Registrar ambas en el root `Settings` vía `Field(default_factory=...)` (settings.py:184-190). Extender `__all__`.

    Detalles obligatorios:
    - `WhatsAppSettings(BaseSettings)` con `model_config = SettingsConfigDict(env_prefix="WA_", env_file=".env", extra="ignore", case_sensitive=False)`. Campos:
      - `token: SecretStr` (REQUIRED, D-06)
      - `phone_id: str` (REQUIRED, D-06)
      - `business_account_id: str | None = None` (informational, D-06)
      - `webhook_secret: SecretStr` (REQUIRED — HMAC POST, D-16)
      - `verify_token: SecretStr` (REQUIRED — GET challenge, D-17)
      - `echo_allowlist: Annotated[list[str], NoDecode] = Field(default_factory=list)` con `@field_validator("echo_allowlist", mode="before")` `_split_csv` clonado verbatim del existente en `LLMSettings._split_csv` (settings.py:105-113). Esto permite `WA_ECHO_ALLOWLIST=+1...,+2...,+3...` como una sola env var.
    - `SoftSegurosSettings(BaseSettings)` con `env_prefix="SOFTSEGUROS_"`. Campos:
      - `base_url: str = "https://app.softseguros.com/"` (D-13)
      - `username: SecretStr` (REQUIRED, D-01)
      - `password: SecretStr` (REQUIRED, D-01)
    - `LangSmithSettings`: agregar `workspace_id: SecretStr | None = None` (RESEARCH Pitfall 9 — Phase 1 follow-up, env var ya existe en Railway). NO breaking change — opcional.
    - Registración en root `Settings`:
      `whatsapp: WhatsAppSettings = Field(default_factory=WhatsAppSettings)` y `softseguros: SoftSegurosSettings = Field(default_factory=SoftSegurosSettings)` agregadas debajo de las 7 existentes (después de `sentry:`).
    - `__all__` extendido con `"WhatsAppSettings"`, `"SoftSegurosSettings"` (alfabético — ver el orden actual settings.py:197-207).

    **NO** crear nuevas clases para Chatwoot / Lambda — esos son F3/F6, fuera de scope.
    **NO** modificar otras `BaseSettings` existentes excepto añadir `workspace_id` a `LangSmithSettings`.

    **Archivo 2 — `tests/conftest.py`**:

    Extender el `_test_env` autouse fixture (líneas 12-24) con placeholders para que `Settings()` instancie en tests sin levantar `ValidationError`:

    - `os.environ.setdefault("WA_TOKEN", "wa-test-token")`
    - `os.environ.setdefault("WA_PHONE_ID", "1267241483129092")`
    - `os.environ.setdefault("WA_BUSINESS_ACCOUNT_ID", "1451322196454283")`
    - `os.environ.setdefault("WA_WEBHOOK_SECRET", "test-webhook-secret-do-not-use-in-prod")`
    - `os.environ.setdefault("WA_VERIFY_TOKEN", "test-verify-token-do-not-use-in-prod")`
    - `os.environ.setdefault("WA_ECHO_ALLOWLIST", "+15555550100,+15555550101")` (dos números E.164 ficticios para que tests de allowlist tengan datos)
    - `os.environ.setdefault("SOFTSEGUROS_BASE_URL", "https://app.softseguros.com/")`
    - `os.environ.setdefault("SOFTSEGUROS_USERNAME", "test-user")`
    - `os.environ.setdefault("SOFTSEGUROS_PASSWORD", "test-pass")`
    - `LANGSMITH_WORKSPACE_ID` se deja unset (es `None` por default — válido).

    Mantener el comentario explicativo arriba del fixture en español (mismo registro que el resto del archivo).
  </action>
  <verify>
    <automated>uv run python -c "from app.config.settings import settings; assert settings.whatsapp.phone_id == '1267241483129092'; assert settings.whatsapp.token.get_secret_value() == 'wa-test-token'; assert settings.whatsapp.echo_allowlist == ['+15555550100', '+15555550101']; assert settings.softseguros.base_url == 'https://app.softseguros.com/'; assert settings.softseguros.username.get_secret_value() == 'test-user'; assert settings.langsmith.workspace_id is None; print('settings OK')"</automated>
  </verify>
  <done>
    `Settings()` instancia clean con env vars de conftest; `WhatsAppSettings.echo_allowlist` parsea CSV a `list[str]`; `SoftSegurosSettings.base_url` tiene default sano; `LangSmithSettings.workspace_id` opcional acepta `None`; mypy `--strict` pasa; tests existentes siguen verdes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear app/models/meta.py + app/models/softseguros.py — Pydantic v2 inbound/outbound shapes</name>
  <files>app/models/meta.py, app/models/softseguros.py</files>
  <action>
    **Archivo 1 — `app/models/meta.py`** (per CONTEXT D-09 "Claude's Discretion" + PATTERNS.md Pitfall 10 — modelar SOLO lo que el echo handler lee):

    Estructura del módulo:
    - `from __future__ import annotations` + module docstring que explica el scope (solo F2 — F3 puede añadir interactive/button/contacts/etc.)
    - Modelos Pydantic v2 con `model_config = ConfigDict(extra="ignore")` para tolerar campos que Meta agrega sin romper.

    Inbound (lo que llega en `POST /webhooks/meta`, RESEARCH "Code Examples"):

    - `MessageText(BaseModel)` con campo `body: str`.
    - `InboundMessage(BaseModel)`:
      - `from_: str = Field(alias="from")` (Python no permite `from` como atributo — alias)
      - `id: str`
      - `timestamp: str`
      - `type: Literal["text", "image", "audio", "sticker", "location", "video", "document", "voice", "contacts", "interactive", "button", "unknown"]` (whitelist conocida + `"unknown"` fallback para tipos que Meta agregue post-v21)
      - `text: MessageText | None = None` (solo poblado cuando `type=="text"`)
      - Otros media payloads (image/audio/sticker/etc.) — NO modelar; el handler solo necesita `type`.
    - `ChangeValue(BaseModel)`:
      - `messaging_product: Literal["whatsapp"] = "whatsapp"`
      - `metadata: dict[str, Any] = Field(default_factory=dict)` (no extraer fields — `display_phone_number` y `phone_number_id` no se usan en F2)
      - `messages: list[InboundMessage] | None = None`
      - `statuses: list[dict[str, Any]] | None = None` (D-05 dice "messages + message_status"; F2 NO procesa statuses, solo los acknowledge — passthrough dict)
    - `Change(BaseModel)` con campos `value: ChangeValue` y `field: str`.
    - `Entry(BaseModel)` con `id: str` y `changes: list[Change]`.
    - `InboundEnvelope(BaseModel)`:
      - `object: str` (e.g. `"whatsapp_business_account"`)
      - `entry: list[Entry]`

    Outbound (lo que `MetaCloudClient.send_text` posta a `POST /v21.0/{phone_id}/messages`, RESEARCH "Code Examples"):

    - `OutboundTextBody(BaseModel)` con campo `body: str`.
    - `OutboundText(BaseModel)`:
      - `messaging_product: Literal["whatsapp"] = "whatsapp"`
      - `recipient_type: Literal["individual"] = "individual"`
      - `to: str`
      - `type: Literal["text"] = "text"`
      - `text: OutboundTextBody`

    Error (lo que viene en response body cuando Meta retorna 4xx/5xx, RESEARCH "Code Examples"):

    - `MetaErrorDetail(BaseModel)`:
      - `message: str`
      - `type: str | None = None`
      - `code: int`
      - `error_subcode: int | None = None`
      - `fbtrace_id: str | None = None`
    - `MetaError(BaseModel)` con campo `error: MetaErrorDetail`.

    `__all__` final con `["Change", "ChangeValue", "Entry", "InboundEnvelope", "InboundMessage", "MessageText", "MetaError", "MetaErrorDetail", "OutboundText", "OutboundTextBody"]` (orden alfabético).

    **NO**:
    - NO modelar interactive/button/contacts (Pitfall 10 — F3 escalará cuando necesite).
    - NO usar `from typing import Annotated` para `Field(alias=...)` — Pydantic v2 native syntax.
    - NO `Config` clase legacy — solo `model_config = ConfigDict(...)`.

    **Archivo 2 — `app/models/softseguros.py`** (per RESEARCH Open Question #1 + Assumptions A1 — schema desconocido, passthrough OK en F2):

    - Module docstring explicando que en F2 no modelamos fields porque no tenemos una response real de DPG. F3 (cuando QA tools aterricen) añadirá modelos específicos por endpoint.
    - `PolizaRaw = dict[str, Any]` (alias de tipo) — pasthrough deliberado. Documentado en docstring del alias.
    - `__all__ = ["PolizaRaw"]`.

    Nota: el alias `dict[str, Any]` cumple el contrato sin sobre-modelar. Si Task 1 de Plan 02-03 captura una response real, el SUMMARY puede registrarla como hint para F3.
  </action>
  <verify>
    <automated>uv run python -c "from app.models.meta import InboundEnvelope, OutboundText, MetaError, MessageText, InboundMessage; from app.models.softseguros import PolizaRaw; ie = InboundEnvelope.model_validate_json('{\"object\":\"whatsapp_business_account\",\"entry\":[{\"id\":\"x\",\"changes\":[{\"value\":{\"messaging_product\":\"whatsapp\",\"metadata\":{},\"messages\":[{\"from\":\"16505551234\",\"id\":\"wamid.x\",\"timestamp\":\"1749416383\",\"type\":\"text\",\"text\":{\"body\":\"hola\"}}]},\"field\":\"messages\"}]}]}'); assert ie.entry[0].changes[0].value.messages[0].from_ == '16505551234'; assert ie.entry[0].changes[0].value.messages[0].text.body == 'hola'; out = OutboundText(to='16505551234', text={'body':'echo: hola'}); assert out.model_dump()['messaging_product'] == 'whatsapp'; print('models OK')"</automated>
  </verify>
  <done>
    `InboundEnvelope.model_validate_json(...)` parsea el sample payload de RESEARCH (líneas 654-689) sin errores; alias `from` → `from_` funciona; `OutboundText` serializa con `messaging_product=whatsapp` y `type=text`; `MetaError` parsea el sample error shape; `PolizaRaw` es `dict[str, Any]`; mypy `--strict` pasa.
  </done>
</task>

<task type="auto">
  <name>Task 3: Crear module skeletons (_circuit, meta_cloud, softseguros, webhooks/meta, features/handoff/echo) + extender pre-commit mypy deps</name>
  <files>app/integrations/_circuit.py, app/integrations/meta_cloud.py, app/integrations/softseguros.py, app/webhooks/meta.py, app/features/handoff/echo.py, .pre-commit-config.yaml</files>
  <action>
    Cada skeleton tiene: module docstring explicando "qué será cuando Plan 02-XX lo complete", `from __future__ import annotations`, imports mínimos, firmas finales con bodies `raise NotImplementedError("implemented in Plan 02-0X")`, y `__all__` correcto. Esto es **interface-first execution**: Plans 02-02 y 02-03 reciben firmas exactas, no exploran el repo. mypy `--strict` debe pasar contra los skeletons.

    **Archivo 1 — `app/integrations/_circuit.py`** (~30 líneas, implementación final en Plan 02-03 — RESEARCH Pattern 2 + Pitfall 2):

    Docstring: "Compensador para pybreaker 1.4.1 que no soporta asyncio nativo. Plan 02-03 implementa async_call(breaker, fn, *args, **kwargs) que: (1) checa breaker.current_state == 'open' y raisea CircuitBreakerError; (2) await coro_fn(*args, **kwargs); (3) en éxito: breaker.state.on_success(); en falla: breaker.state.on_failure(exc) + re-raise. **NO usar @breaker como decorator** en async functions — silently mis-records success (RESEARCH Pitfall 2)."

    Firma:
    ```
    async def async_call(
        breaker: pybreaker.CircuitBreaker,
        coro_fn: Callable[..., Awaitable[T]],
        *args: Any,
        **kwargs: Any,
    ) -> T:
    ```

    Body: `raise NotImplementedError("Implemented in Plan 02-03")`.

    `__all__ = ["async_call"]`.

    **Archivo 2 — `app/integrations/meta_cloud.py`** (~50 líneas skeleton — RESEARCH Pattern 1 + PATTERNS.md):

    Docstring: "Cliente HTTP a Meta Cloud API v21.0 (graph.facebook.com). Singleton cached via @lru_cache(maxsize=1) — patrón openrouter.py:96-128. Plan 02-02 implementa send_text() y send_media_ack(). **NEVER instanciar httpx clients a Meta en otro módulo** — siempre via get_meta_client() para conservar connection pool warmth y headers consistency."

    Constantes módulo-level (D-08):
    ```
    from typing import Final
    META_API_VERSION: Final[str] = "v21.0"
    META_BASE_URL: Final[str] = f"https://graph.facebook.com/{META_API_VERSION}"
    ```

    Imports: `httpx`, `structlog`, `functools.lru_cache`, `app.config.settings.settings`, `app.models.meta.OutboundText` (y futuro `MetaError`).

    Class `MetaCloudClient`:
    - `__init__(self, http: httpx.AsyncClient, phone_id: str)` — guarda `self._http` y `self._phone_id`.
    - `async def send_text(self, to: str, body: str) -> str` — devuelve el `wamid` del mensaje enviado. Body: `raise NotImplementedError("Implemented in Plan 02-02")`.
    - `async def send_media_ack(self, to: str, media_type: str) -> str` — devuelve wamid. Body: `raise NotImplementedError("Implemented in Plan 02-02")`. (Llamado cuando inbound es imagen/audio/sticker/etc. y format_media_echo retorna el texto.)

    Factory:
    ```
    @lru_cache(maxsize=1)
    def get_meta_client() -> MetaCloudClient:
        """Return the cached MetaCloudClient singleton. Implemented in Plan 02-02."""
        raise NotImplementedError("Implemented in Plan 02-02")
    ```

    `__all__ = ["META_API_VERSION", "META_BASE_URL", "MetaCloudClient", "get_meta_client"]` (orden alfabético).

    **Archivo 3 — `app/integrations/softseguros.py`** (~80 líneas skeleton — RESEARCH Patterns 2, 5 + PATTERNS.md + operator special request READ-ONLY):

    Docstring **debe incluir literalmente** el siguiente bloque (operator special request):

    ```
    READ-ONLY INVARIANT (operator directive, F2)
    ============================================
    Este cliente es READ-ONLY contra SoftSeguros por diseño. NO existe método
    write (POST/PUT/PATCH/DELETE) sobre datos del cliente. El único POST permitido
    es POST /api-token-auth/ (auth bootstrap), que NO modifica datos del cliente.

    Agregar un método write requiere:
    1. ADR documentado en `.planning/adr/`
    2. Threat model actualizado en PROJECT.md §"Seguridad"
    3. Scope update aprobado por el operador

    El CI guard `tests/test_softseguros_readonly.py` (Plan 02-03) introspecciona
    esta clase y falla el build si aparecen verbos prohibidos (post, put, patch,
    delete, create, update, set_, modify_) en method names.
    ```

    Imports: `httpx`, `structlog`, `asyncio`, `functools.lru_cache`, `app.config.settings.settings`, `app.models.softseguros.PolizaRaw`.

    Class `SoftSegurosClient`:
    - `__init__(self, http: httpx.AsyncClient)` — guarda `self._http`.
    - `async def _get(self, path: str, **params: Any) -> dict[str, Any]` — **único primitivo HTTP**. Body: `raise NotImplementedError("Implemented in Plan 02-03")`. NO declarar `_post`, `_put`, `_patch`, `_delete`.
    - `async def _get_token(self) -> str` — gated por `asyncio.Lock` con double-check. Body: `raise NotImplementedError("Implemented in Plan 02-03")`.
    - `async def _refresh_token_on_401(self) -> str` — invalida cache + delega a `_get_token`. Body: `raise NotImplementedError`.
    - Public read methods (D-13):
      - `async def get_poliza(self, poliza_id: str) -> PolizaRaw` — `GET /api/poliza/{poliza_id}/`
      - `async def get_cliente(self, cliente_id: str) -> PolizaRaw` — `GET /api/cliente/{cliente_id}/`
      - `async def get_estado(self, poliza_id: str) -> PolizaRaw` — `GET /api/estadopoliza/{poliza_id}/`
      - `async def get_pagos(self, poliza_id: str) -> PolizaRaw` — `GET /api/pagopoliza/?poliza_id={poliza_id}`
    - Todas las public methods con body `raise NotImplementedError("Implemented in Plan 02-03")`.

    Factory:
    ```
    @lru_cache(maxsize=1)
    def get_softseguros_client() -> SoftSegurosClient:
        """Return the cached SoftSegurosClient singleton. Implemented in Plan 02-03."""
        raise NotImplementedError("Implemented in Plan 02-03")
    ```

    `__all__ = ["SoftSegurosClient", "get_softseguros_client"]`.

    **Archivo 4 — `app/webhooks/meta.py`** (~40 líneas skeleton — RESEARCH Pattern 3, 4 + PATTERNS.md):

    Docstring: "Webhook receiver para Meta Cloud API (D-09). GET /webhooks/meta (challenge — verify_token comparison), POST /webhooks/meta (raw-body → HMAC → parse → dedup → allowlist → echo). Plan 02-02 implementa. **Orden HMAC/parse/dedup/allowlist/echo es invariante** — D-15 lo enforcea. **NEVER read request.json() antes de await request.body()** — Pitfall 1."

    Imports: `structlog`, `fastapi.APIRouter`, `fastapi.Request`, `fastapi.Response`, `fastapi.HTTPException`, `app.config.settings.settings`.

    Body:
    ```
    router = APIRouter(prefix="/webhooks", tags=["meta"])
    log = structlog.get_logger("webhooks.meta")


    @router.get("/meta")
    async def verify(request: Request) -> Response:
        """GET challenge per Meta webhook subscription (D-09, D-17). Implemented in Plan 02-02."""
        raise NotImplementedError("Implemented in Plan 02-02")


    @router.post("/meta")
    async def receive(request: Request) -> Response:
        """POST events: raw body → HMAC → parse → dedup → allowlist → echo. Implemented in Plan 02-02."""
        raise NotImplementedError("Implemented in Plan 02-02")


    __all__ = ["router"]
    ```

    NOTA: la firma `request: Request` (no Pydantic body param) es crítica — Pitfall 10. Plan 02-02 NO debe cambiarla.

    **Archivo 5 — `app/features/handoff/echo.py`** (~30 líneas skeleton — PATTERNS.md):

    Docstring: "Echo bot transicional (D-02). Vive en `features/handoff/` porque es pre-Phase 3 (cuando LangGraph entries reemplacen el echo, este módulo se borra). Pure functions sobre settings + input phone. Plan 02-02 implementa. **E.164 normalization obligatoria** — Pitfall 8 (Meta envía sin `+`, env var con `+`)."

    Imports: `from app.config.settings import settings`.

    Body:
    ```
    def _normalize_e164(raw: str) -> str:
        """Return ``raw`` always prefixed with '+'. Implemented in Plan 02-02."""
        raise NotImplementedError("Implemented in Plan 02-02")


    def is_echo_allowed(phone: str) -> bool:
        """Return True iff phone (after E.164 normalization) is in WA_ECHO_ALLOWLIST. Implemented in Plan 02-02."""
        raise NotImplementedError("Implemented in Plan 02-02")


    def format_echo(text: str) -> str:
        """Return 'echo: <text>'. Implemented in Plan 02-02."""
        raise NotImplementedError("Implemented in Plan 02-02")


    def format_media_echo(media_type: str) -> str:
        """Return 'echo: [<media_type>] received'. Implemented in Plan 02-02."""
        raise NotImplementedError("Implemented in Plan 02-02")


    __all__ = ["format_echo", "format_media_echo", "is_echo_allowed"]
    ```

    **Archivo 6 — `.pre-commit-config.yaml`**:

    Extender el `additional_dependencies` del hook `mypy` (líneas 17-38). Bajo el último comentario (línea 37 `# Plan 01-05 wires ARQ worker REDIS_URL via RedisSettings.from_dsn.`), añadir:

    ```
              # Plan 02-01 introduces tenacity + pybreaker imports in
              # app/integrations/softseguros.py and app/integrations/_circuit.py.
              # mypy isolated env needs them for --strict (pattern from plans 01-02,
              # 01-04, 01-05). Versions match pyproject.toml.
              - tenacity==9.1.4
              - pybreaker==1.4.1
    ```

    Mismo formato/indent que las líneas existentes. NO cambiar nada más en el archivo.
  </action>
  <verify>
    <automated>uv run python -c "import app.integrations._circuit, app.integrations.meta_cloud, app.integrations.softseguros, app.webhooks.meta, app.features.handoff.echo; from app.integrations.meta_cloud import META_API_VERSION, META_BASE_URL; assert META_API_VERSION == 'v21.0'; assert META_BASE_URL == 'https://graph.facebook.com/v21.0'; from app.integrations.softseguros import SoftSegurosClient; forbidden = {'post','put','patch','delete','create','update','set_','modify_'}; names = {n for n in dir(SoftSegurosClient) if not n.startswith('__')}; offenders = {n for n in names for v in forbidden if v in n.lower()}; assert not offenders, f'forbidden write verbs already in skeleton: {offenders}'; print('skeletons OK')" && uv run mypy --strict app/ && uv run pytest -q</automated>
  </verify>
  <done>
    Los 5 archivos skeleton existen con firmas finales + `raise NotImplementedError`; mypy `--strict app/` pasa sin errores (las firmas son válidas); pre-commit con tenacity + pybreaker en `additional_dependencies` no falla; tests existentes siguen verdes; `SoftSegurosClient` introspection ya garantiza zero forbidden write verbs en el skeleton (el CI guard real lo formaliza en Plan 02-03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| env-var input → Settings | Operator-supplied secrets cruzan acá; SecretStr previene leaks via `repr` (settings.py:5-7) |
| Settings → módulos consumidores | Plans 02-02, 02-03 leerán `.get_secret_value()` solo en builders/handlers — nunca en logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Information disclosure | `WA_WEBHOOK_SECRET` / `WA_VERIFY_TOKEN` / `WA_TOKEN` / `SOFTSEGUROS_PASSWORD` impresos en logs vía `repr(settings)` | mitigate | `SecretStr` por default renderiza `**********`; PII_KEYS scrubber en `app/config/logging.py:28-60` ya incluye `wa_token`, `wa_webhook_secret`, `wa_verify_token`, `softseguros_username`, `softseguros_password` |
| T-02-02 | Tampering | `WA_ECHO_ALLOWLIST` parsing acepta valores mal-formados (`"+1,abc,"` ) y los matchea como números válidos | mitigate | `_split_csv` strip-elimina vacíos pero NO valida E.164 shape — esto se delega a Pitfall 8 normalization en Plan 02-02 (defensa en profundidad); F2 acepta este nivel porque la allowlist la setea el operador manualmente, no usuarios |
| T-02-03 | Spoofing | `Settings()` levanta `ValidationError` al boot si falta cualquier required SecretStr → service crash en deploy si operador no setea Railway env vars | accept | Fail-fast es deliberado (settings.py:194 `settings = Settings()` ejecuta al import). Railway smoke en Plan 02-04 verifica antes de declarar F2 done |
| T-02-SC | Tampering | npm/pip/cargo installs (tenacity 9.1.4, pybreaker 1.4.1) | mitigate | RESEARCH Package Legitimacy Audit aprobó ambos (tenacity 80M/mo + Julien Danjou + 13yr; pybreaker 1M/mo + Daniel Fernandes Martins + 14yr). Pre-pinned en `pyproject.toml` desde F1, no installs nuevos en F2. slopcheck no requerido (graceful degrade per RESEARCH section). |
</threat_model>

<verification>
- `uv run python -c "from app.config.settings import settings; print(settings.whatsapp.phone_id, settings.softseguros.base_url, settings.langsmith.workspace_id)"` imprime los placeholders + `None`.
- `uv run python -c "from app.models.meta import InboundEnvelope; InboundEnvelope.model_validate_json(open('.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-RESEARCH.md').read())"` — no aplicable directamente, pero `model_validate_json` con el sample inline en el test del Task 2 sí parsea.
- `uv run mypy --strict app/` pasa.
- `uv run pytest -q` pasa (tests existentes siguen verdes; aún no hay tests nuevos en F2 — esos vienen en plans 02-02 / 02-03).
- `pre-commit run --all-files` pasa (incluyendo el mypy isolated env con tenacity + pybreaker disponibles).
- `grep -nE '_post|_put|_patch|_delete|create_|update_|modify_|set_' app/integrations/softseguros.py | grep -v '^#'` retorna 0 hits — invariante READ-ONLY del skeleton.
</verification>

<success_criteria>
- `app/config/settings.py` exporta `WhatsAppSettings` + `SoftSegurosSettings`, ambos en `__all__` y registrados en root `Settings`; `LangSmithSettings.workspace_id` opcional.
- `app/models/meta.py` exporta `InboundEnvelope` capaz de parsear el sample payload de RESEARCH líneas 654-689; `OutboundText` serializa con shape de Meta `POST /messages`.
- `app/models/softseguros.py` exporta `PolizaRaw = dict[str, Any]` documentado.
- 5 skeletons creados (`_circuit.py`, `meta_cloud.py`, `softseguros.py`, `webhooks/meta.py`, `features/handoff/echo.py`) con firmas finales + `raise NotImplementedError` + `__all__`.
- `META_API_VERSION = "v21.0"` y `META_BASE_URL = "https://graph.facebook.com/v21.0"` como `Final[str]` en `meta_cloud.py`.
- `SoftSegurosClient` skeleton SIN ningún método cuyo nombre contenga `post`/`put`/`patch`/`delete`/`create_`/`update_`/`set_`/`modify_` (operator READ-ONLY directive, baseline para CI guard de Plan 02-03).
- `.pre-commit-config.yaml` mypy `additional_dependencies` extendido con `tenacity==9.1.4` + `pybreaker==1.4.1`.
- `tests/conftest.py` extendido con placeholders WA_* y SOFTSEGUROS_*.
- `mypy --strict app/` verde, `pytest -q` verde, `pre-commit run --all-files` verde.
</success_criteria>

<output>
Create `.planning/phases/02-integraci-n-softseguros-whatsapp-cloud-api/02-01-SUMMARY.md` when done con:
- Diff resumido de `app/config/settings.py` (qué clases nuevas + dónde se registraron en root Settings)
- Diff de `tests/conftest.py` (nuevas env vars)
- Diff de `.pre-commit-config.yaml` (deps añadidas)
- Lista de los 5 skeletons creados con # de líneas cada uno
- Confirmación: `mypy --strict app/`, `pytest -q`, `pre-commit run --all-files` todos verdes
- Confirmación: `grep` sobre `app/integrations/softseguros.py` no encuentra verbos write (baseline operator READ-ONLY)
- Nota para Plans 02-02 y 02-03: las firmas exactas que deben respetar (lista de funciones con su signature; las que cambien rompen el dependency contract)
</output>
